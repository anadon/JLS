<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.anadon</groupId>
  <artifactId>jls</artifactId>
  <!-- v5.0.0: major bump because the (never-reachable) plugin mechanism
       was removed, a feature removal by policy (issue #80) -->
  <version>5.0.1-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>JLS - Java Logic Simulator</name>
  <description>An educational digital logic circuit editor and simulator,
    originally by David A. Poplawski (Michigan Technological University).</description>
  <url>https://github.com/anadon/JLS</url>

  <licenses>
    <license>
      <name>GNU General Public License v3.0</name>
      <url>https://www.gnu.org/licenses/gpl-3.0.txt</url>
    </license>
  </licenses>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- language baseline policy (#92): the floor is the current LTS at
         the time of each raise, revisited once per LTS cycle -->
    <maven.compiler.release>25</maven.compiler.release>
    <!-- reproducible builds: archives get this fixed timestamp instead of
         wall-clock time, so the same commit rebuilds byte-identically;
         bump it alongside the version when cutting a release (#44) -->
    <project.build.outputTimestamp>2026-07-08T00:00:00Z</project.build.outputTimestamp>
  </properties>

  <dependencies>
    <!-- XZ codec for .jls compression (the formerly-vendored xz/ source
         tree is gone; #6 removed it) -->
    <dependency>
      <groupId>org.tukaani</groupId>
      <artifactId>xz</artifactId>
      <version>1.12</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>6.1.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- historical layout: sources in src/, not src/main/java;
         tests live in a sibling test/ tree so they are not swept up
         by the src/ source and resource roots -->
    <sourceDirectory>src</sourceDirectory>
    <testSourceDirectory>test</testSourceDirectory>
    <resources>
      <!-- images and other non-Java files that live inside the source tree -->
      <resource>
        <directory>src</directory>
        <excludes>
          <exclude>**/*.java</exclude>
          <exclude>**/CVS/**</exclude>
          <!-- belt-and-braces: keep Windows Explorer droppings out of the
               jar even if one gets committed again (#47) -->
          <exclude>**/Thumbs.db</exclude>
        </excludes>
      </resource>
      <!-- bundled HTML help content -->
      <resource>
        <directory>resources</directory>
        <excludes>
          <exclude>**/CVS/**</exclude>
          <exclude>**/Thumbs.db</exclude>
        </excludes>
      </resource>
      <!-- single-source version identity: the pom version is filtered
           into jls/version.properties and read by JLSInfo (issue #36) -->
      <resource>
        <directory>src-filtered</directory>
        <filtering>true</filtering>
      </resource>
    </resources>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
        <configuration>
          <compilerArgs>
            <arg>-Xlint:deprecation,removal,unchecked</arg>
            <arg>-Werror</arg>
          </compilerArgs>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.5.0</version>
        <configuration>
          <archive>
            <manifest>
              <!-- dependencies are bundled by the shade plugin below -->
              <mainClass>jls.JLS</mainClass>
              <!-- Implementation-Title/-Version in the manifest (#36) -->
              <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
            </manifest>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <!-- self-contained runnable jar: bundles the xz dependency so users
             need only `java -jar` (#8) -->
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.2</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <!-- JLS is not consumed as a library; skip the reduced POM -->
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>module-info.class</exclude>
                    <exclude>META-INF/versions/*/module-info.class</exclude>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.6</version>
        <configuration>
          <!-- tests exercise load/save and element construction without a
               display; @{argLine} lets the JaCoCo agent attach (#66) -->
          <argLine>@{argLine} -Djava.awt.headless=true</argLine>
        </configuration>
      </plugin>
      <plugin>
        <!-- coverage report on every test run: target/site/jacoco
             (issue #66; the CI ratchet is tracked separately) -->
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.15</version>
        <executions>
          <execution>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
          <!-- coverage ratchet: the floor only ever moves UP. It is set
               slightly below measured coverage to absorb JDK jitter;
               a PR that raises coverage should also raise the floor to
               just below the new measurement (issue #66).
               Baseline 2026-07-08: INSTRUCTION 18.04%, LINE 18.44%. -->
          <execution>
            <id>coverage-ratchet</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>INSTRUCTION</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.175</minimum>
                    </limit>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.180</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <!-- CycloneDX SBOM with every build: target/bom.json/.xml
             (issue #65; publishing with releases rides #44) -->
        <groupId>org.cyclonedx</groupId>
        <artifactId>cyclonedx-maven-plugin</artifactId>
        <version>2.9.2</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>makeBom</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <!-- fail fast on toolchain drift and dependency conflicts
             (issue #68) -->
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.6.3</version>
        <executions>
          <execution>
            <id>enforce</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>[3.6.3,)</version>
                </requireMavenVersion>
                <requireJavaVersion>
                  <version>[25,)</version>
                </requireJavaVersion>
                <dependencyConvergence/>
                <banDuplicatePomDependencyVersions/>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>4.10.3.0</version>
        <configuration>
          <effort>Max</effort>
          <!-- fail the build only on the highest-confidence findings;
               the exclude baseline therefore covers HIGH-confidence
               findings only - medium/low-confidence findings neither
               fail the build nor appear in the baseline (issue #54) -->
          <threshold>High</threshold>
          <excludeFilterFile>config/spotbugs-exclude.xml</excludeFilterFile>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
