**Capstone:** CAP-27 (#511) — ten-minute on-ramp to a running, understood example circuit
**Verdict: ready-with-gaps** — start now (the demo slice #545 via #760/#762, and #550 via #770/#771, are startable today); the gaps below are specific edge/text corrections and two unowned acceptance criteria, none of which requires re-decomposition.

Tree walked: #511 → FEATs #545, #548, #550, #551, #552, #553 → TASKs #760, #762, #764, #766, #768, #770, #771, #773, #775, #777, #779, #780, #782, #784 (all open). Cross-capstone consumers #579/#580/#855, pointer #73, and referenced #381, #84, #62, #572/#573/#574, #866, #510, #513 checked. Code claims re-verified against master.

### 1. Decomposition

**Filed and open:** all six planned features and all fourteen tasks exist, are open, and carry non-overlapping ownership boundaries that are explicitly stated on both sides of each seam (corpus #766 / metadata #768 / surfaces #764; generation #773 / publication #775; content #777/#779 / renderer #780; pages split #782/#784). The two-level native sub-issue tree matches the machine blocks.

**Gap D1 — PF-0 / AC-6 has no filed owner.** The machine block itself admits it: `planned_features: [PF-0 unfiled (recommended), ...]`. CAP AC-6 (documented `docker run ghcr.io/anadon/jls -b -t tests …` completing with a correct exit code on a Docker-only machine) is the grading-instructor on-ramp — the one persona #510 rates JLS category-best for. #775 AC-1 prints the per-entry container command in the gallery, which covers part of it, but nothing owns the AC-6 verification or its ten-minute-path documentation. The substrate is real (release.yml builds, pushes, and cosign-signs `ghcr.io/anadon/jls`), so this is a fraction-of-a-milliweek task — file it before or during execution, or CAP-27 closes with AC-6 unowned.

**Gap D2 — the AC-2(a) fresh-user trial has contested, unresolved ownership.** #552 AC-2(a) says lesson 1 "is the task used in CAP-27 AC-2(a)'s n=5 trial"; #780 explicitly disclaims running it ("owned there, not re-run by this task"); and #381's live body says the trial's home "here vs. #511 remains genuinely undecided as of this writing" and carries it there pending decision. No task in this tree executes the trial. It does not block start, but it blocks capstone close — decide the home (recommend: a small capstone-level task under #511) before the endgame.

**Residue D3 — #73's ownership table is stale in two load-bearing cells** (a defect #550's own body already flags as needing a separate report): it still says bundled samples' "base mechanism via #381" and "#766 (ten circuits incl. RV32I showcase)". #381's current body disclaims building `resources/samples/`, #766 declares itself sole creator, and #766 AC-1 says "The RV32I CPU is not a row in this index." #764 hedges on exactly this ambiguity ("that baseline is claimed by more than one issue right now"). Correct #73's table so no executor routed through the pointer builds the corpus twice.

**Minor D4:** #762 (structured comparison source → README generated block) and #866 (claims-ledger identity paragraph → README generated block) are two separate structured-source-to-README render mechanisms landing in the same shop-window area. Content is distinct; coordinate so they share one render/drift-check idiom rather than two.

### 2. Acceptance-criteria composition

CAP AC-1 → #545 AC-1/#760 AC-1–AC-3 (composes; children are stricter — content-drift check, not just path presence). CAP AC-4 → #550 AC-1/#770 (composes; children add the close-last-tab path). CAP AC-5 → #552 AC-2/#780 AC-3 (composes). KC-27-1/KC-27-2 are carried faithfully by #771 and #777/#780.

**Gap A1 — RV32I: every child can pass while CAP AC-3 literally fails.** CAP AC-3 requires the ≥10-circuit corpus to include the "RV32I showcase". #548's RV32I disposition and #766 deliberately exclude it from the openable corpus, with sound, code-verified reasoning: the generated full CPU is unusable without #62's layout entry point, and `riscv/gui/cpu.jls` (verified in tree) is a one-instruction `addi` demo. The children's position is correct; the capstone text is what needs amending — reword AC-3 to "datapath category + RV32I as regenerated credibility material in README/gallery (#545/#551)". Two stale echoes propagate the old requirement and should be cleaned with it: #551 AC-2 ("the RV32I showcase required by #548 AC-2") and #775 AC-1 ("the RV32I-class showcase #548 AC-2 requires") — #548 AC-2 requires no such thing.

**Gap A2 — welcome-pane ghost: #764 AC-4 is unsatisfiable as written.** #511 PF-3, #550's decision record, #771 AC-1, #770, #780 AC-2, and #552 AC-5 all record: no welcome/first-run pane, ever. But #548's Outcome and AC-1 still name "PF-3's welcome/first-run pane (primary)" as the primary discovery surface, and #764's Outcome and AC-4 require a test asserting the welcome pane's "open example" actions resolve from the shared `OpenExampleAction` registry — a test against a surface the capstone forbids building. Re-cut #548 AC-1 and #764 AC-4 to the surfaces that will exist (File → Open Example…, Help pointer, #780's lesson pointer, and the starter-circuit launch) before #764 is picked up.

**Gap A3 —** CAP AC-6 and AC-2(a) have no child executor (D1/D2 above). Conversely #551 and #553 map to no capstone AC — benign overcoverage serving the outcome, not a defect.

### 3. Dependency chains

**Acyclic — verified.** Task graph: #766 → #768 → {#764, #773} → #775; #777 → #779 → #780 (also after #764); #760 → #762; #770 → #771; #782 → #784. No cycle, no edge to a closed issue anywhere in the tree. (Minor: `ordering_after` mixes numeric ids and `TASK-C5xx-N` symbolic ids; the orchestrator must normalize.)

**Defect C1 (the one that matters) — stale `[381]` edges chain the whole corpus behind #91.** #766 and #764 both carry `ordering_after: [381]` while their own bodies say the opposite (#766: "there is no baseline to wait on or extend"; #381 disclaims the samples clause; #550/#770 confirm #381 is not a prerequisite). #381 is itself `blocked_by: [91]` (display-suite flake detector). Honored literally, these edges put #91 — unrelated GUI-test infrastructure — on the critical path of #766, and therefore of #768, #764, #773, #775, #777, #779, #780, and #782/#784's example links: nearly the entire capstone. Delete `381` from #766's and #764's `ordering_after` before orchestration. Also fix #773's ordering comment, which misstates #768's chain as "#768 → #766 → #764 → #381" (#768 does not depend on #764).

**Defect C2 — CAP `ordering_after: [84]` looks stale.** Its rationale ("PF-3's starter-circuit hook touches SimpleEditor's 9-state mouse machine") does not match the filed PF-3 work: #770/#771 are confined to `JLSStart` (launch `open(...)`, tab invariant, `newCircuit()` prompt) and neither #550 nor its tasks carries the edge. Re-derive; as written it serializes the cheapest capstone behind an open refactor for no stated task-level reason.

**Defect C3 — #553 `ordering_after: [573]` contradicts its own scope.** The body says the CircuitVerse page ships as a stub until CAP-32's demo exists, and #784 (the task that authors that page) orders only after #782 and #548 — yet the feature-level edge makes CAP-27's last feature wait on #573, which is itself gated by #572's go/no-go in a different capstone. Relax to `ordering_after_by_criterion` on the demo-link content or drop the edge; otherwise CAP-27's closure acquires an undeclared external prerequisite.

**Cross-capstone consumers:** #580/#855 carry the mirror edge correctly (`ordering_after_by_criterion: {AC-4/AC-5: [511]}`). CAP's `blocks: [579]` is stale — #579 has been restructured into an independent Flathub go/no-go decision record (`serves_capstones: []`) and no longer consumes the screenshot set; prune the edge (harmless meanwhile). #73's `blocked_by: [545, 548, 550, 551, 552]` mirrors are present on the features.

### 4. Staleness, evidence, cost bands

**Evidence: reproduces.** Verified directly on master: README is 359 lines (body says 368 — trivial drift), carries exactly one badge (OpenSSF Scorecard) and zero images; `resources/samples/` does not exist; no `welcome`/`firstRun` symbol under `src/jls/`; `examples/` holds only `autograde/`; `JLSStart` anchors all hold (`stateChanged` `ed == null` branch, `newCircuit()` naming prompt at 2186–2209, headless one-shot modes, `-b`/`-t` batch path); `CircuitRenderer.java:323` white-fill (note: file is `src/jls/edit/CircuitRenderer.java`, not `util/`); `scripts/wayland-rig.sh`, `riscv/jlsbuild.py`, `riscv/gui/cpu.jls`, `riscv/examples/` present; all six cited `docs/*.md` present; `src/jls/collab/op/` with `CircuitOp`/`CircuitOpReader` exists, so #777's op-block substrate is real; `release.yml` builds, pushes, and cosign-signs `ghcr.io/anadon/jls`, so PF-0's "the image already ships" claim holds.

**Gap S1 — lesson step-format conflict, reconcile before #777 starts.** #552's Outcome specifies steps as "plain-text `FORMAT 1` saves (the existing `-savetext` path)"; #777 AC-1 specifies each step as a single `jls.collab.op` op block, "not a full circuit save"; #779 states both in one paragraph. These are materially different artifacts (snapshots vs gesture deltas) with different renderer and replay-test implications. #777's op-block design reads as the deliberate later revision; update #552's (and #779's) wording to match, or #777's normative `docs/lesson-format.md` contradicts its parent on day one.

**Gap S2 — cost bands don't sum.** Feature bands total 7–13 mw, not the capstone's claimed 9–15 ("sums against PF-1..PF-6's six ranges" — it doesn't). Worse in the other direction at task level: #552 is banded 1–2 mw ("refactor, not from-scratch authorship") but its three tasks sum 3–4.5 mw — the op-block format, normative spec, replay-equality tests, and docs-page renderer are more than chopping Poplawski's prose, and the feature band was never updated. #548 (2–3) vs tasks 2.5–4 is a milder overrun. Expect PF-5 at roughly double its feature band; the capstone's "actual cost should land nearer the low end" note is contradicted by its own decomposition.

### Verdict: ready-with-gaps

Start now. The demo slice (#760 → #762) and the launch-path pair (#770 → #771) have no unresolved prerequisites and their evidence is fresh. Before orchestrating the corpus chain, make five cheap corrections: (1) drop `381` from #766/#764 `ordering_after` (C1); (2) rewrite #764 AC-4 and #548 AC-1's welcome-pane references (A2); (3) reconcile #552/#777/#779 on the step format (S1); (4) amend CAP AC-3's RV32I wording plus the #551/#775 echoes (A1); (5) file the PF-0/AC-6 task and assign the AC-2(a) trial a home (D1/D2). Also re-derive the `[84]` and `[573]` edges (C2/C3) and correct #73's ownership table (D3). None of this is decomposition work — the feature/task set, once these text-level fixes land, composes to the capstone's promised outcome on real, acyclic dependencies.
