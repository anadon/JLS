**Scope reviewed:** #297 (CAP-04) body at evidence commit `c5cee1b` (= current `master`); required features #315, #336, #337, #341, #349, #365 and their filed TASKs (#372, #375, #373, #468, #382, #387, #400, #450, #394, #427, #430); deferred set #316, #318, #319, #322, #329; related #298, #307; transitive blockers #391, #879. Code anchors checked against the local `master` checkout at the evidence commit.

**Verdict: ready-with-gaps** — the build-plan arm (5 of 6 features) is startable today and the evidence hygiene is the best in the corpus, but the physical-truth arm (AC-2) rests on a dependency story that #341's own filed text contradicts, and the dual-run mechanism has no filed owner. Gaps are named below; none requires restructuring the capstone, each has a bounded fix via the issue's own §5 re-planning protocol.

---

### 1. Decomposition

**Sound, with one ownership hole and one unfiled task.**

- All six `requires_features` are **open** and carry filed, open TASK children: #315 (→ #372, #375), #336 (→ #468, #373), #337 (→ #382, #412), #341 (→ #387), #349 (→ #400, #450), #365 (→ #394, #427, #430). No required feature is closed or redirected. CAP-04 itself has no native sub-issues, which is correct here: all six features are shared (each `serves_capstones` lists ≥2 capstones), so none can be a native child.
- **Capstone-owned scope is unfiled.** The Cost section prices 4–8 mw of capstone-owned work (`-breadboard` CLI surface, the `examples/sap1.jls` fixture, the dual-run undetermined-node report) that no feature carries — yet `requires_tasks_exception: []` and CAP-04 has no children. §2's claim "every required scope is filed and carried by number" is false for exactly this subtotal. Before implementation is orchestrated, these three items need filed TASKs (or an explicit `requires_tasks_exception` entry), otherwise the work that distinguishes this capstone from #298 has no tracker.
- **TASK-0093 is unfiled and ambiguously owned.** #365's `planned_tasks` carries it (counted at 2 wk in #365's own cost reconciliation, and #365's DoD requires it "filed and landed, or descoped"), while #341's body says TASK-0093 "is unfiled and **belongs to #329**" — the canvas feature this capstone explicitly deferred. Since CAP-04's completion criteria require #365 *closed as landed*, deferred-canvas-adjacent scope rides back into the required set through #365's DoD unless #365 REPLANs it out. One of #365/#329 must own it, and CAP-04 needs #365's answer before #365 closes.
- Minor: #315's native sub-issues include #488 and #492, which its machine block carries only in `related`, not `requires_tasks` — the native hierarchy and the machine block disagree about whether they are children. #337's machine block still lists TASK-0038 under `planned_tasks` although it is filed as #412 and is a native sub-issue — stale, `requires_tasks: [382]` should be `[382, 412]` or #412 descoped.

### 2. Acceptance-criteria composition

**AC-1, AC-3, AC-4 compose; AC-2's ownership is broken in both directions.**

- **AC-2 is the every-child-passes-while-the-capstone-fails hole.** CAP-04 says `UndeterminedNodePhysicalTruthTest` is "owned by #341". #341's body contains no dual-run diff, no undetermined-node report, and no such test — its exclusive scope is #387 (strength pair, net-kind derivation, two pull elements) and it explicitly disclaims the resolution fold to #391/#322. Meanwhile CAP-04's own Cost table books the "dual-run undetermined-node report" as *capstone-owned* scope — contradicting AC-2's "owned by #341" in the same document. As filed, #341 can close green without any dual-run mechanism existing, and CAP-04's steps 4–5 fail. Fix: either file the dual-run report as a capstone-owned TASK (consistent with the Cost table) or mirror it into #341/#387's scope and DoD — not both, and currently it is neither.
- **AC-1's named test is not mirrored either**: `BreadboardPlanGoldenTest` ("#365 builds it") appears nowhere in #365's body. #365's tasks do substantively cover checks 1–6 (packing totality via #394, netlist equivalence via #336-derived IR, floating/fan-out/vacuous-family via #430 verbatim), so this is a naming gap, not a scope gap — but check 7 (determinism + additive-only diff) is only implied by #394's refdes-stability scope, worth an explicit mirror at #365.
- AC-3 maps cleanly onto #315's residual (#372/#375 titles state exactly this obligation). AC-4 (two independent human builders) is deliberately capstone-owned, non-automatable, and correctly separated from step 3's internal-consistency observes; note it carries an unpriced real-world cost (two builders, two sets of ordered parts) that appears in no cost row.

### 3. Dependency chains

**Acyclic and mirror-consistent as drawn — but the #341→#322 exclusion is internally contradicted.**

- Verified against each feature's own machine block: #315 `blocked_by: []`, #336 `[315]`, #337 `[]`, #341 `[322]`, #349 `[]`, #365 `[336, 349]` — exactly as CAP-04's YAML and mermaid claim; every drawn edge is declared by the target's `blocked_by`; no cycle; no edge into a closed issue (#307/CAP-13 is closed but appears nowhere in the graph).
- **The decisive gap:** CAP-04 asserts #341's driver/net-kind distinctions are deliverable "independently" of #322's four-state value core, and on that basis dropped #322 from the required set. #341's own filed text says the opposite, three times: *"Consumes: the four-state per-bit value #322 defines — strength is meaningless over a two-state value"*; its critical path is "TASK-0057 (#391, owned by #322) → TASK-0058 (#387)"; and #387 "must land [after #391] to build strength into the fold." So as filed, CAP-04's completion criterion "every entry in `requires_features` closed as landed" transitively requires **#322**, which is `blocked_by: [879]` (open) — i.e. the true critical path of the physical-truth arm is #879 → #322/#391 → #387 → #341 → CAP-04, and two of those nodes are outside the funded set. Either CAP-04's engineering claim is right and **#341 needs a REPLAN splitting a two-state-compatible driver/net-kind subset** (computable at `WireNet.propagate`'s existing HiZ path, as CAP-04 argues), or the claim is wrong and #322+#879 belong in this capstone's transitive cost. This must be settled on #341 before #341/#387 work starts; it does not block #315/#336/#337/#349/#365, which are the bulk of the path.
- Startable immediately with no blockers: #315, #337, #349 (all `blocked_by: []`). #336 after #315; #365 after #336+#349. No external unfunded prerequisite exists on the build-plan arm.
- CAP-04's `blocked_by`/`blocks`/`requires_capstones` empty: correct; #298 sharing is properly `related`-only (Open Question 5's reasoning holds — every §1 step is observable with #298 unfunded).

### 4. Staleness and gaps

- **Evidence hygiene of the CAP body itself is excellent.** Every line-exact anchor reproduces at `c5cee1b` = current remote master: `LogicElement.java:473` ("Initialize all inputs to 0.") and `:480` (`in.setValue(BitSetUtils.Create((long)0))`); `Put.java:16-17` (`sealed … permits Input, Output`); `CircuitOp.java:34-37` and `:51` (`apply(Circuit, Graphics)`); `Adder.java:261` (`propDelay = bits * defaultPropDelay`); `grep -rniE "footprint|refdes|pinout" src/` → 0; 35 `new ElementType(`; `examples/` contains only `autograde/autograde.py`; `timeout-minutes` → 0; board-package files at exactly the cited 98/199/125/159 lines. The body also correctly self-reports that `docs/plan/**`, `docs/machine-calibration.md` do not exist on master (confirmed).
- **Two required features pin a phantom evidence commit.** #337 and #349 both carry `evidence_commit: 2d0ca9d…`, which resolves on **no branch** in the clone — #322's own machine block already flags this ("does not resolve on any branch (#493)"). Their anchors need re-verification against a real commit before their tasks are picked up.
- **The #341 cost row is affirmatively withdrawn by its own issue.** CAP-04's table carries #341 at 6–9 mw; #341 states that band "no longer describes this issue's scope in either direction" (own exclusive scope ~2 wk via #387, plus unpriced strength-transparency additions) and refuses to assert a corrected number. CAP-04's Cost section generically flags all bands as provisional, but this row is not merely unverified — it is contradicted. #365's row (5–8 mw) is compatible with #365's own audited 5.5–7.5 wk reconciliation.
- **Stale prose:** CAP-04 claims "#349's and #365's own `serves_capstones` lists still name 297, 298 **and 307**" — both actually read `[297, 298]`; #307 (CAP-13) closed as a duplicate and was correctly removed from the features, leaving the CAP's sentence pointing at a closed capstone. Harmless direction (the mirrors are right, the prose is behind), fix on next edit.
- **`docs/realization-layer.md` does not exist** on master, yet §3, Related Issues, and a completion criterion cite it as *the* shared FEAT-040/#298 contract's home. Like the cost documents, it must land before #349/#365 interface work is designed, or the two capstones re-author the contract by convention — the exact failure §3 warns against.
- Unowned check: #365's Open Question 6 records `C3_UNPLACED_PART` (a packed part absent from the physical wiring description) as "currently unowned anywhere in the tracker" — directly relevant to §1 step 3's placement observe. Needs an owner before AC-1 is authored.
- Open Questions 1–5 all carry recommended defaults or recorded decisions; none blocks start.

### Verdict

**ready-with-gaps.** Start now on #315, #337, #349 (unblocked), then #336, then #365. Before any #341/#387 work: settle the #322 contradiction with a REPLAN on #341 (split the two-state-compatible subset or accept #879/#322 into the funded path). Before AC-1/AC-2 authoring: file the three capstone-owned deliverables (CLI surface, `sap1.jls` fixture, dual-run report) as TASKs, give the two named tests real owners on #365/#341, resolve TASK-0093's owner, and land `docs/realization-layer.md`. Fix the phantom `2d0ca9d` evidence commits on #337/#349 and the stale serves-capstones sentence on the next respective edits.
