# CAP-37 readiness review (#521)

**Verdict: ready-with-gaps** — start now with PF-1 (#802) and the unblocked #593 Layer-1/2 tasks (#806, #804); the gaps below are named and none blocks the entry point.

Tree reviewed: CAP #521 → FEATs #592–#596 (all open, all native children, matching `planned_features` PF-1..5 exactly; no shared cross-capstone features are claimed as children) → TASKs #802–#813 (all open). Load-bearing code claims were checked against the JLS checkout (local master 6744b56; remote head c5cee1b; the one commit-hash citation, 2d0ca9d in #809, resolves on the remote).

## 1. Decomposition

**Sound overall.** Every PF has exactly one open owning FEAT; every FEAT has open TASKs; ownership boundaries between #593/#594/#595/#596 and the out-of-capstone owners (#542, #289, #570, #76, #536) are stated reciprocally and consistently. The #441→#84 duplicate redirect is handled correctly everywhere it appears (#521, #592, #802, #803, #807 all cite #84 and warn off #441). No dependency edge points at a closed issue.

Named gaps:

- **G1 — FEAT-593 Layer 3 is unowned, and #805 mis-attributes it.** #593 AC-5 (raw AWT `MouseEvent`-to-input-alphabet wiring behind #84's `MouseMachine`) has no owning task: #804 is Layer 2 (headless policies, explicitly zero dependency on #84), #805 is Layer-2 manipulation, #806 is Layer 1. #805's disposition says Layer 3 is "owned by sibling task #804," which #804's own body contradicts. Since Layer 3 is correctly deferred behind #84 (open, blocked by #440/#382), this does not block start — but a task must be filed when #84 lands, and #805's mis-attribution should be corrected so nobody concludes the layer is covered.
- **G2 — `OpSink.submitAll` atomicity is a named prerequisite with no filed issue.** Verified at HEAD: `submitAll` is a naive `for` loop over `submit(op)` — a mid-batch rejection leaves prior ops applied with no undo snapshot. Both #593 (boundary notes) and #805 (outcome + status) declare fixing this a prerequisite for shipping duplicate/group-drag safely, and both state "no tracking issue exists yet; filing one is this feature's to do." That filing is still outstanding, and it sits on PF-2's critical path.
- **G3 — the KC-37-1 ratchet has no owner.** #521's sequencing step 2 is "KC-37-1 ratchet over the `markChanged()` inventory," but no task builds it: #802 changes nothing under `src/` and its ACs don't include it; #804 AC-4 adds an AWT-freedom rule for its new types only. (Inventory note: KC-37-1 says 17 sites; grep at HEAD finds ~14 call sites plus the definition — the body's own "re-measure at execution" hedge covers this.)
- **G4 — double-owned scope between #802 and #803.** #802 as rewritten absorbed the score column, owner column, and the 4-bit-counter transcript ratchet (its AC-3/AC-6, and its cost note says it was re-banded upward for exactly these additions), while #803's AC-1/AC-3 still own the funding-score column and the counter ratchet (`EditingCostRatchetTest`). #803's own disposition recommends folding into #802; that maintainer call is unexecuted, so today two open tasks own the same two deliverables. Also, FEAT-592's band (1–1.5 mw) is now less than its children's sum (1.5–2.5 mw).
- **G5 — #811 is a placeholder for a row both its parent and it disclaim.** #596's rewrite and #811's own disposition assign the wire-net-identity row to #542 (open, CAP-26); #811 stays filed under #596 as blocked-on-#542 placeholder pending a maintainer re-homing decision. Acknowledged in both bodies; not blocking, but the re-homing decision is open.
- Minor: the shared "interaction-script golden vehicle" that #593's boundary notes recommend filing once at #521 level appears nowhere in #521's body.

## 2. Acceptance-criteria composition

CAP AC-1 (generated, cited, ranked catalog), AC-2 (every closed row pinned), and AC-3 (bsiever `#18`/`#4` dispositioned by name, JLS-side) compose cleanly downward: #592 AC-1..4/#802, #593 AC-8, #595 AC-8/#810 AC-5 cover them with build-enforced totality. Two upward gaps where every child could pass while the capstone fails:

- **G6 — CAP AC-4's ratchet covers one of four corpus tasks.** CAP AC-4 requires the gestures-per-op ratchet to hold over all four canonical tasks (4-bit counter, 8-bit bus wiring, eight-pin rename, duplicate-and-rewire-a-slice). Only the 4-bit counter is owned anywhere (#802 AC-6, #803 AC-3). No child AC records or ratchets baselines for the other three tasks. Either #802/#803 must be extended to the full corpus, or CAP AC-4 narrowed.
- **G7 — CAP AC-5's regression half has no instrument and no owner.** "Startup time and per-edit interaction cost do not regress from their pre-capstone baselines" requires a baseline instrument that #813 AC-8 and #596 AC-5 have each verified does not exist in the tree (the four existing ratchets are architecture/notification/socket/dialog-coverage), and both deliberately refuse to assert against it. No child builds it. The "no new default-visible chrome" half is well covered (#594 AC-5, #807 AC-4, #808 AC-1, #813 AC-8). Fund the instrument (naturally under PF-1) or soften CAP AC-5.
- **G8 — FEAT-594 AC-2 (MRU ranking) is unowned after #808's re-cut.** #808 deliberately replaced the recently-used list with a single repeat-last-placement ("not a recently-used list"), and #807 excludes recency. No task delivers "a most-recently-used ordering over the item vocabulary." Revise #594 AC-2 to match #808's re-cut, or file the slice.
- **G9 — #593 AC-8 contradicts CAP AC-4.** #593 AC-8 still mandates a human timed re-measurement ("at least 3 trials… operator-blind… after-median not slower by more than 10%") — the exact stopwatch protocol CAP AC-4, #802 AC-6 and #803 AC-3 now explicitly reject, and which #806 states "both now explicitly reject… as insufficient." Stale residue of the pre-rewrite protocol; #593 AC-8's second sentence should be replaced by a reference to the CI ratchet.
- **G10 — CAP PF-5 prose diverged from #596's rewrite.** #521's PF-5 promises a semantic token layer discharging wire coloring, monospace, the #76 chrome audit and dark mode "in one seam" (the 179 `Color.` call-site figure is verified exact at HEAD). #596 as rewritten explicitly does not build that layer: it dissolved into a residual score-ordered bucket and redirected the token-layer work to #542/#536/#76/#289. The capstone's outcome and ACs remain satisfiable (the catalog's allowed owner set {593,594,595,596,542,289,570} carries those rows), but #521's PF-5 section and its 2–4 mw line describe a deliverable no child will build.

## 3. Dependency chains

**Real and acyclic.** Full task graph: #802→#803; #806→#804→#805 (also #803→#805); #803/#482/#277→#807→#808; #802→#809→#810 (also #803→#810); #542→#811→#812→#813 (also #592, #536 blocks on #812; #592 on #813). Every edge points at an open issue; no cycles; the chain heads (#802, #806, #804) have empty `ordering_after` and are startable today, which matches the capstone's "PF-1 now, not blocked on #84" funding claim.

External prerequisites, all open and owned elsewhere (none unfunded, none blocking the entry point): #84 (blocked by #440/#382) gates only the unowned Layer 3 (G1); #542 (CAP-26) hard-blocks #811; #536 (CAP-24) blocks #812's redirected portability gap; #482/#277 sit ahead of #807. One inconsistency: #812's yaml `blocked_by: [592, 536]` vs its body, which says AC-1..3 are workable without #536 (only the redirected font-portability fix belongs there) — the 536 edge over-blocks the narrowed scope.

## 4. Staleness and gaps

Ground-truth claims were spot-verified and are in unusually good shape: `SimpleEditor.java` 5,852 lines (exact); 179 `Color.` sites in `src/jls/edit` (exact); `OpRejected` a bare `Exception(String)` (verified); `WireNet.setBits`/`recheck` `Math.max` folds at the cited lines (verified); `Util.isValidName` 8 files/12 call sites (exact); `submitAll` non-atomic (verified); 34 files in `test/jls/ui/`, 21 in `src/jls/collab/op/` (exact); `ExtensionPointCatalogTest` idiom, `OptionMenuPolicy.ATTACH_PROBE_LABEL`, `LoadError` category/location/hint shape, the ~126-site FlatLaf note in `ARCHITECTURE.md`, `resources/help/Map.jhm` + `HelpTopicsTest`, `examples/` containing only `autograde`, and the canConnect/`overlapMessage` sites incl. the four "Bits don't match" literals — all confirmed. `SimpleEditor.setup(Element, boolean)` at :5358 (exact). Trivial drift only: `SpatialIndex` `HashSet` at :191 vs cited :189; `Wire.getRect` is now `getIndexBounds` (same lines, same behavior); #803's "policy" rung is not actually named in `test/jls/ui/package-info.java`'s three-layer ladder.

- **G11 — the "#381 first-run gate" citation in #592 AC-3 / #802 AC-3 is stale.** Both describe #381 as the gate for "empty launch tab, no example circuit beyond `examples/autograde`, no README screenshots." #381's current body explicitly disclaims all of that — the welcome/starter-circuit, example-corpus and README items were redirected to the CAP-27 cluster (#550, #548, #545; see also #73's superseded pointer) and #381 now owns only composed-scale rendering. The unfundable-below-the-gate flag is a good idea pointing at the wrong number; it should cite #550/#548/#545 (or #511).
- **Cost bands:** #521's PF-4 line (2–3 mw) is contradicted by #595's re-derived 3–5 mw (slices 1.5–2.5 + 1.5–2.5), so the capstone's "PF-2..5 8.5–13 mw combined" is now 9.5–15 by child sums; plus the FEAT-592 band-vs-children mismatch (G4). Stale arithmetic, not scope-breaking.
- **Upstream citations:** the four core external rows (Digital #1308/#1129, Logisim-Evolution #88/#1234) carry a re-verification note dated 2026-08-16 in #521 itself; #802 AC-1 mandates live re-verification with quoted excerpts at authoring time and a copy of #510's branch-only evidence tree under `docs/` if unmerged. The corpus's own discipline covers the remaining re-check risk (CircuitVerse #1412/#349/#5328/#34 were not independently re-verified by this review).
- **Open questions blocking start:** none. #802 is startable today with `ordering_after: []`, and its two design decisions (annotation shape, ratchet idiom) are already made in-body.

## Verdict

**ready-with-gaps.** Start PF-1 (#802) and #593's Layer 1/2 (#806, then #804) now. Before or alongside the first fix wave, the maintainer should: resolve the #802/#803 overlap (G4, ideally the fold-in #803 itself recommends); extend the corpus ratchet to all four tasks or narrow CAP AC-4 (G6); decide CAP AC-5's regression instrument (G7); file the `submitAll`-atomicity issue (G2) and the eventual Layer-3 task under #84 (G1); fix #593 AC-8's stopwatch residue (G9); repoint the #381 first-run citation to the CAP-27 cluster (G11); revise #594 AC-2 to match #808 (G8); and refresh #521's PF-5 prose and bands to the shape its children actually compose to (G10, cost lines). None of these requires re-decomposition; all are issue-text edits or single new filings.
