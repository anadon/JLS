**Capstone:** CAP-15 (#310) — external HDL toolchains become required behavioral witnesses via a conformance harness; hierarchical export
**Verdict: ready-with-gaps** — five of six required features are cleanly startable now; the sixth (#359, the oracle itself, last in the dependency order anyway) must not be scoped until one contract divergence between it and this capstone body is reconciled by REPLAN. Gaps named below.

Tree walked: CAP body; `requires_features` [358, 339, 336, 315, 327, 359] (all shared — the capstone has **zero native sub-issues**, consistent with every required feature serving multiple capstones); each feature's native TASKs (#292; —; #373/#468; #492/#375/#372/#488; #478/#398; #386); out-of-set chain #317 ← {#353, #354, #363}; related #291, #873, #492, #59, #493, #320/#342/#360/#322/#321/#340/#328. Code claims verified against the local master checkout (6744b56) and evidence commit 828822672 confirmed an ancestor of remote `master` (head c5cee1b, 6 ahead).

## 1. Decomposition

**Sound overall.** All six required features are open, correctly titled (rule E verified: number↔title matches the roster comments), and all six list 310 in `serves_capstones` — the bidirectional bookkeeping holds. No required row is closed or redirected. §2's sufficiency table maps each §1 observation to a named owner, and the demotions (FEAT-026 four-state core; the netlist-interchange half) are argued, not asserted.

Findings:

- **G1 — #492 is bound to AC-3 by prose only, nowhere by machine block.** CAP-15 §1 step 3 and AC-3 require the fourth (`REJECTED`, with reasons) bucket, and name #492 as the funding task "under FEAT-001 (#315)". #492 is indeed a native sub-issue of #315 — but #315's `requires_tasks` is `[372, 375]` only, and CAP-15's `requires_tasks_exception` is `[]`. #492's sole ordering edge is `blocks: [292]`, and that edge is **unmirrored** — #292's own `blocked_by` is `[336]` only. So the entire required set can close, per every machine block, with #492 still open and §1 step 3 / AC-3 unmet. Fix is one line: add 492 to CAP-15's `requires_tasks_exception`, or to #315's `requires_tasks`.
- **G2 — double-owned hierarchical-goldens scope under a dead label.** #359's `planned_tasks` row 2 claims "TASK-0044, shared with FEAT-018/#358. Not filed" — but #358's machine block says TASK-0044 (#384) *was* filed and closed as a duplicate of #292, #358's sole child. The hierarchical emitters + goldens work is owned by #292; #359's row 2 restates it under a label the tracker retired. Not a blocker (the work has one real filed owner), but an orchestrator reading #359 would try to file or execute a task that #358 says is #292.
- **Resolved loose end, stale in the CAP body:** the Background section records #291 (Memory export remainder) as having "no filed feature" owner after #59's closure. That is no longer true — #291 is now natively parented under open feature #873, whose scope is exactly the Memory/RegisterFile/FieldExtend export disposition. Good news; the body should stop reporting it as unresolved.
- Unfiled planned work inside #359 ("the registry's task id space is closed at TASK-0112" so criterion 2 "has no task id — Not filed") is tolerable under the maintainer's D13 anti-ceremony ruling, but note it means the capstone's single most important deliverable exists only as feature-body prose.

## 2. Acceptance-criteria composition

**One substantive failure, one soft gap.**

- **G3 (the decisive finding) — CAP-15's AC-1 and #359's own design describe two different oracles, and the capstone's version has no owner.** CAP-15 §1 step 2 / AC-1 specify: compare **all backends' VCD dumps** at settling points with a **three-verdict comparator** (AGREE / DIVERGE / UNCONSTRAINED), record an **UNCONSTRAINED-rate percentage per run**, with falsification case (b) explicitly requiring an X/Z-class divergence be "verified as a real DIVERGE, not silently absorbed into UNCONSTRAINED". Built by "FEAT-023's residual (#359)". But #359's current (2026-08-08 rewrite) disposition **inverts the design**: Layer 1 diffs JLS's own `BatchSimulator` against a *committed trace* (gating, offline); Layer 2 diffs the external simulator's VCD against *the same committed trace* ("never to each other"), **non-gating, skip-when-absent**. #359's body contains no three-verdict logic, no UNCONSTRAINED verdict, and no rate reporting (grep confirms: zero occurrences). Consequences:
  - Every child can close per its own criteria while §1 step 2 and AC-1 fail as written.
  - The UNCONSTRAINED-rate number is load-bearing *twice over* in CAP-15 — it is the stated empirical gate for the demoted FEAT-026 (#322) four-state decision (§2, Open Question 5). Under #359's design that number is never produced, so the demotion's promised measurement has no producer.
  - CAP-15 risk 5 says making the external legs "required rather than skippable... is inside FEAT-023's residual scope"; #359's disposition explicitly demotes arming (TASK-0051/#386) to "infrastructure hygiene, not this feature's correctness guarantee", and #359's `requires_tasks` is `[]` — not even #386 is required. The capstone's *title* ("**required** behavioral witnesses") is not delivered by any machine-block-required work item; the arming owners (#386, #317's lanes) sit outside the required set.
  - CAP-15's own §5 REPLAN trigger ("a feature's contract deviates → reassess §3 risk 1 and re-run AC-1/AC-2") covers exactly this case. The reconciliation — either CAP-15 adopts #359's committed-trace design and rewrites step 2/AC-1/the FEAT-026 gate, or #359 re-adopts the three-verdict multi-backend comparator — must land before #359 is scoped. It does not gate the other five features.
- **G4 (soft) — AC-4 + non-gating Layer 2 admit a zero-external-execution close in CI.** AC-4 only *documents* where each backend's suite executed; with Layer 2 non-gating and no arming required anywhere in the closure, a close in which the external legs ran only in the §1 walk-through transcript (one machine, once) satisfies every checkbox. The Definition-of-Done's §1-walk-through requirement prevents a fully vacuous close, but "an emitter change fails a build" as a *standing* CI property has no required owner.
- Otherwise the ACs compose well: AC-2 (decomposed design as corpus row) correctly identified as jointly owned with the capstone as integrator; AC-3's mutation check (ii) closes the "mechanism fires but bucketing is wrong" hole; AC-1's three falsification transcripts are the right anti-vacuity device (§3 risk 3).

## 3. Dependency chains

**Real, acyclic, all endpoints open.** Verified edge set: #315→#336 (mirrored), #336→{#358, #327, #292} (mirrored), #358→#359, #292→#359, #317→#359 (all mirrored per #359's own re-verification), {#353, #354, #363}→#317. No path returns to CAP-15; `blocks: []` and `requires_capstones: []` both check out (no feature declares `blocked_by: [310]`; #328's blocked_by names features only). No edge targets a closed issue (#359 correctly dropped its edge to closed #360).

Findings:

- **G5 — the CAP body misquotes three feature-level edges, all in the direction of a stale graph.** (a) The mermaid asserts `F001 --> F018` and §3 risks 1–2 twice quote "`#358 blocked_by [315, 336]`" — but #358 declares `blocked_by: [336]` only, and #315's machine block explicitly records withdrawing the #358 mirror on 2026-08-08. (b) §3 risk 1 quotes "`#359 blocked_by [317, 358]` only" — actual is `[292, 317, 358]`. (c) §3 risk 8 says "#317 is itself `blocked_by [353, 354]`" — actual is `[353, 354, 363]`; **#363** (FEAT-035, checkpoint/resume) is a third out-of-set prerequisite the capstone never names, even in `related`. (#359's own body carries the same stale "#358 declares blocked_by [315, 336]" quote — the withdrawal post-dates both rewrites.) The risk-1 conclusion these misquotes support ("FEAT-023 is not ordered behind FEAT-004") still holds, but the graph a reader reconstructs from this body is wrong in three places.
- **Critical-path funding, restated honestly:** the required oracle #359 sits behind #292+#358 (in-set) *and* #317←{#353, #354, #363} — four out-of-set features, uncosted by this capstone's 28–45 mw band. Risk 8 discloses the #317 chain (minus #363) and the transitive-carry convention is defensible; but anyone scheduling from the 28–45 number should know the true tail to AC-1 is longer. All internal and filed — this is a cost/schedule gap, not an external blocker.

## 4. Staleness and evidence

**Code evidence: verifies.** At master: `HdlExporter.java` three policy buckets at the cited lines (EXPORTED/SKIPPED/TOPOLOGY; no fourth, no reasons); `HdlModel.Direction` two members at :28–33; `moduleName` at :891; `Adder.java:261` `propDelay = bits * defaultPropDelay;`; `grep -c "new ElementType(" ElementRegistry.java` = 35; `InstanceStatement` in src = 0; `ProcessBuilder` src=0 files / test=15 files; all cited test files, scanners, `HeuristicLayeredLayouter`, riscv differential scripts, and the three docs exist. Evidence commit 828822672 is on master. The body's self-correction (no `docs/plan/**` citations, no unattributable marginal-cost band) held up under checking.

Findings:

- **G6 — CAP-15's cost table prints a band its own child disavows.** The #359 row says "6-12 mw"; #359's Cost section says, in bold, "**Prior band: 6-12 mw, derived partly from `docs/plan/evidence/BRIEF.md`... do not reuse it**" and "this feature's cost should be re-derived against the trace-based design... before being used for scheduling" — and argues most of the comparator plumbing already ships, i.e. the true residual is likely *below* the band. The 28–45 mw required-set sum is therefore stale at its largest-variance row and overlaps G3 (the band prices a comparator design #359 no longer intends to build).
- **G7 — two required features still pin the doomed evidence commit.** #339 and #327 (and out-of-set #317) carry `evidence_commit: 2d0ca9d...` — the exact commit CAP-15's own machine block calls "on a working branch that will be deleted and was never an ancestor of master" (#493's subject). It still resolves on GitHub today, but the capstone's Definition of Done requires every cited permalink to resolve on the default branch at close; these two features are pre-committed to failing that unless repinned.
- **Minor:** §2's beneficial-features notes discuss #320, #342, and #360 as live deferred candidates; all three are closed as duplicates. Consistent with "file a new issue if pursued", but a reader chasing those numbers finds closed issues.
- **Open questions:** OQ2 (is the batch differential oracle the previously-refused co-simulation?) requires maintainer confirmation "before FEAT-023's residual is scoped" — it stacks with G3 as a pre-#359 gate, not a pre-start gate. OQ3 (settling points) is *answered differently* by #359 (settling read off the stimulus fixture's own event times) than by CAP-15 (a negotiated parity-contract term) — fold into the G3 REPLAN. OQ1/OQ4 have workable defaults and block nothing.

## Verdict

**ready-with-gaps.** Start now on the unambiguous five-sixths: #315 (+ #492 once bound, see G1) → #336 → {#358/#292, #327}, with #339 parallel from day one (`blocked_by: []`). Before #359 is scoped — and it is last in every chain anyway — one REPLAN must reconcile CAP-15 §1-step-2/AC-1/the FEAT-026 gate with #359's committed-trace two-layer design (G3), answer OQ2 with the maintainer, decide where "external legs are *required*, not skippable" actually lives (G4; today: nowhere required), and re-derive the #359 cost row (G6). Housekeeping in the same pass: bind #492 into the required closure (G1), fix the three misquoted edges and add #363 to the risk-8 disclosure (G5), retire the dead TASK-0044 label in #359 (G2), note #291's new owner #873, and repin #339/#327 evidence commits (G7). None of this requires filing new issues or restructuring the tree.
