# Issue #435: TASK-0109: two headless replicas exchange real envelopes over the real transport and save byte-identical files — with divergence reported as divergence, not as timeout
- Lens: adversarial
- Date: 2026-08-08
- Reviewer: Claude Sonnet 5
- Verdict: needs-rework

## Summary

The technical design (three-outcome classifier, seeded/replayable schedules, no-socket/no-sleep discipline, headless requirement) is well specified and its factual observations (O1-O7) check out against the actual repository. The problem is governance/currency: the issue's own machine-readable dependency block, its Definition of Done, and its blocking chain have gone stale or been left self-contradictory by three follow-up comments in the last four days, none of which edited the body. As filed, the issue is not actually startable — its own Open Question 1 says so — and an executor following the written Completion Criteria literally would report landing to two closed issues.

## Findings, most severe first

**1. The Definition of Done points at dead issues.** The body's own text: *"Landing reported on #348 (FEAT-051) with a `STATUS:` comment, and #352 (FEAT-052), #279 and #169 notified."* Both #348 and #352 are confirmed closed as duplicates (comment 5227088748: *"#348 (FEAT-051 RESIDUAL) — the declared `part_of_feature` — is closed as a duplicate... #352 (FEAT-052), named in `related` and §12 as the other consumer, is likewise closed as a duplicate"*). Comment 1 (5181208120) explicitly states *"No body or title of any issue was edited by this pass"* — so this checklist item is still live in the issue as filed. An executor closing this issue by the letter of its own Completion Criteria posts a STATUS comment to two dead issues and calls it done. **Recommendation:** edit the body's machine block and Completion Criteria to name #169 (or whatever the resolved parent turns out to be) before work starts; do not leave this to be caught by whoever executes.

**2. The dependency graph has flip-flopped three times in four days with no resolution.** Comment 1 (2026-08-04) re-homes `part_of_feature` to #169. Comment 2 (2026-08-08T16:50) reviews the same facts and recommends the opposite: *"Recommended: re-home to #171... Flagged for the maintainer rather than done."* Comment 3 (2026-08-08T18:15), less than 90 minutes later, "corrects" it back to #169 — *without acknowledging or rebutting comment 2's #171 argument at all.* A contributor who reads only comment 2 gets a different instruction than one who reads only comment 3. Nothing in the thread reconciles the two; the "correction" simply asserts a conclusion contrary to the prior comment's explicit recommendation. **Recommendation:** a maintainer decision comment that actually engages with why #171 was rejected in favor of #169, or the churn will happen a fourth time.

**3. The corrected blocker (#169) may be broader than what this task actually needs, and the issue's own evidence contradicts its stated reason for the block.** §12/Status block justify blocking on TASK-0108 (now resolved to #169) with: *"without it there is no envelope framing, no peer identity to stamp a `VectorClock` with, and nothing to attribute a rejected frame to."* But this issue's own §6 Materials and O1 cite `src/jls/collab/crdt/OpEnvelope.java` as already shipped — and it already is: `OpEnvelope(OpId id, VectorClock clock, CircuitOp op)` with `origin()` returning a `String` peer id (verified at `src/jls/collab/crdt/OpEnvelope.java`). `PeerId` also already exists (`src/jls/collab/session/PeerId.java`), landed as part of #169's "session-state core" per #169's own decomposition table. The parts of #169 that are actually unlanded — snapshot broadcast (#281), anti-entropy, token gating, presence overlays, peer panel, N-instance runner, three-machine pilot — are things §7.1 of this issue explicitly disclaims needing ("No `.jls` grammar, no CLI flag... nothing in `src/`"). Blocking the whole task on the whole of #169 rather than on the small already-landed identity slice risks stalling #435 for quarters on work it does not use. **Recommendation:** state precisely which piece of #169 this task needs (peer-id type + envelope framing, both already landed) rather than the feature as a whole.

**4. A "falsifiable" hypothesis that says it isn't.** §4 is headed "Hypothesis (falsifiable)" and lists H1-H5, but H3's own falsification entry reads: *"H3 refuted — not by measurement; it is a claim about diagnostic value... the refutation to watch for is a reviewer proposing to simplify the classifier to a boolean."* That is not a falsifiable hypothesis by the document's own definition — it is a design preference dressed in the paper's hypothesis-testing vocabulary. This isn't fatal on its own, but it signals that the elaborate H/P/falsification apparatus is partly decorative, which should lower confidence in treating every other "must hold" prediction as equally rigorous.

**5. The round bound R and trial count N are unresolved "blocks execution" items with no named owner.** Open Question 2: *"What is N, and what is the round bound R?... Blocks execution."* Open Question 1 (ChaosTransport visibility: public-within-test vs. moving the harness into `test/jls/collab/net/`) is also explicitly "Blocks execution," and the issue says the call *"must be made by a reviewer before the task starts, not inside it."* None of the three comments on this issue address either question — they are entirely occupied with the parent-feature churn above. As filed, there is no evidence anyone is positioned to make this call, and the issue names no specific person or process for it. **Recommendation:** resolve both blocking Open Questions in the issue body itself, or name who resolves them and when, before treating this as ready to pick up.

**6. The R bound, once set, is not verified to actually exercise the "unconverged" branch.** P9/P2 require N seeded schedules to converge within R rounds; H5's falsification is a schedule that "genuinely needs an unbounded number." But R is left to be picked ad hoc ("derived from the schedule length rather than guessed") with no Completion Criteria item requiring at least one seed that is deliberately close to or over the bound. An implementer could pick R generously large, and the entire suite could pass green while the "did not converge in the round bound" branch of the three-outcome classifier — one of the three explicit deliverables (§7.6) — ships effectively untested. Only P6 ("divergence reported as divergence... not a timeout") gets a deliberately-injected test (P6); nothing analogous is required for the "unconverged" outcome.

**7. #382 is a nontrivial, not-yet-landed precursor with its own open risk, understated here as a checkbox.** #435's Method step is "Confirm #382 has landed." #382 itself is a substantial task: change `CircuitOp.apply`'s signature across 12 files (not 5, per #382's own corrected O2) and migrate four still-inline editor gestures, which #382 itself calls "the rest" of the work after a one-day signature change, with its own open geometry-parity risk (P3's BufferedImage-vs-editor-font caveat) and an open question about whether any gesture legitimately stays inline. If #382 lands with a gesture deliberately left inline (its own Open Question 2 permits this with a written reason), #435's coverage is silently narrower than its predictions imply, and nothing in #435's own Method cross-checks that.

## Solid parts (one line each)

- O1-O7's file/line citations were spot-checked against the checked-out repo (`CircuitOp.java:51`, `Circuit.java:1466`/`1548`, `LoopbackTransport.java:104`, `ChaosTransport.java:23`) and all match exactly as quoted.
- The claimed absence of `ReplicaHarness`/`Schedule`/any harness (O2) reproduces: no such classes exist anywhere under `test/`.
- The no-socket/no-sleep/injected-tick-source discipline (§7.9, P8, P9) is concrete, testable, and consistent with the existing `SocketConfinementRatchetTest` pattern already in the codebase's culture.
- The scope fence (§7.1: nothing in `src/`, apparatus stays in the test tree) is unambiguous and easy to verify at review time.
- The three-outcome classifier's rationale (H3/§7.10) — that collapsing "converged different" into "unconverged" hides which team owns the defect — is a genuinely useful design point independent of the rigor complaint in Finding 4.

## Verdict rationale

Technical design: sound. Governance state of the issue as filed: not ready — a broken Definition of Done, an unresolved three-way dependency dispute in the comments, two explicit "blocks execution" Open Questions with no resolution or owner, and a blocking chain (#169) that may be wider than the task's actual needs. Recommend the maintainer resolve Findings 1-3 and 5 in the issue body before this is picked up for execution.
