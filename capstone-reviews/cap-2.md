**Verdict: ready-with-gaps** — the required set is fully filed, open, and acyclic, and the earliest rows can start today; but the CAP body's recorded dependency graph, two roster-row descriptions, one kill criterion, and the sibling-capstone cost framing are stale against the filed features, and the guest-image task's feature-level ownership is explicitly unsettled. Start now on the unblocked rows; the first `REPLAN:` on this issue should regenerate the graph and roster from the features' own machine blocks (the protocol this issue itself declares: "the feature's machine block wins for feature-to-feature edges").

Reviewed against: CAP body at evidence commit `333523a`; all 16 required features' live machine blocks; native sub-issue trees; out-of-set prerequisites; codebase at the local master checkout (`6744b56`, an ancestor of the default-branch tip `c5cee1b`) plus default-branch content via the API.

---

## 1. Decomposition

**All sixteen required rows exist, are open, and carry `serves_capstones: [301]`** (or, for #202, `serves_capstones: [301]` outright). The native hierarchy is well-formed: seven CAP-exclusive rows are native children of #301 (#202, #322, #324, #325, #343, #347, #364); the nine shared rows (#317, #319, #327, #335, #353, #354, #357, #362, #363) are correctly *not* native children and each names 301 in `serves_capstones`. Every filed TASK in the tree is open: #391 (under #322); #424/#454 (under #324); #389/#421 (under #325); #390/#423 (under #347); #457 (under #364); #278/#392/#425 (under #202); #444/#445/#395 (under #319); #377/#379/#407/#413/#442/#378 (under #335); #376/#405 (under #353); #426/#456 (under #363); #478/#398 (under #327). No required row is closed; no closed issue is load-bearing.

**Gaps, named:**

- **G1 — the guest software stack has no settled feature owner.** The CAP body's roster row for #343 still reads "jls.mach, reference runner, guest stack ... supplies the parity counterparty and the pinned kernel/DTB/initramfs." #343 as filed has been redirected: the counterparty is now an **external** RISC-V golden model (Spike or Sail, under RISCOF, over the #222 subprocess boundary) with `jls.mach` shrunk to an adapter seam, and #343's body **explicitly disowns the guest image**: "the guest-image task is named as scope this feature no longer requires — its re-homing under whatever feature ends up owning #301's boot claim is reported ... for a maintainer to action on #395 directly." #395 (TASK-0071) is currently natively parented under **#319 (FEAT-013, file format)** — a feature whose own contract #343 describes as "not consumed by this feature at all" — and #395's title demotes the Linux boot to "an informational rebuild recipe published as a digest-verified release asset." The scope is still inside the required set's closure (via #319's `requires_tasks`), so nothing is *missing*, but AC-5's key artifact is parked under a thematically unrelated owner with a pending re-homing decision. Settle #395's owner in the first REPLAN.
- **G2 — B-08's closure moved to a task that is not filed.** The CAP maps B-08 (dense-memory ceiling, verified live at `Memory.java:1224`, `DENSE_CAPACITY_LIMIT = 1 << 22`) to #364. #364 as filed has narrowed to the write-mask only ("no longer bundled with the byte budget or bulk-image work") and hands the capacity/paging work to **#354's TASK-0013, which is planned but not filed** (#354: `requires_tasks: []`, five planned tasks "not yet filed"). #354 is in the required set, so closure holds, but the B-08 → #364 mapping in §2's findings table is wrong, and the actual owner's task does not yet exist as an issue. (#364's narrowing also deliberately removed the mutual-deferral hazard between it and #354 — an improvement the CAP body doesn't yet reflect.)
- **G3 — #343, the "earliest-startable" 14–22 mw row, has zero filed tasks.** Its own decomposition table lists TASK-0070 (revised) as "not filed," and its native sub-issue list is empty. Startable, yes; orchestratable from issues, not yet.
- **G4 — minor:** #324, #325, #347, #364 each have native TASK children but `requires_tasks: []` in their machine blocks; #202/#319/#322/#327/#335/#353/#363 list them. If `requires_tasks` is meant to mirror native children, four blocks are inconsistent; if it means something narrower, the convention should be stated once.

No double ownership found. #278 appears both as #202's Direction-A task and in the CAP's prior-art section; the CAP correctly requires only #202's Direction-B slice (#392, #425), and no two features claim the same task.

## 2. Acceptance-criteria composition

**The primary proof (AC-1/AC-2) still composes.** #325 (TASKs #389 boundary sealing, #421 mid-run switch) + #347 (TASKs #390 RetireRecord, #423 named-divergence reporting) + #343's oracle feed cover AC-1 through AC-4; #202's #425 is precisely AC-4/AC-5's byte-stream-plus-clock-period machinery, and #392 is AC-6's drawn-boundary census. AC-3's falsification demand (assert the report *text*) is implemented by #423's title. The CAP's promotion of AC-1/AC-2 over the boot demo survives every scope change reviewed — indeed #343's redirect *strengthens* AC-2's independence (the counterparty is no longer same-author).

**Gaps:**

- **G5 — KC-02-5 no longer grades anything that exists.** It reads "`jls.mach` does not reach the `jls.sim` PACKAGE coverage floors (0.930/0.920/0.845, `pom.xml:458,463,468` — verified live) ... → cut the model to RV32I + Zicsr, M-mode only." Under #343 as filed, `jls.mach` is a thin adapter over an external oracle, not an architectural model — there is no in-tree model to hold to a model-sized coverage bar, and no model to "cut to RV32I + Zicsr." The kill criterion's *intent* (a bounded bet on the counterparty) is still needed, but its terms must be re-derived against #343's adapter + external-oracle shape. Not start-blocking; must be fixed before anyone treats KC-02-5 as live.
- **G6 — the sufficiency argument for #319 is now argued from false premises.** "§#319 removed: #363, #364 and #357 cannot start (each is filed `blocked_by: [319]`)" — all three are now filed `blocked_by: []`. #319 remains genuinely required, but via #445 (the length- and hash-checked sidecar image the guest RAM needs) and #444, not via the ordering edges the CAP cites. Rewrite the justification; do not drop the row.
- AC-5's excluded-artifact discipline, AC-7/KC-02-3's sole-guard status (#202 still does not file `blocked_by: [362]`; #362 files `blocks: []`), and the DoD's licensing checkbox all still compose — the licensing checkbox should now key on #395's "recipe + digest-verified release asset" design, which matches the CAP's own recommended default.

## 3. Dependency chains

**Recomputed from the filed machine blocks (not the CAP body), the graph is real and acyclic, and no `blocked_by` edge points at a closed issue.** Current in-set edges: #353→{#335,#354,#317}; #354→#317; #363→#317; #335→{#347,#362}; #325→{#347,#202}; #343→{#347,#202}; #347→#202; #324→#202; #364→#202; out-of-set: #336→#327 (open), #879→#322 (open), #337→#202 (open). Forward `blocks` arrays are symmetric with these. No cycles; no dangling closed endpoints. The stale `#324 → blocked_by: [330]` pointer the CAP flagged for repointing has been resolved — by **deletion**, not repointing: #324 is now `blocked_by: []`, and #212/#223 (which absorbed #330) declare `blocks: []`. This is coherent with #324's revised design (BytePort as an in-tree `LogicElement` — sealing verified live: `Element.java:17-18` permits 3, `LogicElement.java:18` permits 24 — needing no module dispatch), but it silently dissolves B-11's "out-of-set prerequisite of #324" row and the CAP's fourth beneficial-prerequisite table row.

**The CAP body's recorded graph is stale on at least six edges** and, per its own protocol, must be regenerated: #324 `blocked_by` is `[]` (was `[316, 330, 354]` — the mermaid's F006→F032 edge no longer exists in any filed block); #364 is `[]` (was `[319]`); #363 is `[]` (was `[319]`); #357 is `[]` (was `[318, 319, 340]` — and the direction has *inverted*: #319 now `blocks: [318, 332, 340, 356]`); #317 gained `blocked_by: [363]` (edge F035→F007, absent from the mermaid); #322 gained `blocked_by: [879]` (open TASK-C232-2, the #232 allocation-ratchet line — an ordering fact the CAP records as "blocked_by: []").

**Startability is better than the CAP claims:** #325, #343, #353, #319, #357, #363, #364 are all filed `blocked_by: []` (the CAP names only #343 and #325). The critical path to #202 runs #353→#335→#347-side and #325/#343→#347→#202 with #317/#324/#337/#364 feeding in; #202 remains the most-blocked row (7 edges, all open).

**External prerequisite, funded but flagged:** #343 now puts Spike-or-Sail + RISCOF on the critical path as a pinned CI subprocess. The provisioning is in-scope of #343 (digest-pinned, arm-in-CI / skip-cleanly-locally per the `IverilogCompileTest` pattern), so it is not *unfunded* — but it interacts with #317's "all three platforms block a merge": external-oracle availability on the Windows lane is unexamined anywhere in the tree.

**Sibling capstones:** #299 (CAP-01) closed 2026-08-03 as **duplicate of #163**; #295 (CAP-03) closed 2026-08-03 as **not planned**. No CAP-02 dependency edge touches either (correct per Open Question 5), but see §4 for the cost consequences, and note the roster-wide `serves_capstones` arrays still citing 295/299 are corpus staleness the board should sweep.

## 4. Staleness and gaps

- **Evidence commit `333523a` is not on the default branch.** It sits 15 commits ahead of the master tip `c5cee1b` on `claude/github-issue-review-agents-j99xga` (compare API: ahead 15 / behind 0). The DoD requires every citation to resolve on the default branch at close; the pin should move to a master commit once that branch merges. The findings themselves survive: **B-01** (no `src/jls/mach`), **B-02** (no Console/Uart/Clint), **B-03** (`Circuit.java:1466` `save(PrintWriter)`), **B-04** (`Simulator.java:224-234` poll-then-limit-check drop), **B-05** (`BatchSimulator.java:75-90` pause≡stop, `which` unread), **B-06** (`SigSim.java` `+=` loop — note the file is `src/jls/elem/SigSim.java`, not `sim/`), **B-07** (no `timeout-minutes` in `.github/`), **B-08** (`Memory.java:1224`), **B-09** (`FileAbstractor.java:65` 64 MiB cap — file is `src/jls/FileAbstractor.java`; `Circuit.java:102` `FORMAT_VERSION = 2`) — all re-verified at the current checkout. The two doc anchors the CAP calls "in the tree today" resolve on the default branch: `keystone-c-performance.md:655` (318 ns/event, 8,090 cycles/s) and `AMENDMENT.md:95` (130–190 mw).
- **The cost framing's sharing premise partially collapsed.** CAP-00 (#296, open) does fund #317/#335/#353/#354 (confirmed in its `requires_features`), so that half of the 42–67 mw spine holds. But #319/#357 were attributed to CAP-01, now closed as a duplicate of #163 — whether #163 funds them must be re-derived; and CAP-03, the "fourteen shared rows" co-funder that justified ordering CAP-02 first (Open Question 4), is closed not-planned, making OQ4 moot and leaving several rows (#322, #324, #325, #327, #343, #347, #364) with 301 as their only (or with #304/#306/#313) live serving capstone. The marginal band 117–186 mw needs re-derivation; the standalone band's #343 (14–22, priced for a full model + guest stack, now an adapter) and #364 (3–7, priced pre-narrowing) rows are both stale, in opposite directions.
- **The riscv/ narrative is internally contradicted.** The CAP: "`riscv/` ... is not scheduled for removal." #335's filed TASK #413: "the CPU-scale calibration anchor becomes a tracked, censused fixture **and `riscv/` is deleted** without taking the measurement basis with it" — while #343 plans to *retarget* `riscv/fuzz_diff.py` and keep `riscv_ref.py` as a second voice. Three issues, three dispositions; reconcile in the REPLAN.
- Calibration figures remain legacy-unverified pending #335 exactly as the CAP says (no `MeasurementGate`/`CalibrationFixture` in src/ or test/ — reconfirmed; `riscv/.gitignore` line 1 is `build/` — reconfirmed), and #362 is correctly sequenced `blocked_by: [335]`. B-10's ratification question is properly parked on #347 (OQ6). Open Question 8 (definition-level fidelity toggle) remains recorded and undecided on #325 — still the right place.

## Verdict and start order

**ready-with-gaps.** Nothing in the required set is missing, closed, cyclic, or externally unfunded; the primary proof chain (#325 → #347, fed by #343) is intact and its rows are unblocked. Start now: **#353** (spine head; blocks three in-set rows), **#325**, **#343** (file TASK-0070 first — G3), **#319** (unblocked and on the #445 sidecar path the guest RAM needs), with **#335** immediately behind #353. Before orchestration treats the CAP body as authoritative, post the owed `REPLAN:` covering, in one edit: the regenerated mermaid/roster from filed machine blocks (six stale edges, §3), the #343 and #364 roster-row rewrites (G1, G2), KC-02-5's re-derivation (G5), #319's corrected sufficiency argument (G6), #395's ownership decision (G1), the riscv/ disposition (§4), and the post-#295/#299 cost re-derivation. None of that work blocks the first line of code on the rows named above.
