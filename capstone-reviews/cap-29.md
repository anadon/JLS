# CAP-29 (#513) readiness review — 2026-08-16

**Verdict: not-ready** — decomposition/AC repair needed at the capstone level before orchestration. The child specifications are largely high quality and PF-1 is startable today, but the capstone body itself cannot currently serve as the orchestration source: two of its five acceptance criteria are unsatisfiable as written, its declared funding order contradicts the recorded dependency edges, and six open sub-tasks specify work the capstone explicitly de-funded. The repair is an editing/closure pass, not a re-design; concrete fix list at the end.

Tree walked: #513 → native children #556, #558, #559, #561, #562; their TASKs #608, #610, #612, #614, #615, #617, #619, #621, #622, #624, #626, #628, #629, #631, #633, #635, #637, #639; shared PF-6 #553 (+ its #782/#784); ordering/external refs #311, #314, #323, #357, #404, #451, #466, #510, #38. Codebase claims checked against the master checkout.

---

## 1. Decomposition

**All five PFs are filed and open** (#556, #558, #559, #561, #562 — all native children of #513), the shared PF-6 (#553) is correctly *not* a native child, and the #556/#608/#610 report-contract slice is crisply scoped with `blocked_by: []`. The core .dig pipeline (#612 → #614 → #615/#617 → #619) is a clean, ordered task chain. That is the good half.

- **[Major] Six open TASKs under the recipe FEATs specify de-funded importer work.** CAP-29 redirected PF-3 (#559) and PF-4 (#561) from importers to hand-executed recipes (0.5-1 mw each), but the task families were only annotated, not dispositioned:
  - Under #559: #621, #624, #626, #628 each carry an explicit "**Recommendation: close this task as superseded / not-planned**" in their own bodies, yet remain open native sub-issues. #628's title still promises "imports as a single **undoable operation**" — contradicting both the recipe scope and #513's own Architecture direction #2 (undoability retired capstone-wide). Only #622 was genuinely re-scoped to recipe content (0.25 mw).
  - Under #561 (whose title says "**no parser ships**"): #629 and #631 remain fully-specified parser/importer tasks (1 mw each, fixtures, `LoadError` wiring, `JumpStart`/`JumpEnd` driver election) with only a "provisional `part_of_feature`" footnote; #633 self-recommends closure as superseded by #561 AC-1/AC-4/AC-5.

  An orchestrator walking native sub-issues in good faith would fund roughly 5-6 mw of parser work the capstone's own text refuses, or halt on the contradiction. These seven tasks need an explicit close/re-parent decision before implementation is orchestrated from this tree.
- **[Major] #558 AC-7 (invocation surface) has no owning task** — #558's Decomposition section names this itself: "real, unfiled scope." The capstone's Architecture direction makes this load-bearing ("a gap this capstone's PF-2 **closes, not just notes**"; verified at master: `NetlistImporter` is 1,067 lines with zero call sites outside `src/jls/hdl/imp/` — the five-importers-zero-doors risk is real). A TASK for the CLI flag/menu wiring must be filed.
- **[Minor] PF-6 double-accounting.** #553's machine block says `serves_capstones: [511]` only and `band_mw: 1-2`; CAP-29 counts "PF-6 0.5-1" inside its own 9-14 band. No mirror link to #513 in #553's block, and the joint cost is counted ambiguously by both capstones. #553's AC-4 slot mechanism does align with PF-6's deliverable, so this is bookkeeping, not scope conflict.
- **[Noted, non-blocking] The fold-into-CAP-16 disposition is unresolved by design** ("status: unresolved — CAP-29 continues as its own capstone until #311 decides"). Acceptable as written; it does not block start, but it is a standing invitation for double-ownership of the report infrastructure if #311 decides late.

## 2. Acceptance-criteria composition

- **[Major] CAP AC-2 is unsatisfiable as written.** The capstone's Outcome, PF-5 description, and AC-2 all promise `.dig` tests arriving as "runnable `-t` files" whose "translated vectors … pass under `-t`". #562 and its tasks (#635: "no `-t` file emitted") redesigned PF-5 into a direct reader against #466's verdict channel — for a reason I verified at master: `docs/batch-interface.md` §2.2's `-t` grammar (`file / signal / step / initial`) has **no expected-value production at all**; a `-t`-only translation literally cannot carry the grading assertions the capstone exists to migrate. As the tree stands, every child could pass (verdict parity via `VerdictSet`, #639's generated oracle + mutation control — a *stronger* mechanism than the capstone asks for) while CAP AC-2's literal text fails, because no translated `-t` vectors will ever exist. #558 AC-1(b) also still references "the translated `-t` vectors (FEAT-C29-5 / #562's output)". CAP AC-2 and #558 AC-1(b) must be rewritten to verdict-parity-through-#466.
- **[Major] CAP AC-3 is unsatisfiable at present.** It requires "round-tripping FEAT-025's (#323) **existing** `.circ` report through the shared schema". #556 AC-3 states flatly: "There is no `.circ` report to round-trip yet" — correct (verified: no Logisim code and no XML parser anywhere in `src/`; #451 is open) — and substitutes retrofit proofs against the two shipped shapes (`HdlExporter` buckets, `NetlistImporter`'s `ImportSummary`/`ImportResult`, both confirmed in-tree). The child's substitution is sound; the capstone AC must be restated to match, else #556 can close while CAP AC-3 remains formally unmet until CAP-16 ships.
- **[Composes] AC-1's per-format split** (importer bar vs. recipe bar) maps cleanly onto #558 AC-1 (5 circuits / 2 sources / net-partition equality) and #559 AC-1 / #561 AC-1 (2 real published circuits each, net-structure checks). AC-4 (XXE) is owned by #612 with a stronger spec than the capstone asks (StAX-bounded, no reflective deserialization). AC-5 (license) is correctly owned capstone-wide, with #558/#562 explicitly refusing to treat it as satisfied by default and #561 already recording "no hazard found."
- **[Tracked, acceptable] Two known schema gaps** against #556's contract (#559's "present-and-mapped-but-not-semantics-preserving" disposition; #561's whole-conversion caveat axis) are recorded on #556 with a re-cost trigger rather than silently absorbed. Vocabulary is consistent (#608's five-term disposition set; #637 cites its four loss-bearing terms).

## 3. Dependency chains

- **[Major] The demo-slice premise is contradicted by the tree's own edges.** The capstone: "PF-5 … **funded first / demo slice** … independent of whether the schematic importer (PF-2) has landed yet." But #562's `blocked_by: [556, 558, 466]`, #635's `ordering_after: [612, 614, 466]`, and #639's dependence on #619's corpus mean PF-5 actually sits behind PF-1, most of PF-2's pipeline, **and #466 (TASK-0111, open, unbuilt)** — the verdict channel #562 itself prices at 9-15 mw and which lives outside this capstone's control. The "cheap 2-3 mw first slice" transitively requires on the order of 15+ mw of prerequisites; the funding order cannot execute as stated. #466 is also **absent from the capstone's `ordering_after`** — the single most load-bearing external edge is unrecorded at the capstone level. (#562's interim `-vcd`-bridge measurement is a genuinely cheap start, but it is a measurement, not the demo slice.)
- **[Self-flagged] Unmirrored edges:** #556's `blocks: [558, 559, 561]` omits 562; #466 carries no `blocks: 562`. #562's own yaml names both gaps.
- **[Real but manageable] AC-level mutual dependency #558 ↔ #562:** #558's AC-1(b) verdict-parity oracle consumes #562's output while #562 is `blocked_by: [558]` — acknowledged in #558's yaml ("in turn"). Not a `blocked_by` cycle, but the two features can only *close* together; the orchestrator should plan joint closure rather than sequential.
- **Otherwise sound.** The `blocked_by`/`ordering_after` graph is acyclic; every edge I followed points at an open issue (no closed/redirected targets — #38 is closed but cited only as a hardening *standard*, which is fine). Cross-capstone edges into CAP-16 are real and mostly recorded: #612 consumes #451's shared `jls.imp.xml` front door (with an explicit either-promotes coordination fallback), #614 orders after #451/#404, and #610 — part of the 1-2 mw PF-1 — is `blocked_by: [451]`, i.e. a slice of PF-1 waits on CAP-16's largest task. CAP's `ordering_after` lists #323/#314, which covers this in spirit; #357 is correctly demoted to a value dependency (its `blocked_by` is empty; confirmed open).

## 4. Staleness, cost bands, open questions

- **[Major, self-declared] PF-2's band is unreconciled.** #558's own body: child tasks sum to **6-9 mw against a 4-6 mw feature band**, with the sum's low end already equal to the kill criterion's 6-mw hard stop — "unresolved and named here rather than absorbed silently." The capstone's 9-14 mw total inherits the understated PF-2 figure, and PF-5's 2-3 is marked "provisional — contingent on #466." The capstone total is not currently supportable; KC-29-1's midpoint check is meaningless until the band or the task prices move.
- **[Stale framing] Generics.** The capstone still calls PF-2's generics refusal "a real coverage cliff … the refused half is disproportionately the course material worth winning" and lists #357 as the gate. #558 AC-3 / #615 have since narrowed refusal to procedural-HGS-only — the common concretely-resolvable parameterized circuit now maps or elaborates-and-declares **with no #357 dependency**. The capstone text overstates both the cliff and the #357 edge and should be softened to match, lest #357 be re-prioritized for a reason that no longer holds.
- **[Verified] Codebase evidence resolves.** Checked at master: `NetlistImporter.java` = 1,067 lines exactly as claimed, no external call sites; `git grep 'DocumentBuilderFactory\|SAXParser\|XMLInputFactory' -- src/` empty; `docs/extension-points.md:33` carries the pending `hdl.importer` seam row; `docs/file-format.md` documents the silent-drop forward-compat valve; `HdlExporter`'s four-bucket disposition policy at lines 65-91; `SchematicLayouter` seam exists for #617. No stale code claims found anywhere in the tree — unusual and to the corpus's credit.
- **[Risk note, non-blocking] #510's evidence corpus is on an unmerged branch** (`claude/jls-project-review-505pnf`); #510 itself marks claims not re-derivable from master as provisional. The capstone's entire evidence line cites #510 §3-4. Landing that corpus on master is already tracked; noted here because CAP-29's strategy (Falstad/CircuitVerse redirects, Digital-first) rests on it.
- **Open questions that do not block start:** #559's CircuitVerse testbench-in-JSON question (recipe-step add-on either way); KC-29-2's re-price trigger (pending CAP-16 corpus measurement — a gate on *further funding*, not on PF-1); the CAP-16 fold-in decision (explicitly non-blocking by its own text).

---

## Verdict and repair list

**not-ready.** No external blocker: PF-1 (#556 → #608) is startable immediately with zero prerequisites, and #612/#614 are startable modulo #451 coordination. But orchestration *from these issues as filed* would misfire in the first week: it would fund de-funded parsers, sequence PF-5 first into two unbuilt prerequisites, and gate capstone closure on ACs its own children correctly refuse to satisfy. The needed work is a bounded editing/closure pass, estimated well under a week:

1. Rewrite CAP AC-2 (and #558 AC-1(b)) from "-t vector translation" to verdict-parity via #466's channel; update PF-5's Outcome/demo-slice text to acknowledge `blocked_by: [556, 558, 466]`, and pick a real first-funded slice (candidates: #556/#608, or #562's interim `-vcd` measurement).
2. Rewrite CAP AC-3 to #556 AC-3's retrofit form (proof against `HdlExporter` + `NetlistImporter`, with #323 adopting the contract when its reader lands).
3. Add #466 to the capstone's `ordering_after`; add the missing mirror edges (#556 `blocks` += 562; #466 `blocks` += 562).
4. Close or re-parent #621, #624, #626, #628, #633 per their own recorded superseded dispositions; decide #629/#631 (retire vs. park under a future parser feature) so no open sub-issue specifies a parser under a "no parser ships" feature.
5. File the missing #558 AC-7 invocation-surface TASK.
6. Reconcile #558's 4-6 mw band against its tasks' 6-9 mw sum, then restate the capstone's 9-14 total.
7. (Minor) Soften the capstone's generics-cliff/#357 framing to match #558 AC-3; record the PF-6 cost split with #553/CAP-27.
