# CAP-36 readiness review (#520) — 2026-08-16

**Verdict: not-ready** — decomposition/AC reconciliation work is needed before this capstone can be orchestrated, and the capstone is in any case self-declared deferred (per #508 row B) with every `ordering_after` prerequisite unstarted. None of the defects below are large; all are issue-text reconciliation, not re-planning. But an orchestrator executing the tree as written today would either violate the capstone's own kill criterion KC-36-3 (via #774) or stall on machine-block edges the issue bodies themselves disclaim (via #776→#778 and #783).

Tree walked: CAP #520 → FEATs #588, #589, #590, #591 (all native children, all open) → TASKs #774/#776/#778 (under #588), #781/#783 (under #589), #866/#867/#868 (under #590), #785/#786/#790 (under #591). External prerequisites fetched: #300, #466, #512, #560 (+#554/#555), #524, #504, #509, #510, #508, #545, #548, #550, #502, #525, #526, #528, #530, #553, #582, #784 — **all open; no edge in the tree points at a closed or redirected issue, and no dependency cycle exists.**

---

## 1. Decomposition

**Filed and open:** all four PFs map 1:1 to native children #588–#591, each `serves_capstones: ["#520 …"]`; each FEAT's TASKs are filed and open. No double ownership survives inspection — #589 vs #591 (source vs derivative), #590 vs #545/#553/#582, and #774 vs #589's white paper all carry explicit, mutually consistent boundary notes.

**D-1 (blocking): the #588 subtree does not compose to #588.** #588 AC-1 requires `docs/grading-contract-conformance.md` — a conformance spec + scored matrix with exit-status-3 as its central axis, gated on #300, its JLS appendix committed under #560's comparison harness. No task delivers that artifact. #774 delivers a *different, weaker* document (`docs/comparisons/grading-contract-rubric.md`, a 5-question substrate rubric that explicitly excludes every #300-dependent claim, AC-2), #776 is the deferred timing note, and #778 is apparatus. All three tasks can close while #588's AC-1 (and therefore CAP AC-1) remains unsatisfied. The feature's own band arithmetic corroborates the hole: #588's grading slice is 3–5 mw, its grading-slice tasks sum to 1–2.5 mw (#774 0.5–1 + #778 0.5–1.5) — the missing ~2 mw is the un-filed conformance-spec/matrix task.

**D-2 (blocking): #774 contradicts its parents' publication gate.** #774's machine block says `ordering_after: []` and its body argues "neither gates its first publication," with no mention of KC-36-3 or the #545/#548/#550 shop-window gates. But #588's "Proposed disposition" states both #300 *and* CAP-36's promotion out of the defer set "must clear before publication," and CAP KC-36-3 bars *any* matrix/comparison under this capstone from publishing while #510 §5's gates are open. A task-level executor following #774 as written publishes a competitor-scoring document while first launch is still an empty tab — the exact failure mode KC-36-3 exists to prevent. Either #774 gains the KC-36-3 gate, or #588/#520 explicitly carve out the substrate rubric as pre-promotion publishable; today the three levels disagree.

**D-3: #589 AC-6 (`jls --guarantees`) has no owning task.** #781 owns paper prose + render/publish; #783 owns the register, `GuaranteeRegisterTest`, and kit transclusion. Neither mentions the shipped-binary query, which is also a CAP PF-2 headline ("queryable from the shipped binary"). Every child of #589 can pass while that clause fails. (Smaller sibling: #589 AC-2's `guarantees-<version>.json` release asset is likewise unowned; #781 covers only the PDF.)

**D-4: acknowledged-but-unfiled citability work.** #591's disposition item 1 records `CITATION.cff` + Zenodo/DOI as "unclaimed, near-zero-cost work… a maintainer should file one." It remains unfiled anywhere in the corpus. Since AC-4 requires only *submission* (not acceptance/DOI), the capstone Outcome's "citing JLS is possible" currently has no guaranteed provider. Hours of work; should be filed before orchestration.

**D-5 (naming drift, cosmetic):** #589 AC-1 says `GuaranteeRegistryTest` / `guarantees.yaml`; #783 says `GuaranteeRegisterTest` / "register." Same mechanism, two names — pick one before implementation forks them.

## 2. Acceptance-criteria composition

- **CAP AC-1** ← #588 AC-1: upward mapping is clean, but see D-1 — no task feeds it.
- **CAP AC-2 / KC-36-1** ← #588 AC-4 ← #778 AC-4/KC: **the retraction mechanism contradicts across levels.** CAP AC-2: "a stale cell is pulled from the published matrix *by that job*, not by a human recheck." #778: "A state change alone is not a retraction: closed upstream is not the same as fixed… a flagged claim is re-verified by a human against a real release before the ledger row is updated or the claim retracted." #778's design is the defensible one (auto-retracting on `state: closed` would retract on stale-bot closes and duplicates), but the capstone text as written makes #778's correct implementation an AC-2 failure. Reconcile downward-to-up: CAP AC-2/KC-36-1 should adopt flag-then-human-verified-retraction.
- **CAP AC-2/AC-3 fixture location** ← #588 AC-3 vs #778 AC-2: CAP and #588 require the appendix "committed under #560's comparison harness — not a second fixture format"; #778 instead builds a general in-tree `DocCommandGoldenTest` over `docs/**/*.md` (generalizing `AutogradeBridgeExampleTest`, which exists on master — verified) with no reference to #560's tree. Defensible engineering, but it is textually the "second convention" the parent forbids; unreconciled.
- **CAP AC-3** ← #589 AC-1/2/3 ← #781/#783: composes, modulo D-3/D-5. #783's register schema (mandatory `exceptions`, no standalone limits section) quietly supersedes #589 AC-5's "limits section" — an improvement, but #589 AC-5 as written ("a section names what JLS does not guarantee") and #783 ("no standalone limits section exists anywhere") are literally contradictory.
- **CAP AC-4** ← #591 AC-1–AC-4 ← #785/#786/#790: composes cleanly; the explicit "submission, not acceptance; does not gate completion" logic is consistent at all three levels. Good.
- **CAP AC-5 (tone review by a non-author)** — partially devolved: #776 AC-2 orders self-disclosure review; #786 AC-3 requires a named external reviewer; but #774 (the note most likely to publish first) has tone rules yet no named non-author review requirement. Minor gap.
- **#590's ACs** ← #866/#867/#868: the best-composed subtree. AC-1/2/3→#866, AC-4→#867, AC-5/AC-6→#868, AC-7 is event-bound (flare release) and correctly not a task, AC-8 defers to #588's discipline. No findings beyond G-2 below.

## 3. Dependency chains

Real and acyclic; no edge targets a closed issue. Defects are edges that overstate or contradict body text:

- **C-1 (blocking for orchestration): #778 is transitively gated on #504 (CAP-23) for no grading reason.** #778 `ordering_after: ["TASK-C588-1", "TASK-C588-2"]`; #776 `ordering_after: [504]`. So the freshness/retraction apparatus that CAP AC-2 depends on sits behind the chronogram capstone — while the timing note it serves is *explicitly deferred and uncosted* at both CAP and FEAT level. #778 should order after #774 only, or gate only its #776-facing rows on #776.
- **C-2: #783 hard-orders after #525/#526/#528/#530**, three of which are demand-gated per #502/#589 AC-4 and "may never land." #783's own boundary says the register core "can start immediately" and only kit-transclusion needs the kits — but the machine block says otherwise, and machine blocks are what an orchestrator reads. The edge should be dropped or scoped to the transclusion AC.
- **C-3: #776's #504 gate is explicitly undecided.** Its machine block states the maintainer call — does the self-disclosure restructuring lift the gate, or not — "has not been made yet." An open decision on a recorded edge; must be resolved (either way) before the subtree is orchestrated.
- **C-4 (precision, not a defect): the real technical gate for exit status 3 is #466, not #300.** #781 gets this right and cites #300's own standing rule ("order after #466 for the verdict engine/report/exit-status, not after #300 itself"); #588 and #589's machine blocks order after #300 (a 23–40 mw capstone) where #466 (one open task, which does ship exit status 3) is the artifact actually needed. As written the edges overstate the wait substantially.
- **External/unfunded on the critical path:** none strictly external. JOSS's editorial response (#785 AC-1) is the only maintainer-uncontrolled dependency, and AC-4/#590-style wording correctly keeps it off the completion gate. #509's co-authorship is correctly opt-in (#790 AC-4). The prerequisites #300(/#466), #560 (itself after #554/#555), #524, and #545/#548/#550 are all open, all unstarted, all in-plan — plus the meta-gate that CAP-36 must first be promoted out of #508's defer set, a trigger ("adoption evidence") with no quantified threshold.

## 4. Staleness and gaps

Evidence verified against **remote master (c5cee1ba)** — note the local clone at 6744b56 is stale; earlier-cycle claims that `docs/capability-roadmap/` was branch-only no longer hold:

- **Resolves:** `docs/capability-roadmap/sweep-02-timing.md` and `lf-04-formal-and-grading.md`, `docs/standards-adoption/04-tool-qualification-and-scope.md` — all on master (the corpus's own "#510's inlined survey on master, not `docs/reviews/evidence/`" correction is right: `docs/reviews/` is absent). Exit-code table in `docs/batch-interface.md` still 0/1/2 only — #588's central factual premise holds. All cited test anchors exist on master: `VcdExportGoldenTest`, `BatchSimulationGoldenTest`, `CliFlagTableTest`, `CliSmokeTest`, `SimulationSemanticsRegressionTest`, `ExtensionPointCatalogTest`, `AutogradeBridgeExampleTest`; `examples/autograde/autograde.py`, `riscv/verify.py`, `scripts/wayland-rig.sh`, nightly-cron lane in `ci.yml`, README `gh attestation verify` + `ghcr.io/anadon/jls` — all present. Deliverable paths (`docs/comparisons/`, `docs/whitepapers/`, `docs/positioning.md`, `docs/release/`) correctly absent.
- **G-1 (stale band): CAP body vs #591.** CAP's `band_mw` note sums "#591 ~0.5-1," but #591 re-derived its band **2026-08-16** to 1–2 mw (0.25–0.5 + 0.5–1 + 0.25–0.5). The CAP's "consistent with the current child features' own bands summed" clause is no longer true (sum is now 5.5–10 vs stated 5–9). Trivial edit, but it is exactly the drift this capstone's own PF-2 exists to prevent.
- **G-2 (internal contradiction in #590):** CAP `demo_slice` names PF-3 "the one PF with no upstream dependency; startable today," while #590's machine block `ordering_after: [545, 548, 550]` "still sequences this issue's own start after them for the feature as a whole" (its words). The boundary note's AC-level carve-out (AC-1–AC-5 authorable now) is the intended reading, but the machine edge and the CAP demo-slice claim flatly disagree for any tool reading edges.
- **G-3 (open questions that block start):** (a) CAP-36's promotion trigger out of #508's defer set is unquantified; (b) #776's #504-gate decision (C-3) is explicitly pending; (c) #783's note that #781's ordering "should be revisited separately (tracked outside this issue)" is tracked nowhere.
- **#785/#790 minor:** #790 lists `ordering_after: [… 509]` while its own AC-4 says submission does not wait on #509 — the edge should be annotated or dropped so an orchestrator doesn't serialize behind #509.

---

## Verdict rationale and the fix list

**not-ready.** The corpus is unusually internally honest — publication gates, kill criteria, and opt-in fallbacks are stated at every level, evidence anchors resolve on master, and the dependency graph is clean of dead edges and cycles. What fails the readiness bar is composition: the #588 subtree cannot close its feature (D-1) and its lead task would violate KC-36-3 if executed as written (D-2); the capstone's own freshness kill-criterion is specified two incompatible ways (AC-2 vs #778); two machine blocks (#776→#778, #783) gate startable work behind issues that are deferred or may never land (C-1, C-2); and one FEAT AC plus the citability record have no owner (D-3, D-4). Fixing all of it is issue-text work, roughly: file one conformance-spec/matrix task under #588, file the CITATION.cff issue, reconcile KC-36-1's mechanism, add the KC-36-3 gate (or an explicit carve-out) to #774, correct four `ordering_after` blocks (#776/#778/#783/#790, plus #588/#589's #300→#466 precision), assign #589 AC-6, and refresh the CAP band. Since CAP-36 is deferred and its prerequisites (#466/#300, #560, #524, #545/#548/#550) are all unstarted, none of this is urgent — but it should be done before, not at, promotion out of the defer set.
