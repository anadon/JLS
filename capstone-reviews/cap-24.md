**Capstone:** CAP-24 (#505) — camera-ready figure export (print SVG, CircuiTikZ, timing figure + WaveJSON, filmstrip, handout bundle)
**Verdict: ready-with-gaps** — start now with #540 → #536 (the demo slice that retires KC-24-1); three named gaps should be fixed before #538 and #536's print-profile work are funded, none requires re-decomposition.

Tree reviewed: #505 → FEATs #536 #537 #538 #539 #540 #541 (all open, all native children, machine block agrees with the sub-issue list) → TASKs #707/#709/#711 (536), #712/#714 (537), #716/#718 (538), #720/#722 (539), #723/#725 (540), #874/#875 (541) — all open. `requires_capstones` and `requires_tasks_exception` are empty and correct.

## 1. Decomposition

The six-feature roster is filed, open, and each row's "what breaks in §1 if removed" argument holds: every §1 observation maps to exactly one owning feature (step 1→#541 composing #536–#539, step 2→#537, step 3→#538, step 4→#536+#537+#538, step 5→#540+#536). Scope boundaries between #536 (artifact + determinism) and #541 (one-command/one-run composition) are explicitly mutually refutable, not overlapping. #874/#875 were verified re-scoped to the `-t`-derived-run design as #541's cross-issue note claims (checked #874's body directly: four artifact kinds, no recorded-run artifact, no PDF, deliberate TASK-C541-1 id reuse recorded).

Three genuine decomposition findings:

1. **#538 AC-6 (`scripts/vcd-to-wavejson`) is unowned by any task — and it is CAP-24 §1 step 3.** #538's own boundary notes concede this: #716 owns the in-memory WaveJSON writer, #718 owns the `-wavejson` batch flag and *explicitly disclaims* VCD parsing, so the VCD-file-to-WaveJSON CLI that step 3 of the capstone's Outcome demonstrates has no task. A TASK-C538-3 must be filed before #538 is funded, or step 3 silently drops out of the deliverable while every filed task passes.
2. **#711 (TASK-C536-3, direct PDF) is still open under #536 with a title that asserts a deliverable the capstone forbids.** CAP-24 resolved OQ-3 as "neither direct nor converted — JLS does not emit a PDF back end"; #536 records #711 as suspended/band-0 pending REPLAN, but the issue itself remains an open native child titled "PDF comes out of the same deterministic renderer." An orchestrator walking the tree can pick it up. It should be closed (or re-scoped) with a REPLAN reference before implementation starts.
3. **The #76 foreground-color sweep is claimed by three documents in two incompatible ways.** CAP-24's §2 roster row for #540 and #536 AC-3 both say the sweep is "delivered as part of #540"; #540 AC-6 explicitly disclaims it — "consumes whatever foreground-color routing exists at pickup time; owned elsewhere (tracked under #76's decomposition, currently assigned to #289)." #289 is open, is not in `requires_features`, and appears in no child's `ordering_after`. Either #536's RenderProfile threading genuinely needs the sweep (then an unfunded prerequisite sits outside the dependency graph) or the CAP roster text is stale (then the "folds into #76's sweep" cost argument for #540's 0.5–1 mw band needs restating). This must be reconciled — most cheaply by a REPLAN on #505 adopting #540's version and stating what #536 actually needs from #289, if anything, for monochrome print output.

## 2. Acceptance-criteria composition

Strong — the best of the CAP-24-adjacent trees reviewed. Each system AC names its child "share" and the children *add* teeth rather than merely restating: #536 adds glyph-coverage (fail-closed on missing glyphs) and diff-locality (the actual "diffs in review" property, which byte-identity alone does not give); #537 adds machine-checked structural connectivity against the netlist (not just "it compiles") and locale-pinned number formatting; #541/#874 add run-arity enforcement via the CLI flag table and a planted-mismatch provenance test; #539 carries both a linear-scaling and an absolute (512 KiB) size bound, asserted not narrated. AC-4's falsification requirement is real and currently true in the tree (see §4).

Two composition wrinkles, both cosmetic-to-minor:

- CAP-24 AC-4 still names `PrintSymbolTotalityTest` and a "scratch element" ratchet; the #540/#723 redirect replaced that mechanism with `ElementRendererTotalityTest` (registry-keyed, standing red case = `FieldExtend`/`RegisterFile`) plus `ElementDrawSmokeTest`'s third parameterization. The intent composes cleanly, but the capstone's AC text is one revision behind its own child — the "machine block/roster agree with reality at close" completion criterion will catch it, but fixing the name now avoids an orchestrator building the wrong test.
- CAP-24 AC-2 says "every back end's export path never calls a font-measurement API"; #537's TikZ back end and #538's emitters restate this per-format correctly, so no child can pass while AC-2 fails. No gap found here.

## 3. Dependency chains

Real and acyclic: #540 (no blockers) → #536 → {#537, #538, #539, #541}, with #537/#538/#539 → #541. The mermaid graph, each feature's `ordering_after`, and #537's prose ("#540 before #536 before this issue") all agree — no contradictions found across the six machine blocks. No `requires_*` edge points at a closed or redirected issue; all six features and all thirteen tasks are open. `related` entries (#71, #154, #216 closed; #369, #405, #508, #77, #121) are correctly reference-only, none load-bearing as a dependency. #812's `blocked_by: #536` is an inbound dependent, not a prerequisite — correctly not on this capstone's path.

External prerequisites: the only one is the TeX Live CI lane (new — zero TeX exists in the tree or workflows today, verified), and KC-24-3 both bounds it (single pinned cacheable subset, dev/CI-only, one required platform per #537 AC-2) and pre-authorizes descoping AC-1's "builds clean in CI" to a non-required job if the bound cannot hold. Not a blocker.

## 4. Staleness and gaps

Verified against the working tree and GitHub master (`c5cee1b`, 2026-08-05; evidence commit `8288226` resolves and is its ancestor, 6 commits behind — current, not stale):

- **Holds:** `getFontMetrics` in exactly 31 `src/` files as claimed; zero tikz/circuitikz and zero wavedrom/wavejson hits in code; ~30 `*Renderer.java` classes + `ElementRenderers.java` registry present; `CircuitRenderer.exportImage` SVG path with pinned defs prefix and deterministic draw order at the cited lines; `TraceGeometry` headless as claimed; `Theme.java` record with two variants and the "~126 hardcoded black call sites" note in its own javadoc; `Trace.MAX_RETAINED_CHANGES = 100_000`; `BatchSimulator`'s 0/1/z-never-x invariant at the cited region; `SvgExportTest`/`ElementDrawSmokeTest` exist; hazard-demo fixture absent from `test/fixtures/` exactly as #536 AC-1 assumes (and #536 owns creating it — the ownership question #537/#538/#539 all defer correctly).
- **Falsification premise still true:** `FieldExtend`/`RegisterFile` shipped as elements (PR #271) but have **no** renderer on GitHub master — #540's standing red case is live, not hypothetical. Note this also means the silent-no-op-render bug ships today; #540 is fixing a real defect, which strengthens its funding case.
- **Cited docs resolve on the default branch:** `docs/standards-adoption/01-iec-ieee-symbols.md`, `07-waveform-formats.md`, `docs/standards-landscape.md`, `docs/batch-interface.md` all present on GitHub master. (They are absent from a stale local clone last fetched 2026-07-28 — a reviewer artifact, not an issue defect.)
- **Cost-band arithmetic:** child nominal bands sum to 10–16 mw (4–6 + 2–3 + 2–3 + 0.5–1 + 0.5–1 + 1–2) against the capstone's "nearer 5–8" standalone band. The gap is covered by #537/#538's own "well under"/"a fraction of" qualifiers, but the nominal sum exceeding the capstone band by ~2x is worth a one-line reconciliation at next REPLAN so the band stays falsifiable. The 2–3 mw demo slice (sink + AWT + SVG + schematic scene subset) is correctly identified as the KC-24-1 risk-retiring first step.
- **Open questions:** both remaining OQs are marked "rides along" and genuinely do not block start; the three resolved OQs (symbol standard, PDF, video) are propagated consistently into every child body — with #711's open state as the sole residue (gap 2 above).

## Verdict

**ready-with-gaps.** Start now: #540 (#723's totality redirect fixes a shipping defect and has no blockers) and then #536's 2–3 mw demo slice, which retires or exposes KC-24-1 before the remaining ~70% of spend. Before funding #538: file TASK-C538-3 for the `scripts/vcd-to-wavejson` CLI (gap 1). Before or at the first REPLAN: close/re-scope #711 (gap 2) and reconcile #76-sweep ownership across #505 §2 / #536 AC-3 / #540 AC-6 / #289 (gap 3). Cosmetic at close: AC-4 test naming and the cost-band sum (§2, §4).
