**Capstone:** CAP-21 (#502) — grading receipts and a report-writer kit; Gradescope now, other platforms demand-gated
**Verdict: ready-with-gaps** — start now (demo slice #701 immediately; core once #466 lands); six named gaps, two of which need a REPLAN before any core issue closes.

## 1. Decomposition

**Required core is fully filed and open.** `requires_features: [524, 525, 531]` all exist, are open, carry `serves_capstones: [502]`, and are decomposed into 11 open tasks: #524 → #686/#687/#690/#692; #525 → #694/#697/#699; #531 → #717/#719/#721/#724. Demand-gated platforms are filed as designed: #526 → #701/#703/#705, #528 → #706/#708/#710, #530 → #713/#715. Ownership splits are explicitly reconciled on *both* sides everywhere I probed (#524↔#724 seeded-violation split, #531↔#710 lane split, #757↔#466 rubric/fixture split, #757↔#350 vocabulary ownership) — this corpus has clearly been cross-reconciled, and I found no double-owned deliverable.

Gaps:

- **PF-7 (per-submission resource envelope) is not filed.** It is required, not demand-gated: CAP §1 step 3, AC-6 (`ResourceEnvelopeTest`), risk 6 ("it ships before any recipe is treated as production-ready"), and the Definition of Done ("PF-7 filed via REPLAN and closed as landed") all depend on it. The underlying claim verifies: `SECURITY.md:53-56` puts resource exhaustion from hostile circuit files in scope, and nothing in the tree bounds wall clock, heap, or output volume (`-d` bounds simulated time only). This is the largest hole in the required core — file it via the §5 REPLAN before the Gradescope recipe is treated as shippable.
- **TASK-C530-3 (nbgrader lane task) is unfiled**, acknowledged in #531's boundary notes. Acceptable since #530 is demand-gated, but it must ride the same demand-gate REPLAN.
- **No child owns receipt emission** — see §2, the decisive AC finding.

## 2. Acceptance-criteria composition

Most CAP ACs compose cleanly downward: AC-2 → #524 AC-2 + #687 (ratchet) + #724 (runs-before-adapter-lanes, deliberately split so neither closes on the other's evidence); AC-3/AC-5 → #694/#699 (single-recipe doc-test, `make-autograder.sh` transcript-diffed against CI); AC-4 → #697 (structural purity ratchet) + #719 (writer provenance) — a reasonable reframing of `RecordedArtifactOnlyTest`; AC-6 → PF-7 (unfiled, above).

**The exception is AC-1, the capstone's headline.** The Abstract, Impact section, and §1 step 2 promise a *re-verifiable grading receipt* — the tuple `(jls_version, lab_spec_sha256, submission_sha256, verdict_digest_sha256)`, "re-computable by anyone." No child issue emits, computes, or tests that tuple. #524 was re-scoped away from the "§7 receipt envelope" to freezing #466's verdict artifact plus an embedded `BATCH-CONTRACT` integer — its exhaustive frozen field list contains no hash field. #531 states it "discharges CAP-21 AC-1's tuple as a build-time golden diff" — i.e. determinism of writer output is proven, but `lab_spec_sha256` and `submission_sha256` are never computed anywhere. **Every child can close green while the capstone's regrade-dispute story ("why did I get this score") fails as written.** Resolution is cheap but must be explicit: either (a) a small task owning receipt emission (a fifth line in the verdict artifact or a `-receipt` rendering), or (b) a REPLAN re-wording AC-1/Abstract to the determinism claim #531 actually proves. KC-21-1's own protocol ("re-scope by REPLAN ... do not quietly drop AC-1") demands this happens before #531's goldens are checked in.

Second contradiction: CAP §1 step 4 and AC-2 freeze the "§1 invocation surface"; #524's Outcome *explicitly excludes* argv shape from the frozen surface ("deliberately not part", additive-only under §6's existing clause) — a defensible re-scope, but the CAP body still tests the old scope. Relatedly, CAP AC-1 enumerates `--report {gradescope,xunit,text,prairielearn}` (a prairielearn writer is demand-gated PF-4, and the actual landed-grammar plan per #466/#525 is `-check e -report <file>`, not `--report <format>`). The CAP body predates the children's rewrite; the machine block even says the *children* "each need a follow-up edit" when in fact the children are now ahead of the CAP prose.

## 3. Dependency chains

**Real and acyclic.** Verified chain: #466 → #686 → #687 → #690, #466+#686 → #692; #524 → #694 → #697/#699; #524 → #717 → #719/#721 → #724 (also after #687). Feature-level: #466 → #524 → #525 → #531 → CAP-21. No cycle; no required edge points at a closed issue (#72, #216 closed but cited as prior art in `related` only — correct). The demo slice #701 has `ordering_after: []`, targets the currently-shipped three-exit-status contract (verified against `docs/batch-interface.md` §1), and can start today.

Findings:

- **Critical-path prerequisite is #466 (open, unbuilt).** Verified in-tree: the batch contract still has exactly three exit statuses and no `-check`/`-report`; the verdict channel does not exist. Internal and funded (CAP-06's band), so this is sequencing, not "blocked" — but nothing in the core (#686 onward) can land before it.
- **`blocked_by: [369, 466]` is over-broad.** #369's `blocked_by: [316, 321, 347]` gates only its formal child #483, and both #369's scope note and #524's `ordering_after` comment say verdict consumers must cite #466 alone — #369's note records that citing #369 has *twice* dragged the 12–20 mw SimpleEditor decomposition into unrelated issues. Narrow CAP-21's `blocked_by` to `[466]`.
- **Mirror edges still missing**, as the CAP itself flags: #466's `blocks` is `[214, 524, 686, 757]` and #369's is `[]` — neither lists 502. Bookkeeping, not structure, but the DoD's "machine block agrees with reality" check will fail until added.
- **Unrecorded cross-capstone edge:** #525 AC-6 is "satisfiable only once #757 (TASK-C576-2, under FEAT #576) extends the rubric with platform-neutral point fields." #757's AC-5 does now carry that obligation (the reconciliation was done), but the edge appears in neither CAP-21's machine block nor #525's `ordering_after` (`["FEAT-C21-1"]` only). #757 orders after #755+#466 — a real critical-path dependency for PF-2's close that a scheduler reading only machine blocks will miss.

## 4. Staleness, costs, open questions

- **Evidence verifies.** `8288226` resolves (merge of PR #294). Confirmed at master: zero grep hits for gradescope/prairielearn/nbgrader; `examples/autograde/` contains only `autograde.py`; `batch-interface.md` §6 freezes §§2–4 but not §1; `SECURITY.md:53-56`, `vcd-interop.md` recording-not-session text, `README.md:103-124` container docs, `FileFormatSpecTest`, `HeadlessCoreRatchetTest`, `UntrustedFileHardeningTest`, and the `FORMAT` header (§4, `file-format.md`) all exist as cited.
- **Cost bands are contradicted by the children, roughly 2–3x.** CAP core band "3.5–5 mw" vs the children's own task sums: #524 = 2.5–4 (CAP said 0.5–1); #525's tasks = 2.1–3.25 (feature band 1–2; CAP said 1); #531's tasks = 3.5–5 (feature band claims "1 mw per #502's own Cost section" while its four tasks sum to 3.5x that). Task-level core sum ≈ **8–12 mw**, plus PF-7's 1–2. Demand-gated bands drifted too (#526: 2–3 vs CAP "~1"). Not start-blocking, but the CAP's Cost section should not be used for scheduling.
- Minor staleness: #524's Outcome says `-check`/`-report` "landed via #466" — #466 is open, nothing landed. #694's band note records "#524's own review found it needs-rework"; whatever cycle that referenced, treat #694's band as contingent on #524 stabilizing.
- **Open questions:** both are marked rides-along/non-blocking and genuinely are (PrairieLearn generator kit deferred to a demand-gate; LTI reopen wording). No blocker here.

## Verdict: ready-with-gaps

The tree under #502 is one of the better-reconciled capstones in this corpus: complete required-core filing, clean two-sided ownership splits, an acyclic and mostly-honest dependency graph, and an immediately startable demo slice (#701). Start now, in this order: #701 (no prerequisites) in parallel with #466 (CAP-06's slice); then #686→#687→#690/#692, #694→#697/#699, #717→#719/#721→#724. Before any core issue closes, resolve by REPLAN on #502: (1) **file PF-7**; (2) **decide the receipt** — emission task or re-scoped AC-1 wording (required before #531's goldens per KC-21-1); (3) fix the CAP body's §1-invocation-freeze and writer-enumeration contradictions with #524/#525; (4) narrow `blocked_by` to `[466]` and add the 502 mirror on #369/#466; (5) record the #757 edge on #525 and the CAP machine block; (6) refresh the Cost section from the children's bands.
