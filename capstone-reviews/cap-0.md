# CAP-00 (#296) readiness review

**Verdict: ready-with-gaps** — start now; the two unblocked entry features (#315, #353) are startable today, every defect anchor re-verifies at master, and the graph is acyclic. Six named gaps below, none of which requires re-decomposition before the first feature starts, two of which must be resolved before FEAT-007 (#317) or the capstone itself can close.

Reviewed 2026-08-16 against: the #296 body, all seven required features (#314, #315, #317, #334, #335, #353, #354) and their machine blocks, their native sub-issue TASKs, the referenced non-required issues (#316, #337, #355, #299, #300, #301, #363, #406, #436, #163, #352, #661–#670), and the working tree at master `6744b56`.

## 1. Decomposition

**All seven required features are filed and open**, each with a filed task roster that is also entirely open: #314 (TASKs #404, #408, shared #372, #451), #315 (#372, #375, plus adopted #488, #492), #317 (#374, #406, #411, adopted #91, #162; shared #378), #334 (#436, #437; Slice A unfiled), #335 (#377, #378, #379, #407, #413, #442), #353 (#376, #405), #354 (#410, #438, #439, #469). No required feature or task is closed. The deliberate exclusion of FEAT-008 (#316) and FEAT-011 (#355) holds up: CAP-00's §1 walk-through and AC-1–AC-7 are all satisfiable without them, and removing #316 genuinely removes #337 from this capstone's path (#337 is reached only via #316's `blocked_by: [317, 337]`, an edge pointing away from this capstone).

**Gap 1a — D-07's closing artifact has no filed owner.** The exemption-ledger lint (AC-4) and the `RatchetRegistryTest` meta-registry (AC-1) are assigned to "this capstone's own close-out, seeded by FEAT-007 (#317)" — but #317's filed text builds a `.github/ci-lanes.yml` policy ledger and its cross-check test, and nowhere mentions a JaCoCo package-exemption table, `RatchetRegistryTest`, or the `WAIVED:` append-only meta-assertion. `requires_tasks_exception` is empty. By CAP-00's own re-planning protocol ("a defect whose owner is removed becomes orphaned scope: re-home it, or file it as a task and record it in `requires_tasks_exception`"), this scope needs a filed home before AC-1/AC-4 are anyone's deliverable. Small work (a lint + a table + a meta-test), but currently owned by nobody.

**Gap 1b — FEAT-003's decomposition is incomplete by its own admission.** #334 states "Filing this task [Slice A] is this feature's own next action" — Slice A carries AC-7's diffability tooling (the `-canon` export / clean-filter work the capstone's `textconv` driver builds on) and is not filed. Additionally #436 (Slice B, the D-02 fix) declares `part_of_feature: 334` but is not attached as a native sub-issue of #334 (only #437 is), and #436's `blocked_by: []` explicitly disputes #334's claim that Slice B is gated on #315 (#436's argument: `SaveTags` maps type tags, not the item-kind grammar it touches). The feature-tier edge #315→#334 stands per protocol, but the task under it says the gate is vacuous — worth an adjudicating REPLAN on #334 rather than silent divergence.

**No double-ownership found.** Shared tasks are declared as composition, not duplicated: #372 (in #314's and #315's `requires_tasks`, native to #315), #378 (in #335's `requires_tasks`, listed in #317's `planned_tasks` as "shared with FEAT-009"). The one prior double-ownership hazard — #406's four literal `ci.yml` edits vs. the #661–#664/#667–#670 slices — has been resolved in #406's favor: six of the eight slices are closed as duplicates of #406, which now owns the edits via its Phase A/B plan (#662 and #668 remain open and are flagged in #406 for the same closure).

## 2. Acceptance-criteria composition

- **AC-2 (defect list closed):** composes cleanly for D-01→#314, D-02→#334, D-03/D-04→#354, D-05→#353, D-06→#317/#374. D-07 is the exception — see Gap 1a. Every child could close green and D-07's lint still not exist.
- **AC-1 (append-only registry):** the meta-mechanism is un-homed (Gap 1a). #317's ci-lanes ledger delivers the *CI* rows; nothing filed delivers the registry-of-registries assertion.
- **AC-3 (timeout totality):** fully owned by #374, whose rewritten mechanism (standalone policy check, anti-vacuity fixture) matches the criterion including the sane-value bound.
- **AC-4 (no silently unfloored package):** mechanism un-homed (Gap 1a); citation target stale (Gap 3 below) though the cited issue #316 is itself open, so the ledger entry would still resolve.
- **AC-5 (three-platform required):** fully homed after the slice collapse — #406 Phase B (drop `continue-on-error`) gated on #374, display promotion gated on #162→#91 retiring `rerunFailingTestsCount=2` (verified still present in intent at `pom.xml:293` territory). Open Question 3 (JDK matrix width) blocks AC-5 *acceptance*, not start, as the body itself says.
- **AC-6 (joint lane fit):** correctly capstone-level; no child owns it, by design.
- **AC-7 (textconv diffability):** the predicate is well-formed and its no-numeric-threshold reframing removed the FEAT-009 dependency as claimed; but the artifact lives in FEAT-003's unfiled Slice A (Gap 1b).

Upward composition is otherwise sound: the §2 minimality table's per-feature removal consequences were spot-checked and hold (e.g. removing #353 really does un-sequence #354, #317, #335 — all three carry `blocked_by: [353]` in their own machine blocks).

## 3. Dependency chains

**Verified real and symmetric** (both endpoints' machine blocks agree): #315→#334, #353→#354, #353→#317, #353→#335, #354→#317. **Acyclic**: every inward edge terminates at #315, #353, #314 (`blocked_by: []`) or #363 (`blocked_by: []`, its former [319] edge dissolved). No required edge points at a closed issue.

**Gap 2 — the body's `blocked_by: []` claim is false: #317 is `blocked_by: [353, 354, 363]`, and #363 (FEAT-035, resume-by-replay) is outside the required set.** CAP-00 asserts "No required feature reaches a prerequisite outside this set. (#337 only entered the graph through #316…)" — but #317's redirection pass (post-dating the CAP rewrite) rebuilt its long-run capability on checkpoint-chain segmentation, adding #363. #363 also `blocks: [378]`, so FEAT-009's required task #378 transitively gates on it too. Mitigating: #363 is open, filed, funded (serves CAP-02 #301), self-scoped at ~1–2 mw, and unblocked — so this is not an *unfunded* external prerequisite and does not justify `blocked`. But it is on CAP-00's critical path, unpriced in the 17–32 mw band, and per CAP-00's own protocol ("a required feature acquires a prerequisite outside the required set → record it in the `blocked_by` comment") it must be recorded. Cost band with it: 18–34 mw — still inside KC-00-1's 48 mw kill line by a wide margin.

**Gap 4 — the mermaid edge F001→F002 (#315→#314) is contradicted by both endpoints.** #314's `blocked_by` is `[]` (it gates on #372 at close-out via `requires_at_closeout`, a task edge), and #315's machine block explicitly says "#314 … deliberately NOT listed." The CAP body claims every edge was "checked symmetric in both directions"; this one now fails that check. §3 Risk 1's sibling framing ("#314 and #334 both sequenced behind #315") is stale with it — the risk's *substance* (FEAT-002's legacy-attribute allowlist as a contract term FEAT-003 must empty) survives and should be kept. Per the CAP's own rule the features' blocks win and the graph regenerates; this is a mechanical REPLAN, not a re-plan of substance.

## 4. Staleness and open questions

- **Gap 3 — the FEAT-008 re-homing rationale points at a closed capstone.** CAP-01 (#299) was closed 2026-08-12 as a duplicate of #163 per product review #508. #163's `requires_features` is [167, 168, 169, 170, 171] — it does **not** carry #316 or #337. So "FEAT-008 … already serves CAP-01" and AC-4's "#316 (itself gated on #337 inside CAP-01)" are stale, and #316 (`serves_capstones: [299]`) is now orphaned at capstone tier. This does not un-justify removing #316 from CAP-00 (the removal stands on CAP-00's ACs not needing it), but AC-4's exemption entry cites an issue whose funding home has dissolved; the citation should be to #316 alone, without the CAP-01 framing, and #316 needs a new capstone home on its own side.
- **Evidence commit 333523a is not on the default branch.** It resolves only on `origin/claude/github-issue-review-agents-j99xga` (a strict descendant of master: 39 ahead, 0 behind; deltas are issue-review corpus, docs, and trivial pom/About metadata). **Every load-bearing code claim was independently re-verified at master `6744b56` and holds exactly**: the four silent `setValue` fall-offs (`Element.java:344–396`), dense-id reassignment (`Circuit.java:1499–1503`), `dupCheck.remove` before the `maxTime` check (`Simulator.java:224–234`), `pause` ≡ `stop` (`BatchSimulator.java:77/:89`), the four quadratic `+=` sites (`SigSim.java:64–75`), **23 workflow jobs / 0 timeouts** (census matches the body's per-file breakdown: ci 14, release 5, codeql/mutation/repro/scorecard 1 each), the `jls.edit` prose-only exemption (`pom.xml:408`), 4 JaCoCo `PACKAGE` includes, `jls.sim` floors 0.930/0.920/0.845, the `savetext` flag (`JLSStart.java:787–788`), 35 registry types, 5,852-line `SimpleEditor`, 82,120-line `src/`. The DoD requires citations to resolve on the default branch at close — either the planning branch merges or the pin moves to a master commit.
- **FEAT-009's citation caveat is accurate and self-flagged:** `docs/plan/**` is absent from master (verified: 0 tracked files); the CAP already conditions FEAT-009's start on re-deriving those citations. Good.
- **Minor:** the prior-art section cites "#352 (FEAT-052) is `blocked_by: [315]`" — #352 closed 2026-08-04 as a duplicate of #170 (which #315's own block already records). #314's `serves_capstones` still lists closed #299. #317's top-matter still says the `ci.yml` edits are "ceded to #661–#664/#667–#670" — those slices are closed as duplicates of #406, which re-owns the edits.
- **Open questions:** OQ1/OQ2/OQ4 carry recommended defaults and block nothing. OQ3 (JDK matrix width) blocks AC-5 acceptance only; the body says so and the PR #233 flake evidence it cites (`pom.xml:404–406` headroom convention) is present at master.

## Verdict

**ready-with-gaps.** Start with #315 and #353 (both `blocked_by: []`) immediately. Before FEAT-007 closes: land or re-slice around #363 (Gap 2). Before capstone close: file the D-07 exemption-lint / ratchet-registry task and record it in `requires_tasks_exception` (Gap 1a), file FEAT-003's Slice A and native-parent #436 (Gap 1b), refresh the CAP-01/#316 framing in AC-4 and the Proposed disposition (Gap 3), regenerate the mermaid graph dropping F001→F002 and adding F035→F007 (Gaps 2, 4), and re-pin the evidence commit to a default-branch commit. None of these changes the required feature set, the ordering of first work, or threatens the 48 mw kill line.
