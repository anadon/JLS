**Capstone:** CAP-39 (#888) — no value is silently coerced; floating input, contended bus, floating clock all read X at probe / `-t` stdout / VCD, binary corpus byte-identical.
**Verdict: ready-with-gaps** — the dependency tree is real, acyclic, fully filed and open; the redirect away from mixed-alphabet scope is coherently reconciled across #344/#361; every cited evidence anchor verifies. Start is gated only on the already-filed #878 → #879 chain. Five gaps are named below; one (AC-5) is a defective acceptance criterion that must be rewritten before the fixture phase, and one (the capstone's own unestimated increment) is a pre-scheduling obligation the body itself acknowledges.

## 1. Decomposition

- **#888 has zero native sub-issues** (`sub_issues` returns `[]`). That is consistent with the corpus convention — its one required feature, **#322 (FEAT-026)**, serves multiple capstones (#301, #304, #306) and is correctly a shared feature, not a native child. Verified open.
- The machine block is `requires_features: [322]` only; no `requires_capstones`, no `requires_tasks_exception`. #322's own tree: child **#391** (TASK-0057, resolution fold — open, `blocked_by: []`, startable today per its own "no dependency on the value-type migration" argument and #322's recorded condition-lattice sequencing option); external prerequisites **#878** (value type, open) → **#879** (plumbing + perf gate, open, `blocked_by: [878]`), both filed under #232. Every node on the chain is filed and open. Nothing required is closed; nothing is double-owned — the #341-vs-#322 ownership of TASK-0057 is recorded as ratified (#391 declares `part_of_feature: 322`; #341 consumes it as an ordering edge only).
- **Gap (G1): the capstone's own increment is unfiled.** The three fixtures, their goldens, and the k2000 benchmark harness belong to no task issue anywhere — #322's DoD does not include them, so #322/#391/#878/#879 can all close as landed with nobody owning AC-1..AC-4's deliverables. The body itself concedes the neighboring half of this ("has no separate estimate yet and should get one before it is scheduled"). File the increment (one task or three) before orchestration, or the capstone has no closure mechanism.
- **Explicit non-requirements verified reconciled.** #344 and #361 have both already executed the resolution #888 asked of them: `serves_capstones: []` on each, with the redirect quoted and the re-planning protocol invoked ("this feature currently has no beneficiary... recorded rather than papered over"). #888's sentence "Both **currently** record #888 as their sole `serves_capstones` beneficiary" is now stale — resolved in exactly the direction #888 requested, so this is cosmetic. #419's Definition of Done carries the port-not-net guarantee independently, as claimed.

## 2. Acceptance-criteria composition

- **AC-1/AC-2/AC-3 compose cleanly** to the Outcome. Each pins a committed fixture against a golden at all three rendering points; AC-2's per-bit confinement clause and AC-3's byte-identical-register clause are correctly written so a whole-signal-blanking or phantom-edge implementation fails. They are exactly the capstone-level checks that #322's feature-level criteria (its §5 items 2–4) do not assert alone — in particular AC-3 pins one of #322's 27 per-element `react` coercion decisions (`Register`'s clock input) that no #322 criterion names individually. Good upward composition, no overlap.
- **Gap (G2, the serious one): AC-5 has no owner in the required set.** "Every pre-existing element type continues to report a `[0,1]` domain, swept mechanically over `ElementRegistry`" presumes a per-element **domain-reporting API that does not exist at HEAD** (grep over `src/jls/` finds no `getDomain`/`domain()`; `ElementRegistry` carries none) and that **no required feature builds** — element/port alphabet domains are #344's scope (via #419/#422), which #888 explicitly does not require and whose absorption KC-39-2 explicitly forbids. AC-5 is near-verbatim #361's integration criterion 2 ("Every pre-existing type still reports `[0,1]`... swept over `ElementRegistry`") — a residue of the pre-redirect mixed-alphabet framing that survived the rewrite. As written it is either unfalsifiable (nothing to sweep) or a KC-39-2 violation (build #344's machinery to satisfy it). Rewrite it to a property #322 actually delivers — e.g. "every circuit file lacking a `semantics` attribute loads as `compat` and renders/simulates byte-identically, swept over the golden corpus" (#322's Global Invariant 5 / criterion 7) — or delete it.
- **Minor (G3): AC-4's flat-result ambiguity.** The cited gate (`keystone-c-performance.md` §8.1, verified at the evidence commit) is *"the event loop is no slower, and the expectation is 15–25% faster"* — no-slower is the gate, improvement is the expectation. AC-4's gloss "not a tolerated flat result" makes it undecidable whether a flat benchmark passes CAP-39. Since KC-39-1 makes this criterion revert-triggering, state numerically whether flat passes. Note also the improvement is really #879's/Stage-0's deliverable; CAP-39 inheriting it as a hard AC double-charges a number the capstone's own increment cannot move.
- The three counted claims verify at HEAD: 35 `ElementType(` registrations in `ElementRegistry.java`, 27 `react` implementations, `Put.java:385` `@Nullable BitSet currentValue`, `WireNet.propagate` at `:443`.

## 3. Dependency chains

- **Chain: #878 → #879 → #322 (∥ #391) → CAP-39. Real, acyclic, fully open, fully in-repo.** #322's recorded DAG walk (upstream ∩ downstream = ∅) checks out; #391 is deliberately unblocked and offers a startable first slice (the fold over a condition lattice) that delivers the classroom-visible win early. No edge points at a closed or redirected issue; no unfunded external prerequisite exists — #878/#879 are filed, and #232's H1 falsification gate is resolved *confirmed* (37.6% warm-loop share), so the "no swap is ever built" stranding risk is retired. The two-plane-vs-three-plane conflict is recorded resolved by #878's 2026-08-16 rewrite (three-plane `LogicValue permits Word, Wide`).
- **Gap (G4): #322's `serves_capstones` is stale in both directions.** It still lists **#295, which is closed** (`not planned` — and #888's own `supersedes_disposition` insists it stays closed), and it **omits #888**, which it names only in prose/`related`. Consequence: #322's DoD obligation "every capstone in `serves_capstones` notified with a `STATUS:` comment" would notify a closed capstone and skip this one, and #322's re-planning line "Losing #295 specifically would free the spare-code-point reservation" describes a loss that has already happened without the recorded consequence being drawn. One-line mirror fix on #322: `serves_capstones: [301, 304, 306, 888]` (or 888 under a consumes-notation the corpus prefers), plus reconciling the #295 spare-code-point rationale with the measured (§5/§10/§11) justification that now carries the three-plane choice alone.
- **Gap (G5): an open adjudication sits on the critical path.** The #232-vs-#322 ownership of the representation migration itself is explicitly unresolved ("escalate to the maintainer"; #232's 2026-08-09 disposition names the gap; #322 tracks it in Related issues). It does not block starting #878 or #391, but the migration is the bulk of the 17–22 wk band CAP-39 inherits, and an ownership flip mid-flight is a re-plan on both issues. Adjudicate before the plumbing (#879) starts in earnest.

## 4. Staleness / gaps

- **Evidence verifies.** `evidence_commit: 8288226` resolves (merge of PR #294, 2026-08-02). Maintainer comment **5227653907** exists on #888 (anadon, 2026-08-08, the port-not-net directive superseding PR #887). `docs/capability-roadmap/README.md` §3 is the four-state-plus-strength keystone with sixteen/twenty-four dependent entries as claimed; `keystone-c-performance.md` carries the exact AC-4 baseline (`k2000.jls`, 2,331,793 events, 0.742 s, 318 ns/event, 8,090 cycles/s) and the §8.1 gate. `docs/batch-interface.md` §4.3/§6 frozen clauses ("`x` never appears"; major-bump-or-flag) exist as #322 says it must amend. (Reviewed against commit 8288226 and current GitHub master `c5cee1b`; the local working checkout is at 2026-07-28 and predates these docs — a review-environment note, not an issue defect.)
- **`riscv/build/k2000.jls` is a generated artifact, not committed** — `riscv/build/` does not exist in the tree at any commit; the fixture is produced by `riscv/build_cpu.py`/`jlsbuild.py`. AC-4 is therefore reproducible but not turnkey: the unfiled benchmark-harness increment (G1) must pin the regeneration recipe and the vector file, or "measured the same way" is not mechanically checkable.
- **Cost band honest but incomplete by its own admission.** `band_mw: "see Cost"`; the capstone's own increment has no estimate and the body says to get one *before it is scheduled*. That is a pre-start obligation, folded into G1.
- **No open question blocks start.** #322's remaining open question (U-with-reset) rides along; its Open Questions 1 and 3 are recorded resolved. The demo_slice (coercion fixture before corpus re-run) is well-ordered.

## Verdict and required actions

**ready-with-gaps** — orchestration can start now on the filed chain (#391 immediately; #878 → #879 next; #322's migration after), while:

1. **(G2, before the fixture phase — ideally before start)** rewrite or delete AC-5; as written it requires scope KC-39-2 forbids or is unfalsifiable.
2. **(G1, before scheduling)** file the capstone's own increment (fixtures + goldens + harness incl. k2000 regeneration recipe) as task issue(s) with an estimate.
3. **(G4, one-line)** fix #322's `serves_capstones` mirror: drop closed #295, add #888.
4. **(G5, before #879)** obtain the maintainer adjudication of #232-vs-#322 migration ownership.
5. **(G3, editorial)** make AC-4 state numerically whether a flat benchmark passes.
