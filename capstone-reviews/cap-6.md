**CAP-6 / #300 — course delivery and autograding at scale**
**Verdict: ready-with-gaps** — start now on the unblocked spine (#466, #353, #337); five named gaps need bookkeeping or a REPLAN before close, one of them before #354's implementation planning finishes.

### 1. Decomposition

**Sound core.** All six `requires_features` (#317, #334, #337, #353, #354, #369) are filed and open, and every one carries the mirror `serves_capstones: 300` in its own machine block — verified individually. Each has filed, open task-tier children (#317: #91/#162/#374/#406; #334: #437; #337: #382/#412; #353: #376/#405; #354: #410/#438/#439/#469; #369: #214/#466/#483). #300 itself has no native sub-issues, consistent with the shared-feature convention. The demo slice's task owner #466 is open with `blocked_by: []` as the capstone claims, and the #757 ownership boundary (directory-mode command) is recorded symmetrically on both issues. The FEAT-016/#340 and FEAT-017/#357 exclusion argument is sound, and both issues' `serves_capstones` have **already been corrected** to drop 300 — the capstone's note that "the owning issues should be corrected" is now stale in the harmless direction.

**Gap D1 — the per-submission budget (AC-8, §1 step 7) is owned by no filed issue.** The capstone states "FEAT-006 (#354) owns the budget enforcement." #354's own body never mentions a per-submission wall-clock or heap budget — its scope is single-run capacity (past-limit event adjudication, run-outcome reporting, DenseWordStore paging, RunControl suspend/resume, streaming trace writer); its only "budget" is the memory-paging byte budget. #466 and #757 contain zero mentions of a budget, an `exceeded-budget` bucket, or fault isolation either. Every required feature could close as scoped and nobody has built the mechanism AC-8, §1 step 7, and the step-1 summary line's `X exceeded-budget` bucket require. This is the classic every-child-passes-while-the-capstone-fails hole. Fix: scope it explicitly into #354 (or a new task under #369/#576) before #354's task set is finalized.

**Gap D2 — the `-ref` reference-oracle mechanism has no task-tier owner.** The capstone's spine narrative (Abstract, §1 steps 1-2/5, AC-1's reference digest, AC-4, AC-5) is a reference-*circuit* differential: run the reference and each submission through the same stimulus, diff traces. But #466 was re-scoped to an **expectations-file** channel (`jls -b -t v -check e -report r.xml` — its own AC-1 wording), #757 folds #466's `GradeReport`s (also expectations-based, no `-ref`), and #483 is the deferred equivalence/property mode this capstone explicitly does not require. Nothing filed owns deriving expectations from a reference circuit or the `-ref`/`-cex` flags and reference-digest fields the capstone's ACs name. The expectations-file design plausibly composes to the same outcome (run the reference once to produce the expectations), but then AC-1/AC-4/AC-5 and §1's literal command lines need rewording, or a small derivation task needs filing under #369. The capstone's demo-slice paragraph also still lists "`-ref`, `-cex` … a directory operand" as #466-owned, though the directory operand is #757's by both issues' own boundary text.

**Gap D3 — closure semantics span unrequired work.** AC-7 (300 submissions / 30 min), AC-8 ("the other 299 … in the same invocation"), and three Definition-of-Done checkboxes (the §1 walk-through with summary line, the 300-corpus run, AC-7's five parameters) all require the directory-mode invocation the capstone explicitly assigns to #757 (TASK-C576-2, `part_of_feature: 576`) — and #757/#576 appear in neither `requires_features` nor `requires_tasks_exception` (empty). As written, the required set can fully close while the capstone's own DoD is unexecutable. Fix: add #757 to `requires_tasks_exception` (it is small, 1-1.5 mw, `ordering_after: [755, 466]`), or re-scope AC-7/AC-8/DoD to the per-submission surface.

**Minor:** #436 (TASK-0005, the stable-id-refs slice this capstone's #334 residual needs) is filed and open but is not a native sub-issue of #334 (only #437 is), and #334's own block says #436's filed scope "undershoots this design and needs correcting" — unresolved. #354's unfiled planned "TASK-0015" (streaming VCD writer) overlaps #353's filed #405 and collides in number with #317's TASK-0015/#374; #354's boundary note acknowledges the overlap and defers reconciliation.

### 2. Acceptance criteria

AC-1 through AC-6 compose upward well: each names a concrete new test, its owning feature, and a falsifying today-state that I verified (no report artifact — `grep -rniE "xunit|testsuite|junit-xml|results\.xml" src/` → 0; three exit statuses only — `docs/batch-interface.md:38-40`; the AC-3 adversarial submission passes today — `examples/autograde/autograde.py:45-57` literal finals/stdout lines, pinned by `test/jls/AutogradeBridgeExampleTest.java`, which exists). The determinism split (canonical verdict record pinned, xUnit envelope exempt) is a genuinely good design. The upward-composition failures are the three gaps above: AC-8's mechanism is unowned (D1), AC-1/AC-4/AC-5's reference-digest fields are unowned at task tier (D2), and AC-7/AC-8/DoD depend on unrequired #757 (D3).

### 3. Dependency chains

Acyclic and mirror-verified among the required set: #353→#354 (#354 `blocked_by: [353]`), #353→#317 and #354→#317 (#317 `blocked_by: [353, 354, 363]`) — exactly the mermaid edges, drawn per the graph's stated rule. No required edge points at a closed issue. But the critical-path disclosure is incomplete:

- **Disclosed:** #369 `blocked_by: [316, 321, 347]` — all three open, all outside the required set, cost excluded from the 23-40 mw sum; the capstone flags this and proposes narrowing. Still un-narrowed today; the #466 (`blocked_by: []`) escape hatch is real for the demo slice, but capstone *closure* requires #369 closed, which today drags in FEAT-008's residual, FEAT-019 (netlist export), and FEAT-034 — the latter two plausibly serve only #483's deferred equivalence mode, strengthening the case for narrowing.
- **Undisclosed:** #317 `blocked_by` also names **#363** (FEAT-035 checkpoint/resume, open, unrequired, unpriced here) — an edge added by #317's redirection pass, absent from the capstone's mermaid (excluded by its node-set rule) and from its critical-path cost caveat, which names only #316/#321/#347. And #334 `blocked_by: [315]` (FEAT-001 residual, open) gates exactly Slice B — the stable-id-refs residual this capstone requires from #334. So the true critical path includes #315 and #363, neither priced nor mentioned.
- #299 (CAP-01) is **closed as duplicate**, yet §2 and Related still describe it as the live funder of #340/#357 ("belong to #299, which already funds them as required rows there"). #340 is now funded by #304/#311, #357 by #295/#301/#304, so nothing is orphaned — but the text should point at the surviving funders.

### 4. Staleness / gaps

- **`evidence_commit: 2d0ca9d` is not on the default branch.** It is a merge commit on side branch `claude/jls-virtual-hardware-linux-njsoma` (2026-08-02), diverged from master (master is 6 ahead / 15 behind it). Four of the six required features pin the same commit. The DoD's own rule ("every citation resolves on the default branch") — and #334's explicit warning about the phantom 970db41 precedent — argue for re-pinning to a master commit. Mitigating: I re-verified every load-bearing citation on the current tree and **all hold** — `SigSim.java:71-75` per-line string concatenation; `Simulator.java:225/231-233` dupCheck-remove-then-drop past `maxTime` (only simulated time bounded); `BatchSimulator.java:87-90` pause == stop; `FileAbstractor` XZ default; `Element` file-local `int id` + `stableId`; `CircuitOp.apply(Circuit, Graphics)` sealed interface; `-savetext` flag present; `timeout-minutes` count 0 in workflows (re-checked against remote master's ci.yml, which changed since); a11y checklist exactly 146 lines; installer file-associations at `build-installer.sh:363/409/477`.
- Cost bands are internally coherent (demo slice 4-7 ⊂ #369's 9-15; 23-40 declared an upper bound with the two residual rows priced at full scope; the uncitable marginal band honestly deleted) — but the "critical path, not standalone cost" caveat undercounts by omitting #315 and #363 (see §3).
- Both Open Questions are explicitly non-blocking for the demo slice, and genuinely are.

### Verdict: ready-with-gaps

Start now: #466, #353, and #337 have empty `blocked_by` and are the demo-slice spine; nothing in gaps D1-D3 blocks the first 4-7 mw of work. Before close (and ideally before #354's plan freezes): (1) give the per-submission budget a filed owner; (2) reconcile the `-ref`/reference-digest surface with #466's re-scoped expectations-file design, or file the derivation task; (3) put #757 in `requires_tasks_exception` or re-scope AC-7/AC-8/DoD; (4) extend the critical-path note to #315 and #363, and pursue the already-proposed narrowing of #369's `blocked_by`; (5) re-pin `evidence_commit` to a default-branch commit and refresh the #299/#340/#357 funder text.
