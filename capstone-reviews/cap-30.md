**Capstone:** CAP-30 (#514) — outside developer's first PR merges within a week; external contributors retained
**Verdict: not-ready** — decomposition/AC reconciliation needed before orchestration. The tree's coverage of PF-1..PF-6 is complete, the dependency graph is acyclic, and the task-tier bodies are unusually well-verified against the code — but the capstone body and two of its five features contradict their own children at the acceptance-criteria level, and two capstone ACs are owned by no child at all. An orchestrator working top-down from these issues would receive conflicting instructions on the *first two features in the capstone's own required order* (PF-1/#567, PF-2/#568). The fix is an editing pass, not new design.

Tree walked: #514 → FEATs #567, #568, #569, #570, #571 (all native sub-issues, all open, matching `requires_features`) → TASKs #820/#821, #822/#823/#824, #825/#826/#827, #787/#788/#789, #828/#829/#831 (all open). `planned_features: []`; no `requires_capstones`/`requires_tasks_exception` arrays present. Cross-referenced: #223, #212, #316, #289, #558, #511, #84, #76, #440, #382, #592, #75, #91, #317, #509, #517, #577 (all open); #69, #47, #51, #200, #201, #222 (closed, cited as history only — none load-bearing as a live dependency).

## 1. Decomposition

**Every PF has a filed, open owner** — PF-1→#567, PF-2→#568, PF-3→referenced-only (#316, correctly excluded from the acceptance path), PF-4→#569, PF-5→#570, PF-6→#571 — and every feature has tasks. Nothing required is closed. But:

- **Capstone AC-7 is unowned.** Neither the independent-rebuild half (a non-maintainer reproduces a tagged release and publishes matching `.buildinfo`/hashes, exercising `docs/reproducibility.md` — the doc exists, verified) nor the merge-rights half (a non-maintainer holds and exercises merge rights on a scoped area) appears in any of the five features or fourteen tasks. All children can close and AC-7 has had zero work done on it.
- **Capstone AC-5 is unowned.** The capstone's PF-2 bullet asserts #568 "also carries the corpus-separation fix: README and CONTRIBUTING link the filtered view (`is:open -label:tier:task ...`)". #568's current (rewritten) body carries no such AC, and no task (#822/#823/#824, #820/#821) implements the filtered-view link. The labels the filter needs do exist (`tier:task`/`tier:feature`/`tier:capstone` verified live), so the work is an hour — but today nobody owns it.
- **#568 and its tasks are double-owned in the destructive sense: they specify two incompatible designs.** #568 AC-1/AC-2 mandate a *mechanically generated* shelf — surviving PIT mutants + circuit-fixture tasks, published as `docs/first-tasks.md` from the weekly `mutation.yml` artifact, with an 8-day staleness check that "replaces any human 'restock below 10' rule". #823 mandates the opposite: four hand-verified recipe families (color literals, GIF→SVG, duplicate image tree, first-test coverage), artifact `docs/first-contributions.md`, and — verbatim — "any mutation-report- or circuit-fixture-derived shelf sourcing" is *out of scope*. #824 ships exactly the human restock-below-10 rule #568 AC-2 says is replaced, and defers automation behind a trip-condition. Both layers are open and current; an implementer cannot satisfy both. (The task-tier version is the better-evidenced one; the feature body appears to be the stale layer.)
- **#567 and #820 contradict on the chooser design.** #567 AC-1: chooser shows *exactly two* entries; `capstone.md`/`feature.md`/`scientific_task.md` move to `docs/plan/templates/` and are unreachable from the chooser. #820 AC-1: planning templates "keep their content and behavior unchanged and keep their place" in the chooser, with numeric prefixes sorting the plain templates first and a renamed `feature.md` entry. Also divergent: #567 AC-3's `jls.BugReport.environment()` + `jls --bug-report` + URL-prefilled form + About "Copy diagnostics" button vs. #820 AC-6's `-bugreport` flag + clipboard copy + `JLSerror` paste; #567 AC-1's contact links (Discussions + SECURITY.md) vs. #820 AC-3's (`/security/advisories/new`). And the AC cross-mapping is stale: #820/#821 claim to implement "#567's AC-1/AC-2/AC-4" and "#567's AC-3 (quickstart)" respectively — but in #567's current body the quickstart is AC-5 and AC-3 is the diagnostics API. #567 was rewritten after its tasks and only partially reconciled (its decomposition note acknowledges #821's rescope but not #820's divergences).
- **#570 AC-2b (ScopePath generalization: nested VCD `$scope`, `-t` targets, trace window) has no task.** #788 explicitly excludes it. As written it is an acceptance criterion of #570 with no executing unit, gated behind #84 ← #440 + #382 (all open, unstarted). If #570 cannot close without AC-2b, PF-5 silently inherits a multi-feature refactor chain the capstone never funded; if it can, the body should say AC-2b is severable.

## 2. Acceptance-criteria composition

- **AC-1** (drive-by bug report) → #567/#820: composes, modulo the §1 contradiction about which design ships.
- **AC-2** (≥10 good-first-issue items continuously for a quarter) → the bar is inconsistent down the chain: capstone ≥10 continuous; #568 AC-1 ≥3; #823 files 15; #824 restocks below 10 → ≥15. #568 as literally written (≥3) can pass while the capstone's ≥10-continuous fails. The #823/#824 pair does compose upward; the feature layer doesn't. Capstone AC-2's citation of "#568's own AC-4 escape hatch" does match current #568 AC-4 (verified).
- **AC-3** (fork CI, zero maintainer action) → #568 AC-3(a/b/c) + #822: composes cleanly. #822's claims verified against the checkout: `ci.yml` triggers are `push: branches: ["master"]` + `pull_request`, so a fork branch push runs nothing today, exactly as stated.
- **AC-4** (three external human PRs, median first-response <48h, rolling quarter) → the *measurement* composes: #829 AC-8 keeps the capstone figure computable from the disposition ledger, and #829 AC-3 imports the capstone's own externality definition verbatim. The outcome itself is a market result no child can guarantee — acceptable for an outreach capstone with KC-30-2 as the escape.
- **AC-5** → unowned (§1). Every child passes, AC-5 fails.
- **AC-6** ("one external element **jar**, built from the published API (#569)… contingent on #212") → **stale against its own delivery path.** #827 (and #569's rewritten body) retargeted the validation to a course-specific element authored as a `SubCircuit` and distributed as a plain `.jls` file through the existing import path — explicitly "no classpath jar… and no dependency on #212", for the good reason that `Element`/`LogicElement` are sealed (verified: `SealedHierarchyTest` exists) and #212 is demand-gated. As the tree stands, #569 closes with a `.jls` walkthrough validated and *no jar ever built*, while capstone AC-6 as worded (jar, contingent-on-#212 retirement clause) is neither satisfied nor retired. AC-6 must be rewritten to the retargeted path (or explicitly kept as a jar ambition that only #212's gate can activate). #569's own title ("backed by a jar built outside the repo") is stale the same way.
- **AC-7** → unowned (§1).
- **KC-30-1**'s discharge-by-construction ("nothing ships pre-emptively, so nothing can ship only to poach") is **invalidated by #570's restructure**: #570 now specifies building both wishlist features outright (#788 2-3 mw, #789 3-5 mw), with no design-and-invite trial anywhere in it. The trial the capstone budgeted PF-5 as (file, sketch, invite; build only if unclaimed in a stated window) exists in no child. #831 AC-2 still points adoption candidates at "#570's Digital-wishlist trial to draw on" — a mechanism that no longer exists in #570.
- **KC-30-2**'s clock condition (CAP-27/#511 shop-window gates live, or a declared ordering edge) is coherent and #511 is open; this is a cross-capstone coupling on the *kill-criterion*, not on start.

## 3. Dependency chains

**Acyclic — verified.** 567→∅; 568→567; 822→∅; 823→{567,822}; 824→823; 569→#223 (open); 825→#223's four already-normative rows (verified present in `docs/extension-points.md`); 826→825; 827→∅; 570→{#84,#76}; 787→#592; 788→787; 789→787(+#76); 571 per-criterion→{567,568,#558}; 828/829→∅ (gated criteria only); 831 AC-3/4/8→{#558,567,568}. No edge points at a closed or redirected issue. Two hygiene items and three real exposures:

- **#569's machine block still lists `#212` in `ordering_after`** while its own body states no criterion is gated on #212 anymore. Stale edge; should be dropped. (The stale-#399 reference the 2026-08-08 review flagged was fixed; #826 AC-5 cites #399 as history only — fine.)
- **#570's implementation is gated on #592 (FEAT-C37-1, CAP-37's parity catalog — open, file `test/jls/ui/ParityCatalogTest` does not exist yet).** #787 cannot start until #592 lands, and #788/#789 order behind #787. So *all* of PF-5's build work sits behind another capstone's feature, an edge declared nowhere in #514's machine block (`ordering_after` names only #223). Not a cycle, but an undeclared cross-capstone critical-path dependency.
- **#789 additionally needs #76's settings dialog** (open, unbuilt) for its AC-3 settings page; #570 AC-2b needs #84 ← {#440, #382} (all open, unstarted). Both are unfunded-by-this-capstone prerequisites on #570's full closure.
- **#571 AC-2 / #831 AC-3/4/8 gate on #558** (FEAT-C29-2, `.dig` importer — open, CAP-29's scope). Reasonable and explicitly recorded, but it means PF-6's broader-outreach half is hostage to another capstone's delivery; only the pilot (#831 AC-1/AC-2) and repair work (#828, #829) are truly startable.
- No external-to-the-project (unfunded third-party) prerequisite exists on the startable path; the genuinely external dependencies (a non-maintainer volunteer for #827 AC-3, reply rates for #831) all carry explicit fallbacks/kill criteria. Good.

## 4. Staleness and gaps

Code-level claims in the task tier verify remarkably well against master (`6744b56`): `SimpleEditor.java` = 5,852 lines and `StateMachineDialog.java` = 1,929 (the capstone's AC-5 replacement rationale is factually right); `DefaultExceptionHandler.java:143/:158` carry the exact "Please attach it to a bug report" strings #820 quotes; `JLSInfo.java:20` `versionString`; `JLSStart.java:2074` Help menu; `CircuitRenderer.of` at `:68` / `draw(..., @Nullable SimpleEditor)` at `:86`; `InteractiveSimulator.findTraces` at `:973`; `docs/extension-points.md`'s four typed-now rows (`ElementType`, `PaletteEntry`, `HdlEmitter`, `OpSink`) and `ExtensionPointCatalogTest.docTableAndConstantsAgreeBothWays` (`test/jls/ExtensionPointCatalogTest.java:165`); `docs/reproducibility.md`, `mutation.yml`, `About.java` all present; `good first issue` label exists with 0 open items, as #824 records. Exceptions and contradictions:

- **`.github/ISSUE_TEMPLATE/` holds only `scientific_task.md`.** Both #567 AC-1 and #820's "Context (verified 2026-08-15)" assert three planning templates (`capstone.md`, `feature.md`, `scientific_task.md`) sit in the chooser; `capstone.md`/`feature.md` do not exist there (and `docs/plan/templates/` does not exist). The instruction "move all three to `docs/plan/templates/`" cannot execute as written. Small, but it means the "verified" stamp on #820 is not fully trustworthy.
- **Cost bands are contradicted by scope, twice over.** Capstone `band_mw: 4.5-8`; its children's own bands sum to **9-16 mw** (#567 "1-2" + #568 "2-3" + #569 "1.5-3" + #570 "4-7" + #571 "0.5-1+ongoing"). The single largest divergence is PF-5: the capstone prices it "0.5-1 mw (narrowed to a design-and-invite trial)" while #570 prices itself 4-7 mw as a full build — the narrowing was reversed at the feature tier without the capstone body or band following. Second-order: #820+#821's Related sections claim their 0.25-0.5 each "sums to #567's stated 0.5-1 mw band" — #567's band is now "1-2"; and #571's tasks sum to ~1.15-2.0 mw against #571's "0.5-1".
- **Capstone body carries dangling references to children's dropped scope:** `related:` cites "#289 (dark mode, consumed by #570's AC-1)" — #570 dropped dark mode entirely and has no AC-1; the PF-6 bullet gates by-name invitations on "#316's decomposition" — #571/#831 re-gated onto #558 and record on the tree that #316 was never the real gate; the PF-6 bullet promises publishing "a <48h first-response … record" — #571's entire reframing is that no response-latency SLO is published (capstone AC-4's median stays computable from #829's ledger, so the AC survives; the prose doesn't).
- **Open questions that block start:** none formally open, but the #567↔#820 and #568↔#823/#824 contradictions function as open questions — each pair needs a one-line adjudication of which layer is authoritative before an implementer touches it.

## Verdict and the fix list

**not-ready.** All blocking items are reconciliation edits, roughly a day of tracker work, no new design:

1. **#514:** rewrite AC-6 to the `.jls`/`SubCircuit` walkthrough path (keeping or explicitly retiring the jar-contingent-on-#212 clause); assign AC-5's filtered-view link to a named child (fits #820 or #823); either file a small task for AC-7's two halves or move AC-7 behind an owner; fix the PF-5 bullet + band (or re-narrow #570); delete/repair the stale `related:`/PF-6 references ("#570's AC-1", #316 gate, "<48h record"); consider declaring the #592 (CAP-37) ordering edge the #570 chain actually has.
2. **#568:** rewrite AC-1/AC-2 to match #823/#824's family-recipe + phased-automation design (or formally overrule the tasks); align the shelf count with capstone AC-2's ≥10-continuous; add or reassign the README filtered-view AC.
3. **#567 / #820:** adjudicate chooser design (exactly-two vs. alongside), diagnostics surface (`--bug-report`+prefill+About vs. `-bugreport`+clipboard), contact links; renumber the tasks' AC cross-references to #567's current body; reconcile the band; correct the nonexistent `capstone.md`/`feature.md` premise.
4. **#569:** retitle (no jar); drop #212 from `ordering_after`; update the Decomposition/Ordering rows its own note already flags as stale against #827's retarget.
5. **#570:** resolve AC-2a's internal contradiction (existing tab in view mode vs. its own prose's and #788's new `CircuitInspector`, never a tab — #788's version is the load-bearing one); state whether AC-2b is severable from #570's closure; reconcile with the capstone's PF-5 trial framing or have the capstone adopt the build framing.

What is genuinely startable today, independent of the fixes: #822 (fork CI job), #825/#826 (catalog publication + stability labels; only #223's already-normative rows gate them), #828 AC-1..AC-4 (the bounced-author repair and the two policy paragraphs), #829, and #831 AC-1/AC-2 (pilot + catalogue). None of these is on the contradicted surfaces.
