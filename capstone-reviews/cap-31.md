# CAP-31 (#515) readiness review — 2026-08-16

**Verdict: ready-with-gaps** — implementation can start now on three independent, unblocked entry points (#648, #566 slice A, #657, and the #468→#872 prerequisite chain), but five named gaps need capstone-level disposition before any feature is *closed*, and the cost band materially understates the summed task bands.

Tree walked: CAP #515 → FEATs #563/#564/#565/#566 (all open, all natively linked) → TASKs #641/#642/#644/#646/#872 (under #563), #648/#649/#650/#651 (under #564), #652–#656 (under #565), #657–#660 (under #566). Shared/external nodes fetched: #306 (CAP-09), #62, #290, #336, #468, #510, #322, #524/#686, #180, #315. Load-bearing code claims checked against the repo (local master 6744b56, spot-checked against GitHub master c5cee1ba where the local clone is behind).

---

## 1. Decomposition

**Sound overall.** All four planned features are filed, open, and natively parented; every feature's tasks are filed, open, and natively linked (including #565's five tasks — its body's own note that "get_sub_issues on #565 currently returns none" is now stale; the links exist). PF-1's shared extractor is filed (#872), correctly deduplicated with CAP-09, and honestly gated on #468. Ownership seams (#563 owns propagation not detection; #564 owns the minimizer engine; #565 owns Explode only; #566 slices A→B→C) are explicit and non-overlapping. Verified against code: every "current state" claim I checked is accurate at cited line numbers (`TruthTable.react` no-match no-op and don't-care→0 at ~1432/1447; `addInput` `TellUser.error` at ~617; `StateMachine.canCopy()` false at ~460; `State.trans` `HashSet` at :31; `HdlExporter.buildStateMachine` 866–1054; `BatchSimulator.findWatched` :220; `NetlistImporter.Builder` :410; `LayoutGraph`/`SchematicLayouter`/`LayoutInvariants`/`LayoutMetrics` all present).

**Defects:**

- **D-1 (ownership metadata conflict on #872).** #872 is a *native* sub-issue of #563, its own YAML says `part_of_feature: 336` (re-homed to FEAT-004), and #306's body still says it is "homed under #563, `shared_with: [306]`". Three descriptions, one component. Tooling walking the native graph will charge #872 to FEAT-C31-1; tooling reading YAML will charge it to FEAT-004. Pick one (native re-parent to #336, or revert the YAML) and fix #306's stale sentence.
- **D-2 (shared-entry-point ownership ambiguity, #62 vs #654).** #565's boundary notes say promoting `NetlistImporter.Builder` to a shared generated-netlist entry point "is guidance for TASK-C565-2/3's implementation"; #654 AC-1 says the opposite — "if #62's entry point is not yet landed when this task starts, this task is blocked, not free to build a local substitute" (`blocked_by: [62]`). Today `Builder` is a private class and #62 is open with no filed task for the promotion. Either #62 grows a task that owns the promotion, or #654's blocked_by is wrong and #653/#654 own it. Currently the work is double-described and zero-owned.
- **D-3 (CAP machine block is schema-nonconforming).** CAP-09 (#306) carries the full capstone block (`tier`, `evidence_commit`, `requires_features`, `requires_capstones`, `requires_tasks_exception`, `blocked_by`, `blocks`, `related`). CAP-31's block has none of these arrays — only `cap_id`/`evidence`/`planned_features`/`ordering_after`/`band_mw`/`demo_slice`. Any orchestrator that walks `requires_features` sees CAP-31 as requiring nothing; the real cross-capstone edges (#872 via CAP-09, #62) live only in prose strings inside `ordering_after`.

## 2. Acceptance-criteria composition

Most CAP ACs compose cleanly downward: AC-1 → #641 AC-1 + #648 AC-1; AC-3 → #642/#649/#652 with an explicit shared-catalogue reconciliation mechanism in #649 (a stated reason the walls differ — satisfies AC-3's "reconciled in writing"); AC-4 → #566 AC-5/AC-6 + #657/#658/#659; AC-6 → #872 AC-2's registry-keyed classification (including the `SubCircuit` flattening path); KC-31-1 and KC-31-2 are faithfully propagated (sealed `Minimization` type in #649 AC-3, test-evidence closure rules in #658). Four places where every child can pass while the capstone fails:

- **A-1 (CAP AC-7 is unowned — hazard-free cover).** AC-7 requires PF-2 to offer a hazard-free (consensus-closed) cover as a second selection *or refuse it by name with a specific successor issue*. No child owns either half: #564's ACs, #648, #649, #650, #651 contain no hazard/consensus-cover obligation at all (the only "consensus" mention in the subtree is a cost counter in #564 AC-4). The prime-implicant-cover half is only partially owned: #648 AC-4 retains the PI set/chart in the structured value, but no AC makes it a displayed/exported first-class output. Fix: add the AC to #564 (one bullet) or file the successor issue AC-7 demands, before #564 is closeable.
- **A-2 (CAP AC-2's `StateMachine` half is unsatisfiable by the current tree).** AC-2 reads "a `TruthTable` **(or `StateMachine`)** Expands to a drawn circuit whose Collapse reproduces the original table." Collapsing an exploded register-and-gate circuit back to a `StateMachine` requires sequential analysis that no issue owns — and #872 AC-2 *refuses* state-holding elements by design, so the tree architecturally excludes it. #566 AC-5 delivers trace-equivalence (CAP AC-4(a)), not collapse-identity. The parenthetical should be struck from AC-2 (FSM round-trip is covered by AC-4's golden-identical simulation) or a new feature must be filed. As written, CAP-31 cannot close.
- **A-3 (CAP AC-5's "one analysis verb" is undecided and drifting three ways).** AC-5 demands *one* headless verb with structured JSON — "not three or four independent ad hoc flags." The children currently specify four flags (`-truthtable` #646, minimize #651, `-synth` #656/#565 AC-5, `-fsm` #660), with three mutually inconsistent resolution stories: #646's body says whichever flag lands first settles the verb-family shape "in writing here"; #660 says the one-verb-vs-per-flag question "is a CAP-31-level design decision (#515), not decided here"; #651 cites a "#646 2026-08-04 coordination comment" that **does not exist** (#646 has zero comments; the content was folded into its body) and asserts a "single shared analysis-document schema" that #646's actual body contradicts (#646's canonical output is a `.jls` file plus optional CSV — not JSON, not a shared analysis document). Additionally #565 AC-5's `-synth out.jls table.jls` takes two operands, violating the one-operand-per-flag `FlagSpec` model #646 pins. Non-blocking for engine work, but the decision must be made at #515 before the *first* headless flag lands, and #651's phantom-comment citation should be repointed at #646's body section.
- **A-4 (output don't-care handling: CAP scope boundary contradicts the children).** CAP-31's scope boundary says a table with an unresolved output don't-care "is refused by name … never silently zeroed, and never fed to Quine–McCluskey," and AC-2 repeats the refusal. The children instead *process* them: #648 AC-2's default mode fixes output don't-cares at 0 (documented, matching `TruthTable.react`) plus an explicit exploit mode; #653 AC-5 resolves them via #564's minimized form; #655 AC-1 asserts refinement (`extracted ⊒ source`), not identity, on don't-care cells. The children's design is arguably the better one (it matches shipped simulation semantics and is not *silent*), but as filed the CAP text and the child ACs disagree about whether such a table is refused or resolved. One paragraph of CAP-body reconciliation needed; otherwise AC-2's refusal clause fails against a conformant implementation of #648.

## 3. Dependency chains

**Real and acyclic**, with three annotations:

- **Critical path to the demo slice:** `#468 (TASK-0007, open, net-partition unification) → #872 (open, 2–3 mw, blocked_by [468] hard) → #641 → #642 → #644/#646`. Neither prerequisite has landed; #468 is nontrivial (seven partition implementations across two regimes). The CAP's "demo slice … 2–3 mw" claim is the *slice on top of* this chain, as #563 honestly discloses — but time-to-first-demo is realistically ~2× the stated slice even if #468 is charged to CAP-09/FEAT-004. Not blocked in the verdict sense because #564 (via #648, `ordering_after: []`, satisfiable against hand-entered tables at HEAD), #566 slice A (determinism/`canCopy` fixes), and #657 (parity document) are all startable today with no unlanded prerequisite.
- **#654 `blocked_by: [62]` is coarser than the true dependency.** #62 is open (the `HeuristicLayeredLayouter`/`LayoutGraph` seam landed via PR #194/#196; the `-layout` flag and Auto-arrange have not — verified absent from `JLSStart.FLAGS` on GitHub master). What #654 actually needs is the shared public realizer (see D-2), not #62's whole remaining scope. As written, a literal scheduler holds #654 until #62 closes entirely.
- **#660 `ordering_after: [566]` — a task ordered after its own parent feature.** Formally circular (a feature closes when its tasks close). The intent ("after #566's rewrite pinned AC-3") is historical, already satisfied; the edge should be deleted or repointed. No other cycles found; #659→#658→#657→#510 (open survey, content exists) and all cross-feature edges (#650→#644, #651→#646, #652→#641, #653→#648, #655→{#653,#641}) are real and forward-only.
- **No closed/redirected endpoints** on any load-bearing edge. #180 (closed) is cited only as the canonical-order precedent — correct usage. #290 (open) is handled honestly everywhere it gates a claim (refuse-by-name until its corpus lands: #565 AC-4c, #566 AC-5).

## 4. Staleness and cost bands

- **S-1 (wrong issue number, ×2):** #564 and #648 cite "the JEDEC/GAL path (#83)" — #83 is a *closed pull request* about release-tag workflows. The JEDEC/GAL material lives in `docs/standards-adoption/11-costed-rejections.md:578-582` (verified); no issue for it was found. Repoint or drop the number.
- **S-2:** CAP body calls #322 "unfiled"; #322 is filed and open (FEAT-026, four-state values). Minor, favorable direction.
- **S-3 (tracked edits not yet made):** (a) CAP body says #306's Open Questions §2/§6 "should list #515 as a third consumer" of the exit-status lattice — #306 mentions CAP-31 only in the extractor note; the edit is still pending. (b) #641 AC-4 records "a required edit on #642" (bound measured against *expanded bit count*, not raw signal count) — "not yet applied there as of this writing," and indeed #642's ACs still say input count `N` without the expanded-bit clause.
- **S-4:** #651's citation of a nonexistent "#646 2026-08-04 coordination comment" (see A-3). #565's "tasks are not yet linked as sub-issues" note (now false). #565's body task list omits its own fifth task #656.
- **S-5 (cost bands contradicted by task sums).** CAP band 8–13 mw equals the sum of the four *feature* bands (2-3 + 2-3 + 2-4 + 2-3), but the filed task bands sum higher: #563 tasks 4 mw (excl. #872), #564 tasks 4 mw, #565 tasks 5 mw, #566 tasks 4–6 mw ⇒ **17–19 mw**, plus #872 (2–3 mw, raised from 1–2 when re-scoped) and #468 (unbanded) on the PF-1 path. #566's own body records the visionary review's finding that its 2–3 mw "does not cover its full scope," with the re-file-as-three-features decision explicitly parked on CAP-31's owner. The band should be re-stated (or the parked decisions taken) before the band is used for scheduling; the scope itself is coherent.
- **Doc citations all resolve** on GitHub master c5cee1ba (`docs/capability-roadmap/README.md:449-453` hazard/consensus lesson; `11-costed-rejections.md:455-460` QM-not-Espresso; `docs/batch-interface.md`, `docs/file-format.md`, `test/jls/elem/TruthTableModelTest.java` all present). Note the local working copy at 6744b56 predates the roadmap/standards docs entirely.

---

## Verdict: **ready-with-gaps**

Start now on the unblocked fronts (#648 minimizer engine; #566 slice A determinism/`canCopy` fixes; #657 parity document; #468→#872 for the PF-1 chain). Before any feature closes, the CAP owner must: (1) strike or re-home AC-2's `StateMachine` parenthetical (A-2); (2) give AC-7's hazard-free cover an owner or its successor issue (A-1); (3) decide the one-verb-vs-four-flags shape AC-5 demands and reconcile #646/#651/#660's three stories (A-3); (4) reconcile the don't-care refusal clause with #648's documented default-resolve design (A-4); (5) fix the metadata defects (D-1 #872 triple-homing, D-2 layout-entry-point ownership, D-3 missing `requires_*` arrays, #660's self-edge) and restate the band (S-5).
