# CAP-23 (#504) readiness review — 2026-08-16

**Verdict: ready-with-gaps.** The tree is complete end-to-end (6 FEATs, 15 TASKs, all open, acyclic, no edge at a closed issue), the children were genuinely rescoped in the 2026-08-08/-12 rewrite passes to the journal-first design the capstone demands, and multiple threads (#678, #476, #529, #698) are startable today. What has not happened is the reconciliation the capstone's own §5 protocol requires: #504's machine block, cost table, and mermaid graph still describe the pre-rewrite world, one hard prerequisite (P1-S0) named in `blocked_by` is still not filed, and two child scopes (#696 canvas elements, #535 re-sim fallback) contradict the capstone body as written. Every gap below is a REPLAN-comment or single-issue-filing fix, not a re-decomposition.

Tree walked: #504 → #527 (#678, #680, #682), #529 (#684, #685), #532 (#688, #689, #691), #533 (#693, #695), #534 (#696, #698, #700), #535 (#702, #704). `requires_features: [533]` (native child), `requires_capstones: []`, `requires_tasks_exception: []`. Code claims verified against remote `master` (`c5cee1b`, 2026-08-05) via API and the evidence commit locally; note the local clone at `/home/anadon/Documents/code/JLS` is stale (July 28, `6744b56`) and lacks `docs/capability-roadmap/` entirely — pull before orchestration.

## 1. Decomposition

**Filed and open, composing to the outcome — with three ownership defects.**

- Every §1 required step has an owner: step 1 → #534/#696/#700 + #527/#680; step 2 → #529/#684 + #532/#688/#689/#691; step 3 → #533/#693/#695; step 4 → #535/#702; step 6 → #688 (`--why`) + #534/#698 (recorder parity). No child is closed; no task is claimed by two features (`part_of_feature` is unique throughout).
- **PF-0/PF-0b double-owned on paper, single-owned in fact.** #688 (TASK-C532-1) *is* the site-indexed `NetChange`/`causeIndex` journal *and* the headless `jls -b --why` printer. #504's roster still says PF-0 "REPLAN needed to formally point... at #688" (never posted — the only comment on #504 is the 2026-08-04 coverage pass, pre-rewrite) and PF-0b "Not yet filed", which is now false. An orchestrator reading the machine block sees `requires_features: [533]` plus five unresolved planned entries; the sub-issue hierarchy is the truth and the block is not.
- **P1-S0 is still unfiled.** #504 `blocked_by` names replacing the `static long SimEvent.sequence` (verified at master `SimEvent.java:87,119,143-146`) as a hard prerequisite before PF-0's `causeIndex` ordering. It is filed nowhere (searched `P1-S0`, `per-Simulator`, `SimEvent.sequence`). Worse, #476 (TASK-0063) — which #688 orders itself *after* — explicitly **pins** the counter as process-wide static (its P9/O4 invariants: "must not reset `SimEvent.sequence`", "this task does not change that... clean follow-up once `compareTo` is retired"). So the prerequisite is not merely unfiled; the one filed task touching that code contractually defers it. #535's beyond-horizon fallback names the same landmine. File P1-S0 (or post the "single-Simulator build" REPLAN waiver §5 allows) before #688 starts.
- **#533's `blocked_by` names two unfiled prerequisites.** (a) On-canvas signal-value rendering (Wire has no draw path consulting `WireNet.value`) — still unfiled anywhere, and load-bearing for §1 step 3's overlay; a genuinely missing issue. (b) The hazard-demo fixture — stale: #534 AC-2 now authors it as in-scope work, so this entry should re-point at #534, not "unfiled".
- **#704 (TASK-C535-2, external-viewer export) sits inside the native tree while stating it is "not currently funded by any capstone".** §1 step 5 is explicitly out of scope, so an unfunded task as a native child of #535 means the capstone cannot close over its tree without either moving #704 out or a `WAIVED:` comment. Similarly, #688's follow-on TASK-C532-1b (`checkpoint`/`restore`/`runUntil` replay) is deliberately unfiled and tracked only in prose in #688/#532 — acceptable, but it is invisible to any tooling that walks issues.

## 2. Acceptance-criteria composition

**AC-2/AC-3/AC-4/AC-5 compose; AC-1 has no owner; step 4's wording conflicts with its own child.**

- AC-2 (`WhyArtifactGoldenTest`) ≈ #688 AC-5 (byte-identical `--why` across runs, seeded fixture). AC-5 (`InstrumentParityTest`) ≈ #534 AC-3 / #698's `InstrumentGoldenTest` (interactive VCD == batch golden, byte for byte). AC-3 is extended by #532 AC-3 (`CauseChainCompletenessTest`, adds the completeness half) — though the *base* `JournalCausalityInvariantTest` is named only in #504 and #532; neither #688 nor #691 claims writing it. Minor: assign it explicitly (natural home: #688 or #691).
- **AC-1 (`HazardDiagnosisWalkthroughTest`) is unowned.** No child issue names it (search hits only #504 and #529 — and #529 AC-3 *disclaims* it as coverage). The Layer-2/3 GUI-harness extension it depends on (waveform-click, canvas-flash, docked-panel assertions — risk 6) is "not yet priced" per #504's own cost table and has no issue. Every child could pass its own headless/model-level ACs while the capstone's integrated three-click walkthrough has neither a test nor a harness to run it in. File the harness-extension task and give AC-1 an owner (the natural seat is a CAP-level integration task or #700's exit).
- **Step 4 / AC-4 vs #535: a child can pass while the capstone text fails.** #504 §1 step 4 promises "no re-simulation... and no progress bar", and PF-6a's roster entry says re-simulation is "used only as a test oracle, never as the shipped mechanism". #535 (post-rewrite) and #702 *ship* truncated re-simulation as the beyond-retention-horizon fallback (`Trace.MAX_RETAINED_CHANGES = 100_000`), with #535 AC-3 requiring a progress display on that path. The reconciliation is genuinely sensible (projection is the default; O(T) only beyond horizon), but #504's step-4/AC-4 wording must be amended by REPLAN or the walkthrough transcript at close will contradict the body.
- **Two state-at-T stores vs risk 1's one-model rule.** #535/#702 reconstruct canvas state from #527's `Trace.Change`/`Recording` transition log; #504 PF-6a says "reconstructed from the journal (`firstChangeAtOrBefore` per visible site)". The *causal* model is one (#688's journal — good), but state-at-T now has two candidate substrates with different retention policies (append-only journal vs 100K-bounded trace log). Decide by REPLAN which one PF-6a reads, or record that they are the same store; drift here is exactly what risk 1 exists to prevent.
- **PF-5 scope contradiction.** #504's cost table says PF-5 has "No new canvas elements, no save-format change" and its Open Questions record "the analyzer and word generator are not canvas elements — resolved by design". Its cited subtask #696 delivers **three new `react()` canvas elements** (`Trigger`, `EdgeDetect`, `PulseWidth`) that "round-trip with the circuit like any other element" — i.e., new element types in the `.jls` format. The rescope from an analyzer *appliance* to thin markers over #534's predicate service is defensible, but the capstone body and its child currently assert opposite facts; one REPLAN must pick the story.

## 3. Dependency chains

**Real and acyclic; no edge points at a closed issue; no external (outside-repo) prerequisite.**

- Verified graph: P1-S0(unfiled) → #688; #678 → #688; #476 → #688 → #689 → #691; #684 → #685, #684 → #689; #529 → #532 (feature level); #680 → #700, #680 → #702; #696/#698 → #700; #693 → #695; #527 → #533, #527 → #535. No cycles. #216 (closed) is reference-only, correctly annotated.
- **The mermaid edge `PF5 --> PF123` is backwards relative to the filed tasks.** #527/#529 declare `ordering_after: []`; the actual filed dependency is the reverse — #700 (PF-5's chronogram-opening exit) orders after #680 (PF-1's panel). Regenerate the graph at the roster REPLAN as the body itself instructs.
- **#476 (TASK-0063) is the serial gate on the whole causal core.** #688 hard-orders after it (its AC-6 makes landing-order a gate), and #688 gates #689 → #691 → AC-2/AC-3. #476 is open, large, and outside CAP-23's tree; if it stalls, CAP-23's journal stalls with it. It is a funded, in-repo task — not a `blocked` verdict — but the orchestrator should treat #476 + #678 + P1-S0 as CAP-23's true critical path head.
- Consumed-if-present references (#367 physical units, #373 probe names, #405 streaming dump) are all open and correctly non-required.

## 4. Staleness and other gaps

- **Evidence commit is a branch checkpoint, not a master commit.** `evidence_commit: 646d5ae8` is "Checkpoint: transient fixer working file (to be swept in final cleanup)" on the rewrite branch (`claude/github-issue-review-agents-j99xga`), not an ancestor of master. The DoD requires every permalink to resolve on the default branch; a commit slated to be "swept" is the wrong anchor. The *claims* do resolve at current master `c5cee1b` (spot-verified: `SimEvent.java:87/119/143-146`; `WireNet.propagate` winning-driver hold and `value = actual` at `src/jls/elem/WireNet.java` ~463-484 — note the file is in `jls.elem`, not `jls.sim` as risk 2's citation implies; `Trace.firstChangeAtOrBefore` at 445-458; `Traces` at `InteractiveSimulator.java:1102`; the `BatchSimulator.afterEvent` gate at 144-145; `docs/capability-roadmap/lf-03-causal-debug.md` incl. the "per-event journalling... is wrong" finding; `docs/batch-interface.md`). Re-pin `evidence_commit` to a master SHA at the roster REPLAN.
- **Cost bands are contradicted by the filed children, in both directions.** #504's PF-1/2/3 row says 3-4 mw; the filed trio is #527 (2-3) + #529 (2-3) + #532 (4-6) = 8-12 mw. The PF-0 row says 4-6 mw; the rescoped #688 is 1-1.5 mw (with 1b deferred). #527's own 2-3 band is exceeded by its children's sum (#678 1 + #680 2-3 + #682 1 = 4-5). The "useful floor: 6-8 mw" is therefore no longer derivable from the tree, and the §2 funding gap against #508's ~3-4 mw wedge remains unreconciled — #504 itself only requires that reconciliation "before work past the useful floor is scheduled", so it does not block start, but the floor number itself needs re-deriving first.
- Stale cross-references inside children (harmless but should die at the same REPLAN): #532's `blocked_by` says "PF-0... not yet filed" while its own child #688 is PF-0; #529/#532 say "PF-0's causality journal (not yet filed)"; #527's boundary notes still describe #534 as "drawable canvas elements with their own... pre-trigger buffer" (pre-rewrite, checked 2026-08-04).
- Open questions: OQ1 (FST) is resolved-by-stance (VCD only, enforced by #534 AC-4); OQ2 (viewer-sync protocol) is correctly deferred with #535's rewrite dropping it. Neither blocks start.

## What must happen, in order

1. File P1-S0 (or post the single-Simulator waiver REPLAN) — before #688 is picked up. *(gap, pre-PF-0)*
2. File the on-canvas signal-value-rendering prerequisite #533 names — before #533/#695. *(gap, pre-PF-4)*
3. File the Layer-2/3 GUI-harness extension task and assign AC-1 an owner. *(gap, pre-close; pre-#689 ideally)*
4. One roster REPLAN on #504: point PF-0/PF-0b at #688, PF-1..PF-6a at #527/#529/#532/#533/#534/#535 as rescoped; fix the PF-5 canvas-element and step-4 re-sim-fallback contradictions; re-derive the cost floor and reconcile #508; regenerate the mermaid; re-pin `evidence_commit` to a master SHA; disposition #704 (move out or pre-record its waiver).
5. Startable immediately, in parallel, without any of the above: #678, #476, #684 (#529 path), #698's recorder unification.

None of these change the decomposition's shape; the verdict is **ready-with-gaps**.
