# CAP-26 (#507) readiness review — 2026-08-16

**Verdict: ready-with-gaps** — start now with the funding order as written; five named gaps below, all resolvable by filing/REPLAN actions rather than re-decomposition. No missing thinking was found: every gap identified here is one the capstone tree itself already names somewhere; what remains is executing the named resolutions.

Tree walked: #507 → FEATs #542, #543, #544, #546, #547, #549 → TASKs #729, #731, #734, #876, #877, #737, #739, #741, #743, #745, #747, #749, #750, #753, #754, #756, #758; shared/external nodes #380, #355, #316, #101, #75, #536, #540, #505. Code claims checked against evidence commit `646d5ae` (verified on GitHub master, 16 commits behind the 2026-08-05 tip `c5cee1b`; all checked citations below still resolve).

---

## 1. Decomposition

**All six `requires_features` (#542, #543, #544, #546, #547, #549) are filed, open, and non-overlapping in stated ownership; each FEAT is fully decomposed into filed, open TASKs.** No required child is closed. Sub-issue hierarchy matches the machine block exactly.

**Gap D1 — capstone ACs 7 and 8 have no owning issue (PF-7, PF-8 unfiled).** Searched: no open issue owns the Access Bridge installer fix (`jdk.accessibility` + `accessibility.properties`) or the help `alt`/`lang` + `Trace.java` keyboard sweep; only CAP-26-tree issues reference them. The capstone's own §1 Step 0 makes these *prerequisites checked first*, its funding order puts PF-7 at position 1, and its DoD requires `planned_features` empty. Both gaps are verified real at `646d5ae`: `scripts/build-installer.sh:145` uses `jdeps --print-module-deps` (static scan; can never select the runtime-only `jdk.accessibility`; no mention of `accessibility.properties` anywhere in the script); `resources/help/**` has 83 HTML pages, 10 `<img>` elements, zero `alt` attributes, zero `lang` declarations; `src/jls/edit/Trace.java` is 626 lines, `extends JPanel implements MouseListener, MouseMotionListener`, no key bindings. **Action: file PF-7 and PF-8 (specs are already written in #507's `planned_features` block) before or at orchestration start.** Until filed, AC-7 and AC-8 are unowned and #547/#753's bridge-dependent rows have named but unowned findings.

**Gap D2 — #731 (TASK-C542-2) is superseded-but-open with acceptance criteria that contradict its parent.** #542 (rewritten 2026-08-12) declares #731 "superseded prose, not a second checklist" and carries the scope as its own AC-2/AC-4/AC-5; #731's own disposition (2026-08-15) recommends "close #731 as absorbed by #542"; #734 calls the #731 reference "stale." Yet #731 remains an open native sub-issue of #542, and its AC-1 specifies a **three-state** vocabulary (driven-0/driven-1/HiZ, "no 'error'/conflict state to encode... no separate 'bus value' state," citing `docs/simulation-semantics.md` §9) while #542's Outcome and #734 specify **five** (`HIGH, LOW, FLOATING, BUS(value,width), CONFLICT`) and the capstone's §1 step 1 requires bus values and error distinguishable. (§9 verified at `646d5ae`: "There is no wired-AND/OR and no conflict (X) state" — conflicts resolve to a deterministic winner reported via `TellUser`. So #731's reading of *current simulator semantics* is correct; #542 deliberately elevates conflict to a first-time rendered state. That is a design decision #542 owns — but two open sibling checklists disagreeing on the state alphabet is exactly what an orchestrated implementer will trip on.) **Action: execute the recommended close-as-absorbed on #731 (or rewrite its ACs to the 5-state vocabulary), and re-point #543's `ordering_after: [731]` and #877's `[..., 731]` at #542.** This sits in the demo-slice band funded first, so it should be resolved before PF-1 work is dispatched.

**Gap D3 — the shared CVD-transform primitive is double-owned.** #542 AC-4 builds and owns `jls.core.ColorVision` ("this issue builds and owns ColorVision... #543 wraps that same instance"); #876 AC-1 builds and owns `jls.core.CvdSimulation` ("`ThemeTest`'s existing private matrices are deleted in favour of calling this type"). Both are load-bearing acceptance criteria on the same class under two names. To its credit the corpus flags this everywhere (#507 Open Question 5 residual: "Open — names a maintainer decision needed before PF-1 and PF-2 both close"; #876's boundary notes flag it "rather than silently decided"). It does not block start — but it is a real double-ownership that must be adjudicated by REPLAN before either #542 AC-4 or #876 AC-1 is implemented, or the tree gets two tritanopia transforms, the outcome both issues exist to prevent.

No double-ownership elsewhere: #542/#76, #544/#75, #544/#380, #546/#540, #547/#549, #549/#75 boundaries are all explicitly and correctly cut, verified against both sides.

## 2. Acceptance-criteria composition

Mapping capstone §4 down: **AC-1** → #542 AC-4 (headless five-condition fingerprint; creates the adder-lab fixture it needs — correctly noted as not existing today, verified: `test/fixtures/` has no adder circuit) + #734 (injectivity on drawn output + per-state contrast floors + mutation-proven red run) + #543 AC-2 (contact-sheet verdict; explicitly claims `CvdStateDistinguishabilityTest`). **AC-2** → #743 (byte-exact golden tree, `@Tag("display")` lane) + #745 (manifest-generated manual checklist) + #753 AC-4 (rows above Not Evaluated gated on `at-session`). **AC-3** → #750 (one command, provenance manifest) + #749 (tactile lint, BANA by cited edition, profile-as-data) + #747 (narrative; halves tracked separately). **AC-4** → #753 (implements `VpatCoverageTest` with totality/ceiling/staleness/falsifier) + #754 (rendered document). **AC-5** → #756 (permanent seeded fixture, `assertThrows` assert-the-assertion — matches the capstone's falsification requirement verbatim) + #542 AC-5 (K9 as CLASSIC byte-identity — `ThemeTest.classicReproducesTheLegacyPalette` verified present — plus tolerant golden ratchet on DEFAULT). **AC-6** → #746/#750 + #747/#749's glyph-free structural determinism. **AC-7/AC-8** → unowned (Gap D1). The children do compose upward: I found no path where all children pass and the capstone fails, *given* PF-7/PF-8 filed.

**Gap A1 — parent/child disagreement on the spike's apparatus (#544 vs #737).** #544's decomposition note says #737 "still frames this as a scripted session captured 'in the #101 rig'... needs rewording to a manual, recorded pass," and #544's Funding gate says the spike "does not require or fund building that harness." #737's current body (rewritten 2026-08-15) is neither of the things #544 describes: it is a two-tier design whose AC-1 *does* add a headless `pyatspi` AT-SPI2 listener to the #101 rig (bounded by its AC-3: no Orca, no speech-dispatcher in CI) plus a hand-run Orca session (AC-2). The substance agrees (Orca never in CI), but the first task on PF-3's critical path currently has its parent instructing an edit that has already been overtaken by a different resolution. One-line REPLAN on #544 acknowledging #737's two-tier shape (or vice versa) resolves it.

Minor: the enforcement test is named `AcrEvidenceTest` in #547 and `VpatCoverageTest` in #753 and capstone AC-4 — same gate, two names; harmless but should be unified when #753 lands.

## 3. Dependency chains

Full edge set walked (`ordering_after`/`blocked_by` across all 20 tree issues): **acyclic**. Chain: {#729→#731}, #542→{#734, #543, #546, #747, #749, #811}, #876→#877, #756(→#210 closed=satisfied)→{#758, #737}→{#739, #741}→#743→#745, {#741,#745,#753}→#754, #316→#380→{#739, #747}, {#540→#536}→{#546, #749}, #749→#750. The only edge at a closed issue is #756's `ordering_after: [210]` — #210 is closed-as-landed, i.e. satisfied, correct. No edge points at a redirected/refuted issue.

**Gap C1 — PF-4's hard dependency on CAP-24 is understated at the capstone level, and the capstone body contradicts the feature's design.** #546 declares `ordering_after: [FEAT-C26-1, FEAT-C24-1]` and #749 likewise: the tactile SVG is filed as a *profile over #536's device-independent sink*, and #536 (4-6 mw) is itself `ordering_after: [FEAT-C24-5]` (#540) — both CAP-24 (#505) features. Yet #507's machine block says `requires_capstones: []`, `blocked_by: []`, and its §3.4 treats CAP-24 as conditional ("**if** CAP-24 proceeds, the two share one theming seam"), while §1 step 3 describes the tactile SVG as "a render profile of the **existing** `CircuitRenderer`/JFreeSVG output" — which is not what #546 filed. As filed, roughly 8-12 mw of CAP-24 work (#540+#536) sits on CAP-26 PF-4's critical path without appearing in CAP-26's machine block. Needs a REPLAN choosing one of: (a) admit the edge (`requires_capstones`-adjacent note + fix §1 step 3), or (b) re-cut #546/#749 onto the existing JFreeSVG path as the capstone body assumed. PF-4 is mid-order in funding, so this does not block start — but it must be decided before the PF-4 band is dispatched.

**Gap C2 — the outline model is "landing," not landed, and has its own external prerequisite.** The capstone abstract says #380 "is landing"; #380 is open and `blocked_by: [316]` (FEAT-008 residual, open, outside CAP-26; owns the UI harness/coverage floor). So PF-3's true chain is #316→#380→#739→#743→#745 and PF-4's narrative also consumes #380 (#747 `blocked_by: [380, 542]`). Neither #380 nor #316 is in CAP-26's `requires_features` (correctly, per the shared-feature convention — #544 carries the binding edge), but the orchestrator should treat #316/#380 as funded work on this capstone's critical path, not as background that will simply arrive. All nodes are filed and open; nothing is dangling.

External-authority prerequisites: #749/#546 AC-4 require a BANA licensing/permissions check before committing BANA-derived numeric values — a small, named, external gate on PF-4; and the `at-session` evidence class in #547/#753 requires manual AT session logs that #547's own boundary note observes nobody currently owns producing (it degrades honestly to `Not Evaluated`, so it caps outcomes rather than blocking work).

## 4. Staleness and open questions

- **Evidence commits resolve.** `646d5ae` (capstone) is on master, 16 commits behind tip; `c5cee1b` (#380) is the current tip. Spot-checked capstone Background claims at `646d5ae` — all hold: `WireRenderer.strokeFor` dashed/thick/thin exactly as described; `Theme.java` javadoc; `SimpleEditor` caret drawn in `JLSInfo.Palette.selectionColor` (found at ~line 2509); `docs/standards-adoption/03-accessibility-conformance.md` present; `docs/keyboard-a11y-verification.md` 146 lines; `WireValueChannelTest`, `ElementRegistry`, `ExtensionPointCatalogTest`, `NotificationRatchetTest`, `DialogCoverageRatchetTest`, `DialogConstructionSmokeTest`, `ElementFormDialog.installDialogBehavior`/`labelled(...)`, `Circuit.getElementsInStableOrder()` (line 479), `HdlExporter.buildModel` all exist as cited.
- **Capstone Open Question 3 is stated as blocking but is substantively resolved downstream.** #507 says OQ3 "blocks AC-3's narrative half being verifiable — name a distinct source before PF-4/#546 is implemented"; #747 AC (narrative ordering) already names WCAG 1.3.1 meaningful-sequence plus the **DIAGRAM Center Image Description Guidelines**, "cited by name in the test fixture." The capstone body should be REPLANned to record OQ3 as resolved-by-#747; no work is actually blocked.
- **OQ5's residual (naming) is the one genuinely open maintainer decision** — see Gap D3. OQ1 (partner validation) and OQ4 (sonification) correctly ride along.
- **Count drift, cosmetic:** #542 claims the ~113 hardcoded-black count is "recorded in `Theme.java`'s javadoc"; the javadoc says ~126 at both `646d5ae` and current master, while the live grep gives 113 — #542's number is right, its provenance claim is wrong. #543 AC-6 and #876 AC-2 say "the shipped adder lab"; no adder fixture ships — #542 AC-4 owns creating it (and says so); ordering (#543 after #542's task chain) makes this safe but the wording should say "the adder-lab fixture #542 creates."
- **Cost bands:** feature bands equal their task sums for #543, #544 (5.5-8 = exact sum), #547; #546 (4-6 vs task sum 4-6.5) and #549 (1-2 vs 1-2.5) are off by 0.5 mw at the top — noise. #544's band correctly reflects the #380-consumption discount the capstone's Cost section promises. No band is contradicted by its scope.

## Verdict

**ready-with-gaps.** The decomposition is complete and composes; the dependency graph is real and acyclic with no dead edges; the AC set is unusually falsification-hardened (seeded-defect fixture, falsifier-per-row ledger, injectivity-on-drawn-output). Start now, in the capstone's own funding order, with these actions dispatched first:

1. **File PF-7 and PF-8** (specs already written in #507; PF-7 is funding-order position 1) — owns AC-7/AC-8. *(blocking for capstone completion, not for start)*
2. **Close #731 as absorbed by #542** and re-point #543/#877's ordering edges — before PF-1 dispatch.
3. **Adjudicate `ColorVision` vs `CvdSimulation`** by REPLAN on #507 — before #542 AC-4 / #876 AC-1 implementation.
4. **REPLAN the CAP-24 coupling of PF-4** (admit #540→#536 onto the critical path, or re-cut #546/#749 onto the existing SVG path) — before PF-4 dispatch.
5. One-line reconciliation of #544's spike-apparatus note with #737's current two-tier body; record OQ3 as resolved-by-#747.
