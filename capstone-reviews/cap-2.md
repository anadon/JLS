**CAP-2 / #301 — readiness-to-undertake review.** Full tree walked 2026-08-16 via the GitHub API: the CAP body, its 7 native children and their TASKs, all 16 `requires_features` rows, the out-of-set prerequisites, and the sibling capstone rosters (#295, #296, #299). Load-bearing code claims re-checked against the working tree.

**Verdict: ready-with-gaps.** The promoted proof core (AC-1/AC-2 via #325 → #347 with #343's oracle and #335's calibration gate) is fully filed, open, acyclic, and startable today — #325, #343, #353, and now #324 and #357 are all `blocked_by: []`. But the corpus has moved under this body since it was written: four roster rows were rewritten in ways the body does not yet reflect, and the capstone's titular boot demonstration currently has **no owning feature** for its guest image. A REPLAN comment regenerating the roster annotations, the mermaid graph, and the AC-5/§1 ownership story should be the first act of execution — not a prerequisite for starting the unblocked rows.

---

## 1. Decomposition

**All 16 required rows are filed and open**, each carrying `serves_capstones: [301]` (or listing 301 among others) — verified individually: #202, #317, #319, #322, #324, #325, #327, #335, #343, #347, #353, #354, #357, #362, #363, #364. The #326 → #202 duplicate-closure is correctly absorbed: #202's machine block carries `requires_tasks: [278, 392, 425]` and the inherited `blocked_by: [317, 324, 325, 337, 343, 347, 364]` exactly as this body records. No required row is closed; no edge in any current machine block points at a closed issue (the stale `#330` references this body flags have since been resolved by #324's own rewrite — see §3). Sibling rosters confirm the shared-spine funding claims: #296 (CAP-00) requires #317/#335/#353/#354; #299 (CAP-01) requires #319/#357; #295 (CAP-03) shares the fourteen rows this body names.

**Gap D1 — the boot claim is unowned (the decisive finding).** This body states #343 "supplies the parity counterparty and the pinned kernel/DTB/initramfs." That is no longer true. #343's 2026-08-10 rewrite redirects FEAT-033 to an **external RISC-V oracle (Spike/Sail under RISCOF) over a subprocess boundary**, shrinks `jls.mach` to an adapter seam, and states in terms: the guest-image work "serves #301's boot claim rather than this feature's independence claim... Not required by this feature's Completion Criteria," asking for #395 to be re-homed "under whichever feature ends up owning the boot claim. **No such feature is filed yet.**" #395 (TASK-0071) was itself rewritten to split into (a) a bare-metal parity corpus — committed, fast-lane, "what #301 CAP-02 and #295 CAP-03's parity claim actually needs" — and (b) the Linux guest-boot image as an **informational, on-demand demo published as a release asset, gating nothing**. Meanwhile #395's native parent is #319 while its `part_of_feature: 343` stays as a self-declared "bookkeeping placeholder" — contradictory parentage flagged in #395's own text. Consequence: §1's walkthrough, AC-5, KC-02-7, and the DoD boxes that require AC-5 verified end-to-end are promises whose key input (the pinned kernel + initramfs) no required feature now owns. The body's own architecture survives this — it already demoted AC-5 to "demonstration milestone, not the proof" — but the DoD and the roster text contradict the filed corpus. The REPLAN must either adopt a boot-owner (re-home #395's demo half under #202 or a new feature) or demote AC-5/§1 from the DoD to match what #395 now is.

**Gap D2 — #343's scope, cost, and KC-02-5 no longer match this body.** The redirected #343 has zero filed TASKs, its prior 14–22 mw band is explicitly withdrawn by its own text ("A cost figure must be recorded before this task is filed"), and KC-02-5's coverage-floor criterion — written against an instruction-complete in-tree Java reimplementation — now targets a thin adapter package. KC-02-5's "cut to RV32I+Zicsr" fallback needs restating against the external-oracle design.

**Gap D3 — three required rows have decomposition that is unfiled or mismatched.** #357 (the largest row, 25–36 mw) has `planned_tasks: []` with a note that the previously filed #447/#473 "do not match the decomposition below as filed." #363's redirected primary scope (resume-by-deterministic-replay) is carried in two **unfiled** planned tasks, while its `requires_tasks: [426, 456]` point at the superseded serialize-everything design and are kept only as future-accelerator work. #354's TASK-0011/0012/0013/0015 are unfiled (only #469/TASK-0014 is filed). None of this blocks start — the corpus convention permits planned_tasks — but these are the rows where "every required feature is scoped" is currently thinnest.

No double-ownership was found among the sixteen rows themselves; the one ownership contradiction is #395 (D1).

## 2. Acceptance-criteria composition

**The required gates compose.** AC-1 maps to #325's filed TASKs (#389 sealed boundary seam, #421 mid-run switch with named divergence); AC-3's wrong-binding rejection is #421's explicit gate and KC-02-4 backs it. AC-2 maps to #347's filed TASKs (#390 RVFI-shaped RetireRecord validated against the `riscv/` reference; #423 exact-index/exact-field failure naming with a capped, printed exclusion set) plus #343's RISCOF corpus and #395's byte-reproducible bare-metal parity corpus. AC-4 (clock-period guard) is concretely owned: #425 (TASK-0080 under #202) states "changing the clock period changes no output byte" in its title. AC-7 is #442's byte-identity gate over every golden, filed under #335/#362's roster. AC-8/KC-02-1 are correctly flagged by the body itself as provisional until #335 lands; #335 carries six filed TASKs (#377, #378, #379, #407, #413, #442).

**Where every child could pass while the capstone fails:** only through D1 — every required feature could close and the §1 walkthrough would still be unexecutable for want of a guest image, because the artifact that supplies it is a non-gating demo task. The body's promotion of AC-1/AC-2 over AC-5 means the *claim* survives that failure mode by design, but the DoD as written does not. Second, smaller composition seam: the device differential (§3 risk 1) relies on AC-1 being applied to the UART/CLINT pair, but #343's rewrite removed the `Uart16550Model`/`ClintModel` counterparties this body names (the external oracle models an ISA, not a UART) — the device-parity counterparty needs re-deriving in the REPLAN.

## 3. Dependency chains

**Acyclic — verified.** Walked every current machine block's `blocked_by`/`blocks` over the required set: 353 → {335, 354, 317}; 354 → 317; 363 → 317; 335 → {347, 362}; 325 → {202, 347}; 343 → {202, 347}; 347 → 202; 324 → 202; 317 → 202; 364 → 202; 327 ← 336; 322 ← 879. No back edges; #295's independently recorded 57-node DAG walk corroborates. No current edge targets a closed issue.

**But the body's recorded graph is stale in seven places** (in each case the feature's own machine block wins, per this body's §5 protocol):

1. **#324 is now `blocked_by: []`** (body says `[316, 330, 354]`). The "#330 needs repointing" chore is moot; #354 dropped its `blocks: 324` edge 2026-08-16 (#324's committed scope is file-bound/batch-driven). The mermaid edge F006 → F032 is gone. #324 is startable immediately — good news the body doesn't yet claim.
2. **#357 is now `blocked_by: []`** (body says `[318, 319, 340]`) — its authored-plane redirect dissolved all three, per its own and #319's blocks.
3. **#363 is now `blocked_by: []`** (body says `[319]`) — resume-by-replay writes no bulk state and needs no section carrier.
4. **#364 is now `blocked_by: []`** (body says `[319]`) — narrowed to the write-mask input. The mermaid edges F013 → F017/F035/F036 are all gone; #319's own block records the removals ("357, 363, 364 removed 2026-08-16").
5. **#322 is now `blocked_by: [879]`** (body says `[]`, "no ordering edge... in this set"). #879 (TASK-C232-2, with #878, owned by #232) is a **new out-of-set prerequisite** on a required row, reached via #442 → #879.
6. **New edge: #317 `blocked_by: [353, 354, 363]`** — #363 joined the critical path to #317 and hence to #202 (checkpoint-chain segmentation is the redirected long-run mechanism). The mermaid graph lacks F035 → F007.
7. **The out-of-set prerequisite list has shrunk**: of the body's eight (#315, #316, #318, #334, #336, #340, #212/#223), only **#336 → #327** and **#337 → #202** survive as filed edges, joined by the new **#878/#879 → #322**. #334 → #319 dissolved (#319 `blocked_by: []`), taking #315's transitive path with it; #316, #318, #340, #212/#223 all dropped. The DoD checklist naming the old six is stale — in the favorable direction.

**External prerequisite:** the Spike/Sail oracle is existing, digest-pinnable open-source software, not unfunded development — a supply-chain surface, not a blocker. Earliest-startable rows today: #353, #325, #343, #324, #357 (all `blocked_by: []`).

## 4. Staleness and open questions

- **`evidence_commit: 333523a` does not resolve on the default branch** — it is 15 commits *ahead* of master (`c5cee1b`, the actual tip), on an unmerged line. The DoD requires every citation to resolve on the default branch. Mitigating: every load-bearing anchor spot-checked holds on the tree anyway — `Memory.java:1224` `DENSE_CAPACITY_LIMIT = 1 << 22` (B-08), `FileAbstractor.java:65` 64 MiB cap and `Circuit.java:102` `FORMAT_VERSION = 2` (B-09), `Circuit.java:1466` `save(PrintWriter)` (B-03), the four `+=` concatenations in `SigSim.java` (B-06; note the file is at `src/jls/elem/SigSim.java`, not `sim/`), zero `timeout-minutes` in `.github/` (B-07), no `src/jls/mach/`, no Console element (B-01/B-02), no `MeasurementGate`/`CalibrationFixture` (the #335 residual). B-01…B-11 remain live findings.
- **Cost band partially withdrawn by its own sources.** The 159–253 mw standalone sum includes #343 at 14–22 mw, a figure #343's rewrite explicitly disclaims for its redirected scope, and #202's 12–26 slice which this body already flags as re-derive-at-pickup. The band's *shape* (roadmap work ≫ CPU-specific work) survives; the arithmetic needs re-running.
- **Open questions that block start: none.** OQ-1 (Console: element vs subcircuit) blocks AC-1-on-devices acceptance, not the startable rows; OQ-2 (guest config as contract) blocks AC-5, which is downstream of D1 anyway; OQ-6 (ratify parity discipline inside #347) is correctly placed as #347's first task; OQ-8's definition-level fidelity generalization is exactly what #325's filed TASK split (#389 seals the definition boundary first) is compatible with.

## Verdict: **ready-with-gaps** — start now

The proof spine this capstone stakes its claim on (#325 → #347, fed by #343's external oracle, #335's calibration, #395's parity corpus) is filed, open, acyclic, decomposed to TASK level where it matters most, and its head rows are unblocked today. Gaps, in priority order, all resolvable by one REPLAN comment on this issue plus normal feature-level work:

1. **D1**: name the owner of the guest-boot image (adopt/rehome #395's demo half, or demote AC-5/§1 in the DoD to match #395's non-gating status) and resolve #395's contradictory parentage (#319 native parent vs `part_of_feature: 343`).
2. Regenerate the roster annotations and mermaid graph against the features' current machine blocks (seven deltas in §3, including the new #322 ← #879 and #317 ← #363 edges).
3. Restate KC-02-5 and re-derive the #343 and #202 cost rows against the external-oracle redirect (D2); re-derive the device-parity counterparty in §3 risk 1.
4. File the missing decomposition on #357 and #363's primary scope before those rows are picked up (D3).
5. Re-pin `evidence_commit` to a commit reachable from master.
