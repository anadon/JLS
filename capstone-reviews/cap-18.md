# CAP-18 (#313) readiness review

**Verdict: ready-with-gaps.** Every required feature and task is filed and open, the child issues are individually well-scoped with strong falsifiable ACs, the dependency graph is acyclic with no edge at a closed issue, and rung 1's first task (#603) is startable at HEAD today. But the capstone body is now *behind* its own children on several load-bearing points — most seriously, it still requires FEAT-014 (#318, the single largest cost row at 11–17 mw) that no child consumes any more, and its AC-7 contradicts the design #487/#609 deliberately chose. A `REPLAN:` comment on #313 regenerating the machine block, graph, and cost sums should precede full orchestration; nothing needs new decomposition work first.

Everything below was verified against the live machine blocks of all 13 features, all 11 rung TASKs, #468, and the code at master (`c5cee1b`, confirmed identical to the default-branch tip via the compare API).

## 1. Decomposition

**Sound at the child tier; the required set is over-inclusive at the capstone tier.**

- All eight `requires_features` rows (#318, #319, #336, #366, #367, #486, #487, #490) are filed and **open**, and each declares `serves_capstones: [... 313 ...]` — verified individually, so every composition edge is mirrored as claimed.
- The native hierarchy is complete for the three rungs: #486 → #603/#605/#607, #487 → #609/#611/#613/#616, #490 → #618/#620/#623/#625 — all eleven TASKs open. The check-engine surface (`-check` verb + finding-record framework), which #486 lists as a fourth planned row with no task id, is explicitly absorbed into #607's scope and band ("neither is filed as a separate issue yet"); acceptable, documented, but #486's machine block still says `requires_tasks: []  # no task ids exist for this feature`, which is stale against the filed sub-issue roster (same pattern on #487 and #490).
- **Over-inclusion — #318 (FEAT-014 RESIDUAL) is required by the CAP but consumed by no child.** The CAP's stated reason ("step 5's return path has no addressing scheme") is superseded: #487's machine block says #318 "is deliberately absent" from its `blocked_by`, and TASK-C487-4 (#616) implements back-annotation as a CLI-supplied, non-persisted `--routed` input keyed on #336's stable net names, stating outright "No dependency on #318". At 11–17 mw, #318 is the largest row in the required-cost table; keeping it in `requires_features` needs a recorded reason, or a REPLAN should drop it (moving the required sum from 35–60 mw to roughly 24–43 mw). Note #318 itself still declares `serves_capstones: [... 313]`, so both ends of this stale edge need the same pass.
- **Contested-required — the real-valued trace row.** The CAP cost table carries "FEAT-059's real-valued trace row (§3 risk 4), 0.5–1 mw, required; shared with #303/#305". Its task, #620 (TASK-C490-2), is titled and machine-blocked "**on hold, currently unfunded**", with an explicit recommendation not to start until some issue commits to producing a real-valued sample series; #490 replaced it with VCD `$var real` emission viewed in GTKWave. One of the two must give — either the CAP's "required" grading and cost row, or #620's disposition.
- **One unfiled piece:** #490's planned row 3 (VCD `$var real` emission per `docs/batch-interface.md` §4) has no filed TASK — #623's machine block calls it "the not-yet-filed VCD `$var real` task". With #620 shelved, this is the only remaining observation channel for the computed waveform beyond the goldened CSV; it should be filed before rung 2 is orchestrated.
- No double-ownership found: AC ownership at task tier partitions cleanly (see §2), and #366's dedup note correctly avoids requiring #400 twice.

## 2. Acceptance criteria

**Task-tier ACs are strong — exact numbers, failing-direction-first, vacuity named — and cover all five §1 steps. Two capstone ACs no longer compose with the children as designed.**

- Coverage map, verified in the task bodies: step 1 → #607 (budget + refined verdict, three-tier reporting; CAP AC-1); steps 2–4 → #618 + #623 (four-termination corpus, 1e-12 lattice check, `EdgeRateCollapseTest` pinning 166.7% → 132.3% → <105%; CAP AC-2/AC-3) plus #625 (the naming diagnostic); step 5 → #611 (`.kicad_dru` golden, keyword-accepted; CAP AC-4) and #613 (external DRC, failing direction first, digest-pinned container; CAP AC-5), with #616 closing the routed-length loop. §3 risk 8's missing independent oracle is now real scoped work: #623 requires "a genuinely independent oracle (not a same-formula cross-check)" — the CAP's claim that the ngspice check is "not yet scoped as required work on #490" is stale in the good direction.
- **AC-7 conflicts with #487/#609's chosen design and cannot pass as written.** AC-7 demands an older reader open an annotated circuit "structurally, with a clean diagnostic naming the skipped optional section — **not a refusal**". But #487's first planned task and #609 deliberately carry the constraint set under FEAT-013's `required` must-understand marker precisely so an older reader "**refuses by name**, it is never silently dropped" — and the CAP's own §3 risk 2 argues that refusal is the correct behavior for a constraint. Meanwhile #605 moved the rung-1 declared attributes into a **sidecar** keyed on stable net names (the K18-5 fallback, taken silently at task tier), so the FEAT-058 half of AC-7 is now vacuous. AC-7 needs splitting: attributes (sidecar; older readers trivially unaffected) vs. constraints (refusal-by-name *is* the acceptance behavior).
- **AC-6 is vacuous as written.** "Run the check surface over the shipped `examples/` corpus" — `examples/` contains one Python autograder and **zero `.jls` circuits** (verified on disk), so "not assessable on every circuit" is satisfied by an empty iteration. #607 already fixes this correctly (runs over the four real `.jls` fixtures in `test/fixtures/` + `riscv/gui/cpu.jls`, adds a fixture that must fire, declares populating `examples/` out of scope); the CAP should adopt the child's formulation. Minor: #486 cross-labels this silence criterion "#313 AC-7" when it is AC-6.

## 3. Dependency chains

**Acyclic; no edge at a closed or redirected issue (#307's removal was done correctly; all 25 issues touched here are open). But the CAP's `blocked_by` is stale in two of five rows, and one edge is contested four ways.**

- Live chain: #603 is startable now (`ordering_after: []`); #605/#607 wait on #336 (← #315); rung 2 #618 waits on #367 + #486; rung 3 runs #609 (← #486, #319, #336) → #611 (← also #366) → #613, with #616 in parallel. All real, all open.
- **Stale CAP `blocked_by` [315, 334, 337, 349, 365]:** #319 now declares `blocked_by: []` (its FEAT-003 gate was removed as a recorded correction), so **#334 is no longer a prerequisite** via the stated path; #318 now declares `blocked_by: [468]` (TASK-0007, open), so **#337 no longer gates it and #468 appears nowhere in the CAP's accounting**. The CAP's own re-planning protocol ("a required feature's own `blocked_by` changes → re-derive") has not been executed. #315→#336, #349→#366, #365→#366 remain live and mirrored.
- **The #367 → rung-1 edge has four owners and no agreement:** #367 declares `blocks: [486, 490]` claiming the mirror already exists; #486 declares `blocked_by: []` with an explicit rationale (declared quantities parsed from strings, never read from `now`); #605 asserts the lint task *does* need #367's time base for `l_crit = v·t_r/6`; #607 — the lint task — omits #367 from `ordering_after`. The CAP graph draws F047→F058 as real ("both need units"). Someone must own this edge before #607 is scheduled; #367→#490 is genuinely mirrored and uncontested.
- **CAP graph edges rejected by their targets:** F004→F058 and F013→F058 (rejected by #486's `blocked_by: []` rationale) and F014→F060 (explicitly disclaimed by #487 and #616). The graph note admits only the 058→060→059 sequencing question; these three edges are also stale.
- **Sequencing (Open Question 1 / DoD item) unresolved but unenforced:** #487 still declares `blocks: [490]` — admitted in-body as **not mirrored** (#490's live `blocked_by` is `[367, 486]`) — so the old export-before-kernel ordering survives only one-sided. Similarly #487's `blocked_by` rows on #336/#366/#319 are not mirrored on those issues' `blocks` lists (admitted in #487 as "reported, not edited here"). No cycle either way; the DoD item ("re-sequenced or recommendation withdrawn") remains open.
- **No unfunded external prerequisite on the critical path:** the kicad-cli container harness the CAP flags as unfunded is absorbed into #613's own scope and band. One named open question sits on rung 3's path: #611 must not start until #336's Open Question 1 (the net-name digest) is answered — an intra-programme decision, not an external blocker.

## 4. Staleness and gaps

- **Evidence commits:** the CAP's `2d0ca9d` resolves nowhere (deleted branch) — acknowledged in-body, re-anchor owed. #486 re-anchored to `c5cee1b`, verified **identical to the default-branch tip**. #487/#490 still carry the dead `2d0ca9d`; their claimed re-verification commits (`646d5ae8`, `4320ef8` — the latter annotated "origin/master" in #620) are 16–17 commits **ahead of master on an unmerged branch**: they resolve in-repo but fail the DoD's "resolves on the default branch" bar today.
- **`docs/plan/` is still absent from master** (verified), so every cost band remains a non-re-derivable maintainer estimate, exactly as the CAP warns; the DoD's merge-or-rebuild decision is still open.
- **Code claims all verify at HEAD:** `JLSStart.java` FLAGS = `h b i s t d p v r vcd export board pins savetext`, no check verb; `WireNet` fields exactly `ends/wires/bits/hasinput/triState`, single `BitSet value`; `Element.java` file-local `id` reassigned on save beside permanent `stableId` (#165); `docs/simulation-semantics.md` dimensionless time / BitSet-or-null domain; `docs/file-format.md` silent-drop valve; `PaletteContractTest` totality with the three-tag exclusion set. The falsification guard ("every step fails today") holds.
- **The CAP's central §3 risk 1 ("the feature issues haven't caught up") is inverted:** the children have caught up and moved past the CAP. #486 is already the widened check-engine surface (rule registry, first tenant); #490 is already headless, its palette half formally retired (#625), the ngspice oracle scoped (#623), the drawable element in its Rejected section. The DoD reconciliation checkbox is effectively satisfiable — but the CAP text still tells a reader the children carry the old shape, and #486 §"FIRST of three rungs" still describes #490 as committing "a frozen save tag, a mandatory palette entry... a permanent K9 obligation", which is now false. Cosmetic sibling: #486's mermaid labels #487 "RUNG 2" where the CAP calls it rung 3.
- **Cost drift:** #486's task-band sum (1–2 + 1–2 + 2–4 = 4–8 mw) exceeds its 3–6 mw feature band at the top; the #620 0.5–1 mw "required" row is contradicted (above); dropping #318 changes the required and cumulative sums materially; the cumulative prerequisite sum still counts #334 (2–4) and #337 (4–7) that no longer gate anything here, and omits #468.

## Verdict and start conditions

**ready-with-gaps.** Start now: #603 immediately; the live spine (#315 → #336, #319, #367, #349/#365 → #366, #468) is real, open, correctly mirrored work whichever way the gaps resolve. Before the full tree is handed to an orchestrator, one `REPLAN:` comment on #313 should: (1) decide #318 in/out of `requires_features` and re-print the sums; (2) re-derive `blocked_by` from the children's live machine blocks (drop #334/#337, account #468); (3) split AC-7 into the sidecar-attribute and refuse-by-name-constraint halves and re-word AC-6 onto the fixture corpus; (4) settle the #367→#486/#607 edge and the 059/060 sequencing one-sidedness; (5) resolve the #620-vs-cost-table contradiction and file the VCD `$var real` task; (6) re-anchor the evidence commit. All six are edits to existing issues; none is new decomposition.
